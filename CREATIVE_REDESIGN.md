# CREATIVE_REDESIGN — من مولّد صحيح إلى محرّر فعلي

> **حالة الوثيقة:** تصميم فقط. ولا سطر كود انتغيّر، ولا commit.
> **الأساس:** `a0b3e99` على `phase3/agent-layer` · شجرة نظيفة.
> **القاعدة الحاكمة الجديدة:** `Correctness ≠ Creative Quality`.

---

## ١· تقييم البنية الحالية

المسار اليوم بيمشي بسبع مراحل، وكل وحدة بتشتغل صح **بمعزل**:

```
صوت + نص + SRT
  ──► alignment.json        السلطة الزمنية
  ──► ScriptAgent           وين ينقسم النص   (بلا زمن)
  ──► VisualAgent+Resolver  أي ملف           (ببصمة ورخصة)
  ──► TypographyAgent       خط · لون · حركة
  ──► quantize()            ثوانٍ ──► إطارات   ← الحدّ الوحيد
  ──► render.build_command  أمر ffmpeg واحد
  ──► verify_output         فحص الملف نفسه
```

**١٥١٩ فحصًا** · **٦٩٣** منها لهالنظام · ffmpeg 7.0.2.

والتقييم الصادق: **الطبقة التنفيذية ممتازة، والطبقة التحريرية غير موجودة.**
النظام ما بياخد ولا قرار مونتاج — بياخد قرارات **تعبئة**: أي صورة تحت أي جملة.
وهاد مش نقص جودة، هاد **نقص طبقة كاملة**.

---

## ٢· لماذا تبدو الريلز سيئة

من فحص ثمانية مخرَجات حقيقية بهالجلسة، لا من رأي:

| العرَض | القياس |
|---|---|
| إحساس «عرض شرائح» | **٧ لقطات / ٤٠ ثانية** · متوسط **٥.٨s** — الريلز المحترف ١.٥–٢.٥s |
| رتابة الحركة | مفردة الحركة كلها **٥ قيم**، واستُعملت `zoom_in` بـ٦٠٪ من اللقطات |
| تكرار بصري | أصل واحد حمل **٤ لقطات** من ٧ · وأصلان تطابقا بفرق **٢.٠٢/٢٥٥** |
| قفزات غير مبرَّرة | **٨ rewind** بتشغيلة وحدة — قفزة داخل نفس الملف بلا قطع مقصود |
| صوت آلي | **٢١ مؤثرًا / ٤٠ ثانية** — whoosh عند كل حدّ، بلا قرار |
| هوك ميت | أول ٤.٥ ثواني = **لقطة واحدة ساكنة** · ولا سبب للبقاء |
| نهاية بلا حسم | آخر لقطة تنتهي مع آخر كلمة — ولا امتداد ولا إمساك |
| كابشن بلا تصميم | كل الكلمات بنفس الحجم واللون والحركة، بلا استثناء |

**والنتيجة الجامعة:** كل جملة بتاخد نفس المعاملة بالضبط. مش «قالب رديء» —
**قالب واحد بلا استثناءات**، وهاد بيعطي بصمة آلية بيلتقطها المشاهد بثانية.

---

## ٣· الأسباب الجذرية مرتّبة بالتأثير

### RC-1 · القطع البصري مساوٍ للقطع النصّي بالبناء

**`ai_pipeline/timeline/quantize.py:65`**

```python
cuts = [0] + [s.f_start for s in text[1:]] + [total_frames]
```

هالسطر بيخلق **تقابلًا واحد-لواحد** بين مدى النص ومدى الصورة. النتيجة:

- **مستحيل** التعبير عن لقطة تمتد على ثلاث جمل.
- **مستحيل** التعبير عن ثلاث لقطات داخل جملة.
- عدد اللقطات = عدد المقاطع النصّية، دائمًا.

هذا **أعمق سبب في المشروع كله**، وكل ما فوقه أعراض. ولا وكيل ولا resolver ولا
تحسين مؤثرات بيقدر يتجاوزه — النموذج الزمني **لا يستطيع تمثيل** المونتاج.

> ⚠️ `ai_pipeline/timeline/` **شجرة مجمَّدة**. فتحها قرارك أنت وحدك.
> بلا فتحها، كل ما تحت هذه النقطة تلميع.

### RC-2 · فقر مفردات النيّة

مفردات الوكيل كلها بـ`agents/schemas.py`:

```
ShotType   5 قيم        Motion   5 قيم
Palette    4 قيم        Animation 4 قيم
```

يعني كل ما يقدر الوكيل يقوله عن **لقطة** هو ٥×٤×٥ = ١٠٠ تركيبة، بلا
مفهوم للطاقة ولا التصعيد ولا الدور السردي ولا العلاقة باللقطة السابقة.
**الوكيل ما بيقدر يفكّر تحريريًا لأن لغته ما فيها كلمات للمونتاج.**

### RC-3 · حقل ميت: `visual_mood_prompt`

بينكتب بـ`SegmentProposal`، وبينتقل لعقد Phase 1، وبينحطّ بـprompt الوكيل
البصري كنصّ — **وما بينوصل للـResolver ولا للراسم أبدًا**. الـResolver
بيقرأ `query` و`must_include` بس. فأغنى حقل عندنا عن **النيّة** بيتبخّر
بمنتصف المسار.

### RC-4 · الاختيار بمطابقة كلمات

`resolver.score()` بيحسب:

```
4 × |تقاطع كلمات الاستعلام مع كلمات الملف|
+ 3 × (نفس shot_type)
+ 2 × (نفس palette)
```

يعني **اسم الملف هو الذكاء كله**. ولا مفهوم لما فيه اللقطة فعليًا، ولا
لعلاقتها باللي قبلها، ولا لصلاحيتها للحركة أو للكابشن.

### RC-5 · مكتبة أصول فقيرة وغير موصوفة

مقيس: **٤ مشاهد مختلفة فعليًا** لأربعين ثانية، وأطولها (٣٤.٧٥s) **لقطة
واحدة** — بانّة بطيئة على نفس الشاشة. والقاعدة المقيسة:
**مدة اللقطة ≥ ١.٧٥× ما تستهلكه**، وإلا بتتفتّت لقفزات.

### RC-6 · المؤثر تابع للحدّ لا للمعنى

`plan_cues` بتعلّق مؤثرًا على **كل** حدّ لقطة وكل ظهور كابشن. القرار
الوحيد هو `min_gap`. فالصوت بيصير سجادة لا تعليقًا.

### RC-7 · الكابشن بلا مفهوم «كلمة مهمة»

`TypographyContract` بيعطي **قيمة واحدة لكل مقطع**: حجم، لون، حركة.
ما في حقل يقول «هذه الكلمة أهم من جيرانها». فالإبراز الوحيد المتاح هو
تلوين الكاريوكي الزمني — وهو **آلي بالتعريف**، بيمشي على الكل بالتساوي.

### RC-8 · الصوت المصدر نفسه سقف

مقيس على تسجيلك: **١٠٦ كلمة/دقيقة** (الريلز ١٥٠–١٩٠) · **٣٦٪ سكوت** ·
مدى التشديد **٢٥٪** بس. والأخطر: الـHook شدّته `1.50` والـPayoff `1.46`
— **أدنى جملتين بالملف**. ديناميكية الصوت **معكوسة** عن البنية السردية.

> **نتيجة تصميمية مباشرة:** `beat.intensity` **لا يُستنتج من الصوت**.
> لازم يجي من فهم النص، والصورة تعوّض ما لم يعطه الصوت.

---

## ٤· ما هو جيد ويجب ألا يُكسر

| المبدأ | لماذا يبقى |
|---|---|
| **الفهرس هو الزمن** | `sample = frame × (sr ÷ fps)` — ضرب صحيح. كل انزياح صرفنا مراحل نسدّه سببه حساب بالثواني |
| **العقد يحمل القرارات لا المشتقّات** | غياب حقول الزمن من `segments.json` بيجعل ست حالات فشل **مستحيلة بالبناء** |
| **النص المصدر غير قابل للتغيير** | مقارنة بايت-بايت + حارسان مستقلان |
| **طبّع قبل أن تفهرس** | أربع تطبيقات، وكل واحدة انولدت من خلل صامت |
| **الوكيل لا يكتب توقيتًا** | حجر الأساس. **يبقى بلا استثناء** |
| **مفردات مغلقة** | الخطر ليس ما يرفضه الـschema بل ما **لا يقدر النموذج التعبير عنه** |
| **فحص المخرَج لا الخطة** | `verify_output` يفكّ الملف ويقيسه |
| **اكسر الحارس عمدًا** | ١٨/١٨ طفرة. الطقم الأخضر لا يثبت شيئًا |
| **`shared/` بلا منطق** | إعادة تصدير فقط، والاتجاه أحادي |

**التوسيع يبني فوق هذه، لا بدلها.** كل واحدة انولدت من خلل حقيقي مقيس.

---

## ٥· بنية Creative Edit Plan المقترحة

عقد **جديد** بين `segments` و`timeline`، ولا يلمس أيًّا منهما:

```
alignment ──► segments ──► ┌─────────────────────┐ ──► timeline ──► render
                           │  Creative Edit Plan │
                           │  (نوايا لا أرقام)   │
                           └─────────────────────┘
                                     ▲
                          beats · shots · emphasis
                          motion · transitions · cues
```

**ثلاث طبقات، حدودها صارمة:**

| الطبقة | تقرّر | ممنوعة من |
|---|---|---|
| **Editorial Agent** | الدور السردي · الأهمية · نيّة اللقطة · التفضيل النسبي للمدة | أي رقم تنفيذي |
| **Compiler** (حتمي) | يحوّل التفضيلات النسبية لأرقام ضمن قيود مقيسة | اختراع نيّة |
| **Renderer** (نقي) | نفس ما هو اليوم | كل شيء آخر |

> **`plan.json` منفصل عن `timeline.json` تمامًا.** الأول نوايا، والثاني
> إطارات. والمترجم بينهما هو **الوحيد** الذي يعبر.

---

## ٦· الـSchemas المقترحة

### `EditPlan`

```python
class BeatProposal(StrictModel):
    beat_id: int                      # ≥ 1
    segment_ids: tuple[int, ...]      # المقاطع النصّية التي يغطّيها
    role: BeatRole                    # hook|problem|escalation|turn|
                                      # solution|demonstration|payoff|cta
    importance: Importance            # low|normal|high|peak
    energy: Energy                    # calm|building|driving|release
    shot_count: ShotCount             # single|few|many   ← نسبي لا رقم
    pace: Pace                        # hold|steady|accelerate
    visual_idea: str                  # ≤ 300 — النيّة، لا الملف

class ShotProposal(StrictModel):
    shot_id: int
    beat_id: int
    order: int                        # داخل الـbeat
    weight: ShotWeight                # brief|normal|extended  ← نسبي
    intent: ShotIntent                # establish|detail|action|
                                      # reaction|reveal|hold
    motion: MotionIntent              # static|push|pull|drift|follow|micro
    entry: TransitionIntent           # cut|soft|match|hard
    continuity: Continuity            # new|continue|return

class EmphasisProposal(StrictModel):
    segment_id: int
    word_index: int                   # فهرس داخل المقطع
    level: EmphasisLevel              # normal|strong|peak

class CueProposal(StrictModel):
    beat_id: int
    at: CueAnchor                     # beat_start|shot_change|
                                      # emphasis|beat_end
    kind: CueKind                     # impact|whoosh|pop|riser|silence
    weight: CueWeight                 # subtle|normal|heavy

class EditPlan(StrictModel):
    beats: tuple[BeatProposal, ...]
    shots: tuple[ShotProposal, ...]
    emphasis: tuple[EmphasisProposal, ...] = ()
    cues: tuple[CueProposal, ...] = ()
```

**ما هو غائب عمدًا — وهذا مصدر قوة الـschema:**

| غائب | مالكه |
|---|---|
| `start` · `end` · `duration` · `frame` | `quantize` |
| `seconds` بأي شكل | المترجم |
| `file_path` · `provider_ref` · `sha256` | Resolver |
| `font_size` · `#hex` · `y_ratio` | Theme |
| `gain` · `sample` | `sfx.py` |

`weight: brief|normal|extended` **نسبي بقصد** — المترجم بيحوّله لإطارات
حسب مدة الـbeat وقيود المدة الدنيا/القصوى. النموذج ما بيقدر يقول «١.٢
ثانية» لأن الحقل غير موجود.

---

## ٧· مسؤوليات الوكلاء الجديدة

| الوكيل | اليوم | المقترح |
|---|---|---|
| **Script** | يقسم النص | يقسم **ويصنّف الدور السردي** — beats |
| **Editorial** ⭐ | — | **جديد**: يوزّع اللقطات على الـbeats، يحدد الإيقاع والتصعيد والإبراز |
| **Visual** | نيّة بحث لكل مقطع | نيّة بحث لكل **لقطة**، مع وعي بالسابقة |
| **Typography** | خط/لون/حركة لكل مقطع | نفسه + **الكلمات المُبرَزة** |

**الـEditorial Agent هو الإضافة الوحيدة الحقيقية.** وهو الوحيد الذي
«يفكّر كمحرّر»: يقرأ النص والأدوار، ويقرّر أين يكسر وأين يمسك.

---

## ٨· إعادة تصميم Resolver

**التشخيص: `E — كل ما سبق`، لكن بترتيب أثر محدّد:**

| المصدر | الوزن | الدليل |
|---|---|---|
| **D · مكتبة الأصول** | **الأكبر** | ٤ مشاهد لأربعين ثانية. لا خوارزمية تعوّض |
| **C · التخطيط الإبداعي** | كبير | ما في مفهوم «تسلسل لقطات» أصلًا |
| **B · الترجيح** | متوسط | مطابقة كلمات + ٣ + ٢. بلا استمرارية ولا عقوبة تكرار |
| **A · الاسترجاع** | صغير | يعمل صح على كتالوج صغير |

**الترجيح المقترح** — كل حدّ منفصل ومفحوص وحده:

```
score = w₁·semantic_match      تطابق النيّة بالوسوم الدلالية
      + w₂·shot_scale_fit      هل مقياس اللقطة يناسب intent؟
      + w₃·motion_affordance   هل تحتمل الحركة المطلوبة؟
      + w₄·composition_fit     هل فيها مساحة آمنة للكابشن؟
      + w₅·duration_fit        هل ≥ 1.75× المطلوب؟   ← مقيس
      + w₆·continuity_bonus    تناغم مع اللقطة السابقة
      − p₁·repetition_penalty  ← §١٣
      − p₂·recency_penalty     كم قريبًا استُعمل
```

> **قاعدة إلزامية:** كل حدّ يُفحص **معزولًا** بطفرة تثبت أنه يغيّر الاختيار.
> مجموع أوزان بلا فحص لكل حدّ = رقم يبدو ذكيًا وهو عشوائي.

---

## ٩· نظام الإيقاع (Pacing)

**قيود حتمية، لا اقتراحات.** المترجم يرفض الخطة التي تكسرها:

| القيد | القيمة | المصدر |
|---|---|---|
| مدة اللقطة | `0.6s ≤ d ≤ 4.0s` | حدّ إدراكي |
| تباين المدد | معامل اختلاف **≥ 0.25** | يمنع «كل اللقطات متساوية» |
| تكرار الحركة | ولا **٣ لقطات متتالية** بنفس الحركة | يمنع zoom_in الأبدي |
| هيمنة الحركة | ولا حركة **> ٤٥٪** من اللقطات | يمنع الرتابة |
| كثافة المؤثرات | مؤثر واحد كحدّ أقصى كل **2.5s** | يمنع السجادة |
| اللقطات الساكنة | **≥ ١٥٪** من الزمن | الصمت البصري قرار |
| تصعيد الـbeats | `energy` غير متناقصة حتى الـpayoff | يمنع الترهّل |

**والفرق عن العشوائية:** هذه قيود على **التوزيع**، والقرار داخلها للوكيل.
عشوائية = اختيار بلا سبب. تنوّع = **رفض التطابق** مع بقاء السبب.

---

## ١٠· النظام الإبداعي للكابشن

**`TypographyContract` لا يُلمس.** الإضافة **طبقة فوقه**:

```python
class CaptionEmphasis(StrictModel):
    segment_id: int
    word_index: int
    level: Literal["normal", "strong", "peak"]
```

المترجم يحوّل `level` إلى قيم **موجودة أصلًا** بالعقد الحالي:

| المستوى | الترجمة عبر الـtheme |
|---|---|
| `normal` | `size_step 0` · `color_role primary` |
| `strong` | `size_step +1` · `color_role accent` |
| `peak` | `size_step +2` · `accent` · `animation fade_in_scale` |

> **لماذا طبقة لا حقل:** إضافة `emphasis` داخل `TypographyContract` تفتح
> عقدًا مجمَّدًا وتخلط «نيّة» بـ«قيمة». الطبقة الخارجية تُترجَم إلى نفس
> الحقول الموجودة — فالعقد يبقى مغلقًا والراسم لا يتغيّر.

**والقيود العربية تبقى بحرفها:** ولا `arabic_reshaper` ولا `python-bidi` ·
`libraqm` هو المشكّل · كل صور التسلسل بنفس المقاس · `pad_to_box` قبل الربط.

---

## ١١· النظام الإبداعي للصوت

**المؤثر يصير مربوطًا بـ`CueProposal`، لا بحدّ اللقطة.**

| القاعدة | التنفيذ |
|---|---|
| المؤثر قرار لا نتيجة | لا مؤثر بلا `CueProposal` صريح من الوكيل |
| الصمت قرار | `kind: silence` يمنع أي مؤثر بنافذته |
| كثافة قصوى | مؤثر / **2.5s** — يرفض المترجم الزيادة |
| الوزن نسبي | `subtle|normal|heavy` ← يترجم لـ`gain` الموجود |

**الطبقة المقيسة تحته لا تتغيّر:** `normalize=0` إلزامية · `all=1`
و`aformat` قبل `adelay` · ولا `alimiter` · الهامش المحسوب `0.925 < 1.0`.

---

## ١٢· إعادة تصميم بيانات الأصول

`CatalogEntry` **يتوسّع** — والحقول الحالية تبقى كما هي:

```python
# يبقى: provider · provider_ref · path · license · sha256 · probe
class AssetAnalysis(StrictModel):
    subject: Literal["none","person","hands","object","screen","place"]
    environment: Literal["indoor","outdoor","studio","abstract"]
    action: Literal["static","slow","moderate","fast"]
    camera: Literal["locked","handheld","pan","tilt","dolly","drone"]
    shot_scale: Literal["extreme_wide","wide","medium","close","macro"]
    composition: Literal["centered","left","right","full_bleed"]
    energy: Literal["calm","neutral","dynamic","intense"]
    safe_caption_area: Literal["bottom","top","none"]
    best_window: tuple[float, float]     # أفضل مقطع داخل الملف
    semantic_tags: tuple[str, ...]
```

> **`best_window` هو الحقل الأعلى قيمة.** اليوم `in_point_for` بتتوسّط
> رياضيًا — بلا علم بأين الحركة. مع هذا الحقل يصير الاختيار عن
> **أفضل ثانيتين** لا عن منتصف الملف.

**من أين تأتي هذه البيانات:** ثلاثة مصادر، بهذا الترتيب — **يدوي**
(الأصدق، وأداة `build_catalog` تسأل عنها)، ثم **مقيس آليًا** (الحركة
والطاقة والمساحة الآمنة قابلة للقياس بـffmpeg بلا أي نموذج)، ثم
**نموذج رؤية** لاحقًا.

---

## ١٣· منع التكرار

```
repetition_penalty(asset, plan) =
      α · (عدد استعمالاته السابقة)²
    + β · (1 ÷ المسافة الزمنية لآخر استعمال)
    + γ · (تشابه بصري مع اللقطة السابقة)
```

**وحارس صارم فوق الترجيح:** الخطة تُرفض إذا

- أصل واحد يغطّي **> ٣٥٪** من الزمن، أو
- أصل يظهر بأكثر من موضعين غير متجاورين، أو
- لقطتان متتاليتان من نفس الأصل بنافذتين متباعدتين — لأن هذا
  **rewind لا cut**، وقد قِسناه: قفزة بفرق **٢١/٢٥٥** بلا مبرّر.

---

## ١٤· استمرارية بصرية

اللقطات المتتالية من نفس الأصل تُكمل النافذة (`in_point_for(after=…)`
موجودة اليوم وتعمل). **الإضافة:**

| الحالة | القرار |
|---|---|
| نفس الأصل + نافذة متّصلة | لقطة واحدة — **ولا مؤثر قطع** |
| نفس الأصل + نافذة راجعة | **قطع حقيقي** — مؤثر مطلوب أو أعد الاختيار |
| أصل مختلف + `continuity: match` | القطع على حركة متشابهة |
| أصل مختلف + `continuity: new` | قطع صريح — مؤثر مسموح |

> السلطة على الاستمرارية هي `timeline.asset_in_frame` **بالإطارات**،
> لا هوية الأصل. مثبَّت اليوم بـ`shot_plan` وبتسامح إطار واحد **مشتقّ**.

---

## ١٥· استراتيجية الـHook

**أول ١.٥ ثانية تقرّر البقاء.** القيود المقترحة على `role: hook`:

- **٢–٣ لقطات كحدّ أدنى** — أقصر لقطات الريل.
- أول لقطة `intent: reveal` أو `action` — **لا `establish` ساكن**.
- إبراز `peak` على الكلمة المفتاحية.
- مؤثر `impact` عند الإطار صفر، ثم **صمت** حتى نهاية الـhook.
- **السكتة داخل الـhook تُملأ بصريًا، لا تُقصّ** — الصورة تُنهي السؤال
  الذي علّقه الصوت.

مقيس على تسجيلك: الـhook ٤.٤٧s وفيه سكتة **١.١١s** بالمنتصف. اليوم
هي فراغ ميت. بالخطة الجديدة هي **أثمن لحظة بصرية في الريل**.

---

## ١٦· استراتيجية النهاية

- آخر لقطة `weight: extended` وتمتد **٠.٦–١.٠s بعد آخر كلمة**.
- الكابشن الأخير يبقى معروضًا بعد انتهاء الصوت.
- `riser` قبل الـpayoff، ثم **صمت مطلق** — لا مؤثر على آخر قطع.
- الحركة تتوقّف: آخر لقطة `motion: static`.

مقيس: عندك **٠.٨٧s** صمت بعد آخر كلمة. اليوم تُهدر. هي مساحة الإمساك.

---

## ١٧· حدّ التنفيذ الحتمي

```
     نوايا نسبية              │        أرقام مطلقة
─────────────────────────────┼──────────────────────────────
 EditPlan · AssetIntent      │  timeline.json · assets.json
 brief|normal|extended       │  f_start · f_end · n_frames
 subtle|normal|heavy         │  gain · sample
 push|pull|drift             │  zoom · crop · eval=frame
                             │
        ▲                    │            ▲
   الوكيل يكتب هنا           │      الكود يكتب هنا
        └──────── Compiler ──┘
```

**قواعد المترجم — كلها قابلة للفحص:**

1. حتمي بالكامل: نفس المدخل ⟶ نفس المخرج، بايت ببايت.
2. لا يخترع نيّة. نيّة ناقصة ⟶ **يرمي**، لا قيمة افتراضية.
3. لا يقبل خطة تكسر قيود §٩ أو §١٣ ⟶ يرمي برسالة تسمّي القيد.
4. `quantize` تبقى **المعبر الوحيد** من الثواني للإطارات.

---

## ١٨· فصل QA عن الجودة الإبداعية

**طبقتان لا تختلطان أبدًا:**

| | **Technical QA** | **Creative Checks** |
|---|---|---|
| السؤال | هل نُفّذ كما خُطّط؟ | هل الخطة نفسها جيدة؟ |
| المخرَج | مرّ / فشل | **قائمة مخالفات بدليل** |
| المرجع | العقد | قيود §٩ و§١٣ |
| عند الفشل | يرمي | يُبلّغ ويقترح |

**Creative Checks — كلها قابلة للدحض، وترجع دليلًا لا درجة:**

```
✗ shot_variance      معامل الاختلاف 0.11 < 0.25   [اللقطات 3,4,5,6]
✗ motion_dominance   zoom_in 62% > 45%            [11 من 18 لقطة]
✗ asset_dominance    asset_2 يغطي 41% > 35%       [الثواني 5.7–22.1]
✓ cue_density        7 مؤثرات / 31s = 4.4s فاصل
✗ dead_shot          اللقطة 9: تباين إطارات 0.4/255  [24.1–26.3s]
✓ caption_safe_area  ولا كابشن فوق موضوع
```

> ⚠️ **ولا درجة مجمّعة.** `visual_score: 82` **ممنوع بقرار.**
> هذا المشروع موثَّق فيه **ثماني حوادث** انخدعنا فيها برقم يبدو دقيقًا،
> وواحدة منها كلّفت ثماني جولات في جلسة واحدة. الرقم المجمّع يعطي الحكم
> ثقة القياس الزائفة، **ويوقف السؤال بدل أن يفتحه**.
>
> «هل الـhook قوي؟» **يبقى حكمًا بشريًا** حتى نبني مرجعًا مقيسًا من
> ريلات حقيقية نفكّكها.

---

## ١٩· خطة الترحيل

**بلا كسر، وبقابلية تراجع في كل خطوة.**

| خطوة | الأثر | التراجع |
|---|---|---|
| `plan.json` عقد **اختياري** | غيابه ⟶ سلوك اليوم بالضبط | احذف الملف |
| المترجم يولّد `EditPlan` **تافهًا** من `segments` | بيت-بيت مطابق للمخرَج الحالي — **هذا هو فحص الترحيل** | — |
| فتح `quantize` لقبول حدود لقطات صريحة | ⚠️ **يحتاج فتح شجرة مجمَّدة** | العلم الافتراضي = السلوك القديم |
| الـEditorial Agent خلف علم | مطفأ افتراضيًا | `--no-editorial` |
| Creative Checks **تبلّغ ولا ترمي** | ولا كسر لأي مسار | — |

> **فحص الترحيل الحاسم:** المترجم على `EditPlan` تافه لازم ينتج
> **نفس البايتات** التي ينتجها المسار اليوم. لو اختلف بايت، الترحيل غلط
> قبل أن نضيف أي ذكاء.

---

## ٢٠· مراحل التنفيذ

**مرتّبة بالأثر لا بالجهد. كل مرحلة تُقاس قبل التالية.**

| # | المرحلة | يفتح | مخاطرة |
|---|---|---|---|
| **0** | **مرجع مقيس** — تفكيك ٢–٣ ريلات ممتازة إلى نفس الجداول | كل حكم لاحق | لا شيء — **بلا كود** |
| **1** | **إثراء الأصول** — `AssetAnalysis` + `best_window` يدويًا | أكبر مكسب بصري فوري | منخفضة — بيانات فقط |
| **2** | **`plan.json` + المترجم التافه** | البنية بلا سلوك جديد | منخفضة — مضمونة بفحص التطابق |
| **3** | **فصل اللقطة عن الجملة** ⚠️ | **RC-1 — أعمق سبب** | **عالية — شجرة مجمَّدة** |
| **4** | **Resolver الجديد** + منع التكرار | RC-4 · RC-5 | متوسطة |
| **5** | **قيود الإيقاع** في المترجم | RC-2 · RC-6 | منخفضة |
| **6** | **Editorial Agent** خلف علم | القرار التحريري | متوسطة |
| **7** | **Creative Checks** تبلّغ | القياس بدل الرأي | منخفضة |
| **8** | إبراز الكابشن | RC-7 | منخفضة |

**المرحلتان 0 و1 بلا كود إطلاقًا، وأثرهما أكبر من 4–8 مجتمعة.**

---

## القرارات التي تحتاج موافقتك

| # | القرار | لماذا لا أستطيع أخذه |
|---|---|---|
| **ق-1** | **فتح `ai_pipeline/timeline/`** لفصل اللقطة عن الجملة | شجرة مجمَّدة، وهي RC-1. **بلا هذا القرار كل ما فوق تلميع** |
| **ق-2** | عقد جديد `plan.json` بين `segments` و`timeline` | إضافة عقد قرار معماري |
| **ق-3** | توسيع `CatalogEntry` بـ`AssetAnalysis` | يغيّر شكل الكتالوج ويلزمه إعادة بناء |
| **ق-4** | الريلات المرجعية للمرحلة 0 | **مدخل منك، لا قرار مني** |
| **ق-5** | مصدر `AssetAnalysis`: يدوي أم مقيس أم نموذج رؤية | يحدد كلفة المرحلة 1 |

---

## خلاصة بجملة واحدة

المسار اليوم **مولّد فيديو صحيح**. تحويله إلى **محرّر** لا يحتاج إعادة
كتابة — يحتاج **طبقة نيّة تحريرية فوقه**، و**فتح قيد واحد** يمنع النموذج
الزمني من تمثيل المونتاج أصلًا.

وما يجب ألا يتغيّر: **الوكيل لا يكتب توقيتًا، والفحص على المخرَج لا على
الخطة، والرقم المريح يُشكّ فيه قبل أن يُصدَّق.**

---

*ولا سطر كود انتغيّر · ولا commit · الأساس `a0b3e99` على `phase3/agent-layer`*
