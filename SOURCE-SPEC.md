# SOURCE-SPEC — افتراضات المصدر: طبّع قبل ما تفهرس

> **منفَّذة.** المرحلتان أ وب مغلقتان — شوف §٩.
> الوثيقة محفوظة كسجلّ القياسات والقرارات.
> كل رقم مقاس على `ffmpeg 7.0.2-static` (johnvansickle)، والأمر مذكور.
> اللقطة الحقيقية: `cta final clip.MOV` — آيفون، إعدادات افتراضية.

---

## ٠. الخلاصة

**خللان مستقلان، أولويتهما واحدة.**

| | الخلل | بيبيّن |
|---|---|---|
| **أ** | `probe` بترجّع الحجم **المرمَّز**، وffmpeg بيسلّم المدوَّر | غلط واضح — الوجه مقصوص |
| **ب** | مصدر HLG بيمرق بلا إدارة ألوان، **والمخرَج بياخد وسوم المصدر** | **صح-تقريبًا** — باهت وبتلوم الإضاءة |

الاتنين ما بيفشلوا. `✅ خلص` وملف سليم.

**ب أخطر عمليًا** لأن أ بتلاحظه بثانية، وب بتكتشفه بعد شهر لما تقارن.
وب **مش حالة نادرة**: هاي إعدادات الآيفون الافتراضية، يعني المدخل
الافتراضي لكل فيديو.

---

## ١. المبدأ: طبّع قبل ما تفهرس

| الحقيقة | التطبيع | وين | الحالة |
|---|---|---|---|
| معدل الإطارات | `[0:v]fps={fps}` **قبل** `select` | `graph.video_stem` | ✅ |
| معدل العيّنات | `[0:a]aresample={sr}` **قبل** `atrim` | `graph.audio_chain` | ✅ |
| أبعاد المصدر | ولا واحد | `cuts.probe` | ❌ خلل أ |
| فضاء الألوان | ولا واحد | ولا مكان | ❌ خلل ب |

**أي حقيقة عن المصدر بتدخل بتعبير فلتر لازم تنطبّع لصيغة واحدة قبل
ما ينبني عليها فهرس أو رقم.**

---

## ٢. خلل أ — الأبعاد

### ٢.١ القياس على لقطتك

```
$ ffmpeg -i "cta final clip.MOV"
  Video: hevc ..., 1920x1080          ← اللي probe بتقراه
        displaymatrix: rotation of -90.00 degrees
$ ... -vf showinfo -frames:v 1
  ... s:1080x1920                      ← اللي بيوصل الفلاتر
```

### ٢.٢ الأثر — مقيس بالعزل

`tools/compare_geometry.py`: **نفس الإطار مرتين بنفس `size_chain`**،
والفرق الوحيد الأبعاد المُمرَّرة. بلا إعادة ترميز، فبلا ضجيج جيل.

| مقاس | رسم متطابق؟ | PSNR (لقطتك) | PSNR (مصدر اصطناعي) |
|---|---|---|---|
| `reel` | **لا** | **inf** | **inf** |
| `square` | **لا** | 8.50 | 3.82 |
| `wide` | **لا** | 10.19 | 6.56 |

> **تصحيح.** الأرقام اللي كانت هون قبل (20.92 / 8.33 / 9.22) كانت
> مقارنة بمرجع «دوران مخبوز»، والخبز بيلزمه إعادة ترميز فالرقم كان
> ملوَّثًا بضجيج جيل. `inf` عند `reel` هي الدليل الحاسم: **الرسم غلط
> بالتلاتة**، و`crop` بتقصّ الإحداثي الخارج عن الحدود لصفر فبتصلّح
> غلطنا وقت التشغيل. مش «مقاسان مكسوران وواحد ناجي».

### ٢.٣ المعاينة بتقول صح والترميز بيطلع غلط

`render.segment_filter` (المعاينة) بتكتب `x='(iw-W)/2'` — تعابير
ffmpeg، فبتنحلّ على الأبعاد الحقيقية. `graph.size_chain` (الترميز)
بتكتب أرقام بايثون، **لأنها مضطرة**: مع `scale` متغيّر لكل مقطع `iw`
بتتقيّد مرة وحدة وما بتتبّع.

فالمسار الموصى فيه بالREADME — `--preview-frames` ثم التصدير — بيوري
تأطيرًا سليمًا ثم بيصدّر غيره.

### ٢.٤ الإصلاح

`cuts.probe` بتقرا `displaymatrix` من **نفس** مخرَج `ffmpeg -i`،
وبتقلب `w`/`h`:

```
swap  ⟺  round(abs(angle)) % 180 == 90
```

| `-display_rotation` | اللي بيطبعه ffmpeg | قلب؟ |
|---|---|---|
| `90` | `rotation of 90.00 degrees` | ✅ |
| `180` | `rotation of -180.00 degrees` | ❌ |
| `270` / `-90` | `rotation of -90.00 degrees` | ✅ |

الإشارة مش مفيدة: ffmpeg بيطبّع الزاوية لـ‎(-180, 180]‎ فـ`270` و`-90`
بيطلعوا نفس السطر. القرار على `abs`.

**بديل انرفض — `-noautorotate`:** بيخلّي المرمَّز = الواصل فأرقامنا
تصير صح بلا تعديل. مرفوض: الإطارات بتوصل بالاتجاه المرمَّز — محتوى
عمودي مخزَّن أفقيًا — فمنقصّ من صورة مقلوبة. الرقم بيزبط والصورة تغلط.

**ولا تغيير على `graph.py` ولا `render.segment_filter`.**

---

## ٣. خلل ب — HDR ‏(HLG · عشر بتّات)

### ٣.١ شو في بلقطتك

```
الكودك        hevc · yuv420p10le
ملف الألوان   tv, bt2020nc/bt2020/arib-std-b67     ← arib-std-b67 = HLG
```

**`bt2020` + `arib-std-b67` + عشر بتّات = HDR.** إعدادات آيفون افتراضية.

### ٣.٢ ffmpeg ما بيعمل ولا تحويل ضمني

لا للدالة الانتقالية، ولا للنطاق اللوني، ولا لمصفوفة YUV. بيقصّ
١٠→٨ بت وبيمرّر قيم HLG كما هي. (`swscale` ما عندها مفهوم «دالة
انتقالية» أصلًا.)

**التشبّع، مقيس** — مرجع SDR معروف حوّلته لـHLG بنفس وسوم لقطتك ثم
رجّعته:

| رقعة | المرجع | المسار الحالي |
|---|---|---|
| سماوي | sat **1.00** | sat **0.33** |
| أحمر | 1.00 | 0.66 |
| أخضر | 1.00 | 0.64 |
| أزرق | 1.00 | 0.84 |

متوسط التشبّع **٠.٤٣٢ → ٠.٢٨٦** (‎−٣٤٪). السماوي النقي
`(0,189,190)` بيصير `(82,121,123)` — القناة الصفرية طلعت ٨٢.

**الاتجاه: أغمق وأبهت، مش أشدّ تشبّعًا.** وهاد سلوك HLG المقصود عند
العرض على SDR: HLG بيحجز أعلى مداه كهامش فوق الأبيض المنتشر، فالصورة
بتتزاحم بأسفل المدى. (PQ بينكسر بالاتجاه المعاكس — «حليبي» — وهاد فرق
مهم لو دخل مصدر PQ لاحقًا.)

### ٣.٣ الأسوأ: المخرَج **مكذوب الوسم**

```
المصدر : yuv420p10le(tv, bt2020nc/bt2020/arib-std-b67)
المخرَج : yuv420p    (tv, bt2020nc/bt2020/arib-std-b67)   ← نفسها
```

ffmpeg بينسخ وسوم المصدر للمخرَج. فالملف **بيدّعي HLG BT.2020** وجوّاه
بايتات ما انتحوّلت — وثمان بتّات، بينما Rec. 2100 بتعرّف HLG عند
١٠/١٢ بت فقط.

**وهاد أسوأ من غياب الوسم**، لأنه غامض: مشغّل بيحترم الوسم بيطبّق
HLG EOTF، ومشغّل بيتجاهله بيعرض الصورة الغامقة الباهتة. **المستخدم
بياخد اللي جهازه قرّره** — وهاي أسوأ خاصية ممكنة لمخرَج.

مع سلسلة صحيحة الوسم بيصير `yuv420p(tv, bt709)`.

### ٣.٤ الوجهة: SDR BT.709 — والسبب مش تفضيلًا

- **إنستغرام** بيستقبل HDR وبيعمل tone-mapping بشيدرات خاصة لنسخة
  SDR. وبنوفمبر ٢٠٢٥ صار يحافظ على Dolby Vision 8.4 على iOS —
  **عبر AV1**. مسار H.264 بثمان بتّات ما بيقدر يشارك فيه أصلًا.
- **تيك توك** بيوصّل HDR بلا أي توثيق للمنشئين، وشحن سبتمبر ٢٠٢٥
  مفتاح «Standard Video Playback» للمشاهدين اللي بينزعجوا من السطوع —
  ومفتاح كهذا ما بينوجد إلا لأن في مشكلة.
- كل المصادر اللي بتعطي نصيحة بتتفق: **حوّل لـSDR Rec.709 بنفسك**
  إذا بدك نتيجة محدَّدة.

**درجة الثقة:** الصفحات الرسمية لميتا وتيك توك محجوبة عن هالجلسة،
فالقسم ده من ملخّصات بحث وصحافة تانوية — متسقة باتجاهها، بس **ما
انقرأت بكلماتها الأصلية**. القرار (SDR BT.709) ما بيعتمد عليها لحاله:
المسار H.264 ٨-بت ما إله مكان بمسار HDR بأي حال.

### ٣.٥ السلسلة — و`zscale` هي الخيار الوحيد

| فلتر | متاح بالبناء الثابت؟ | ملاحظة |
|---|---|---|
| `zscale` | ✅ (`--enable-libzimg`) | Termux فيه، بس تثبيتات قديمة جدًا ممكن لأ |
| `tonemap` | ✅ مدمج | |
| `libplacebo` | ❌ | Termux فيه، بس بده Vulkan وقت التشغيل |
| `colorspace` | موجود بس **ما بيقدر** | `Unsupported input transfer characteristics 18 (arib-std-b67)` — بيفشل بصوت |

الشكل المقترَح:

```
zscale=tin=arib-std-b67:t=linear:npl={npl},
format=gbrpf32le,
zscale=p=bt709,
tonemap={op}:desat=0,
zscale=t=bt709:m=bt709:r=tv,
format=yuv420p
```

**ولا `-color_*` على المخرَج.** كانت بالمواصفة، وانحذفت بالتنفيذ:
`zscale=t=bt709:m=bt709:r=tv` بآخر السلسلة بتضبط خصائص الإطار
وffmpeg بيمرّرها، والملفان **متطابقان بايت-ببايت** معها وبدونها.
طفرة «شيلهن» ما فشّلت ولا فحص -> إعداد ميت -> انحذف.

`tin=` صريحة عمدًا: ما منثق بوسم الحاوية لتحديد المدخل.

### ٣.٦ تلات فخاخ — اتنين منهن **صامتان**

| الفخّ | السلوك | مقيس |
|---|---|---|
| `zscale` مش مبني | `No such filter` وخروج ≠ ٠ | ✅ صاخب، مقبول |
| **`tonemap` بلا `t=linear` قبلها** | **بيشتغل بلا خطأ، والنتيجة أسوأ من لا شي** | ✅ فشل صامت |
| **السلسلة على مصدر SDR** | **بتدمّره** | ✅ فشل صامت |
| مصدر بلا وسوم | `code 3074: no path between colorspaces` | ✅ صاخب |

**تفصيل «السلسلة على SDR» — وهون صحّحت نفسي:**

قِست أول مرة `ΔY=−0.1` واستنتجت إن السلسلة no-op على SDR. **الاستنتاج
كان غلط**، سببه إني قِست عند `npl=100`. عند `npl=1000` — القيمة الصح
لـHLG حقيقي:

| المدخل | سلسلة «بتحوّل إلى HLG أولًا» | سلسلة `tin=` |
|---|---|---|
| HLG (المطلوب) | Y=38.9 · SAT=0.428 | Y=38.9 · SAT=0.428 |
| **SDR (ممنوع يتأثر)** | **Y=38.5** ← دمار | **Y=70.4** ← دمار |

المرجع Y=101.3. **الاتنين بيدمّروا SDR.** فالتشريط مش تحسينًا، هو
شرط صحّة. القرار: **السلسلة بتنطبّق فقط لما وسوم المصدر تقول HLG أو
PQ.**

### ٣.٧ الكشف بلا `ffprobe`

نفس نداء `ffmpeg -i` الموجود بـ`cuts.probe` بيطبعها على سطر التيار:

```
$ ffmpeg -i src.MOV 2>&1 | grep -oE "arib-std-b67|smpte2084"
arib-std-b67
```

**ولا نداء إضافي، ولا `ffprobe`.** قاعدة PR-1 ما بتنمسّ.

### ٣.٨ الكابشن: overlay **بعد** الـtonemap

سببان مستقلان، الاتنين مقيسان:

**(أ) الـtonemapper بيسحق الكابشن.** ما بيقدر يميّز بكسل الكابشن عن
بكسل المشهد، فبيقرا أبيض الكابشن (١.٠) كوميض ١٠٠٠-نِت وبيضغطه. أبيض
الكابشن (Y=235) لما ينحط قبل الـtonemap عند `npl=1000`:

| الترتيب | أبيض الكابشن |
|---|---|
| **tonemap ← overlay (الصح)** | **235.0** |
| overlay ← tonemap, `mobius` | 200.97 |
| overlay ← tonemap, `hable` | **150.99** (‎−٣٦٪) |

> **وهون كمان صحّحت نفسي.** قِست أول مرة عند `npl=100` وطلع الأبيض
> ناجيًا (255,255,255) فقلت «الأبيض بينجو، اللي بينكسر أصفر
> التلوين». الأبيض بينجو **فقط لما ما يكون في مدى HDR حقيقي يستدعي
> ضغطًا** — يعني بالضبط الحالة اللي مش حالتك. على مصدر بمدى حقيقي
> بينسحق.

**(ب) مصفوفة RGB→YUV للPNG بتتغيّر بصمت** حسب وسوم القاعدة. نفس
الـPNG على قاعدتين:

| لون | قاعدة HLG BT.2020 | قاعدة BT.709 |
|---|---|---|
| أحمر | Y=74 | Y=63 |
| `#40A0FF` | Y=137 | Y=142 |

أصغر من (أ)، بس معناه إن **الصور المرجعية للكابشن صالحة لوسم قاعدة
واحد بس**.

وقِست انزياح أصفر التلوين بنفسي: `(255,214,0)` بتصير `(255,151,0)`
لما الـoverlay يصير قبل الـtonemap — أصفر بيميل لبرتقالي.

**السبب المبدئي:** الكابشن مرسوم بضوء **معروض** (sRGB/BT.709).
الـtonemap عملية على ضوء **المشهد**. الكابشن ما كان ضوء مشهد ولازم
ما يمرق فيها.

### ٣.٩ الرقم الوحيد اللي لسا مش معايَر: `npl`

`npl` بتحكم السطوع، ونوع الـ`tonemap` أقل أثرًا. **والتشبّع بينتصلّح
بأي سلسلة** (كلها ٠.٤٢٨–٠.٤٢٩ مقابل ٠.٤٣٢) — يعني خسارة التشبّع من
غياب تحويل BT.2020→709، مش من الـtonemap.

بس **ما بقدر أعاير `npl` من اختبار دائري**: بنيت المصدر الاصطناعي
بـ`npl=100` فطبيعي يرجع أفضل عند ١٠٠. القيمة الاسمية لـHLG ١٠٠٠ نِت.
**المعايرة لازم تصير على لقطتك، بعينك.** وهاد بند تنفيذ مش بند
مواصفة.

---

## ٤. المبدأ التاني: افحص الفرضية مش الحالة

حارس على «الدوران متصلّح» بيحمي من حالة وحدة. الحارس الصح على
**الفرضية**: كل رقم من `probe` بينبني عليه تعبير فلتر لازم ينتقارن
باللي ffmpeg بيسلّمه.

```
ffmpeg -v info -i <src> -vf showinfo -frames:v 1 -f null -
  ->  ... sar:1/1 s:1080x1920
```

**الكلفة مقاسة:** `ffmpeg -i` = ٥.٥ms · `showinfo` إطار واحد =
**٢١.٦ms**. مرة وحدة لكل تشغيلة، مقابل دقايق ترميز.

### ٤.١ `SAR` — الفحص كان بينكسر على كل ملفات الآيفون

لقطتك بتعطي **`SAR 0:1`** مش `1:1`. `0:1` معناها **غير محدَّد**،
وffmpeg بيعاملها ١:١.

> فحصي بالنسخة السابقة من هالمواصفة كان بيقارن بـ`1:1` — يعني كان
> **بيرمي على كل ملف آيفون**. الصح: `0:1` و`1:1` الاتنين مقبولان،
> وأي شي غيرهن بيرمي.

### ٤.٢ نطاق التطبيق

| اللي بترجّعه `probe` | بيدخل تعبير فلتر؟ | مفحوص؟ |
|---|---|---|
| `w`, `h` | ✅ أرقام محسوبة | ❌ **بينضاف** |
| `has_audio` | فرع مش رقم | ✅ الرسم بيفشل بصوت |
| `duration` | `frame_plan` عند `--no-cut` | ✅ E1 |
| **وسوم الألوان** | ✅ بتحدّد السلسلة | ❌ **بينضاف** |

---

## ٥. توقّعات انكسرت — موثّقة عمدًا

**كتبت هالمواصفة قبل ما أشوف لقطتك.** اللي صحّ واللي غلط:

| التوقّع | النتيجة |
|---|---|
| `probe` ≠ الواقع، دوران ربع دورة | ✅ حرفيًا: `(1920,1080)` مقابل `(1080,1920)` |
| `reel` بينجو بالصدفة، `square`/`wide` بينكسروا | ✅ `inf` / 8.50 / 10.19 |
| HEVC ما بيسبب مشكلة | ✅ |
| **VFR بيلزمه بند** | ❌ **لقطتك CFR تمامًا** — ٣٣.٣٣ms ثابتة على ١٢٠ إطار. **البند انلغى.** |
| `SAR` رح تكون `1:1` | ❌ `0:1` — وفحصي كان رح يرمي على كل ملف آيفون |
| ٨ بتّات | ❌ `yuv420p10le` |
| HLG «ما انقاس، بينستنى الملف» | ⚠️ وصل — **وطلع بند كامل مستقل** |

**البند اللي انلغى مكسب مش خسارة**، والبند اللي طلع كان مسجَّلًا
كمجهول مش منسيًا. الفرق بينهن إن الاتنين انقاسوا بدل ما انفترضوا.

**وملاحظة على الحتمية:** التفريغ أعطى ٢٥ كلمة بتشغيلة و٢٦ بتانية —
نفس الملف ونفس الموديل. Whisper مش حتمي، فادعاء «نفس المدخل نفس
المخرَج» ما بينطبق فوق طبقة التفريغ. مش مانع، بس لازم ينقال.

---

## ٦. اختبارات القبول

**ولا واحد بيبدّل `probe`** — نفس قاعدة `tests/test_probe.py`.

### الأبعاد

| # | بيفحص | كيف |
|---|---|---|
| S1 | `probe` بترجّع أبعاد العرض | مصادر بـ`-display_rotation` ٩٠/١٨٠/٢٧٠/‎-٩٠ |
| S2 | التطابق مع الواقع | `probe(src)[:2] == showinfo(src)` على كل مصادر الطقم |
| S3 | الترميز بعد الإصلاح | `compare_geometry` -> رسم متطابق بالتلات مقاسات |
| S4 | المعاينة = الترميز | نفس النافذة من المسارين |
| S5 | الفحص بيرمي | `probe` مبدَّلة بأبعاد غلط -> رمي بالرقمين |
| S6 | **الفحص موصول** | شيل النداء من `cli` -> S5 أخضر و**هاد** بيفشل |

### الألوان

| # | بيفحص | كيف |
|---|---|---|
| C1 | كشف HLG/PQ من `ffmpeg -i` | مصادر موسومة `arib-std-b67` · `smpte2084` · `bt709` · بلا وسوم |
| C2 | التشبّع بينسترجع | HLG مبني من مرجع معروف -> SAT ضمن ٢٪ من المرجع |
| C3 | **وسم المخرَج `bt709`** | فحص سطر التيار بالمخرَج — مش الصورة |
| C4 | **مصدر SDR ما بينتأثر** | bt709 داخل -> ‎`ΔY < 1`‎ · بايت-ببايت لو ممكن |
| C5 | **مصدر بلا وسوم بيمرق** | ما بينكسر بـ`no path between colorspaces` |
| C6 | **الكابشن بعد الـtonemap** | أبيض الكابشن = ٢٣٥ ±٢ عند `npl=1000` |
| C7 | `zscale` ناقص | رسالة مفهومة، مش `No such filter` خام |
| C8 | `SAR 0:1` مقبولة | ما بترمي؛ و`2:1` بترمي |

### فحوص الطفرة

| الطفرة | لازم يفشل |
|---|---|
| شيل قلب `w/h` | S1 · S3 · S4 |
| اعكس شرط القلب (`% 180 == 0`) | S1 |
| شيل نداء التحقّق من `cli` | S6 |
| خلّي التحقّق يحذّر بدل ما يرمي | S5 |
| شيل `zscale=t=linear` من السلسلة | C2 |
| شيل شرط «HLG فقط» (طبّق دايمًا) | **C4** |
| حطّ الoverlay قبل الـtonemap | **C6** |
| شيل وسوم `-color_*` من المخرَج | **C3** |

---

## ٧. الفشل بصوت — البند الصريح

> أي فرضية عن المصدر بينبني عليها تعبير فلتر، لازم يكون عليها فحص
> بيقارنها بالواقع **قبل** الترميز، والاختلاف **بيرمي** — ما بيحذّر
> ولا بيصلّح لحاله.

«بيرمي مش بيحذّر» مقصودة: التحذير بيضيع بين أسطر تقدّم ffmpeg
والمستخدم بيشوف `✅ خلص` بالآخر. والتصليح التلقائي أسوأ — بيخفي إن
فرضيتنا كانت غلط.

---

## ٨. برّا النطاق

- **مصدر PQ (`smpte2084`)** — بينتكشف وبينعامل زي HLG بالسلسلة، بس
  ما انقاس. PQ بينكسر بالاتجاه المعاكس (حليبي مش غامق).
- **الحفاظ على HDR بالمخرَج** — مسار إنستغرام HDR عبر AV1 + Dolby
  Vision، وH.264 ٨-بت ما إله مكان فيه. لو صار AV1 يومًا، بينفتح.
- **`SAR` مش ١:١ ولا ٠:١** — الفحص بيرمي، وما في تعامل. مش حالة
  الآيفون.
- **معايرة `npl`** — بند تنفيذ، بالعين على لقطتك.

---

## ٩. التنفيذ — مغلق

مرحلتان مستقلتان، الاتنين منفَّذتان ومقيستان.

| | قبل | بعد |
|---|---|---|
| الرسم (٣ مقاسات) | مختلف | **متطابق · PSNR `inf`** |
| المعاينة ↔ الترميز | ١٠–١٣ dB | **٤٩.٦–٥١.٥ dB** |
| تشبّع HLG مقابل SDR | ‎−٣٤.٦٪ | **‎−١.٣٪** |
| وسم مخرَج من HLG | `bt2020/arib-std-b67` | **`bt709`** |

**٨٠١ فحص على ffmpeg 7.0.2-static.** فحوص الطفرة: ١٠، كلها انمسكت
إلا وحدة كشفت إعدادًا ميتًا فانحذف.

### اللي انكشف بالتنفيذ

1. **`cli` كانت بتمرّر ثلاثية** فوسوم الألوان بتنقطع و`build_graph`
   بتبني بلا tonemap **بصمت** — مخرَج صادق الوسم وغلط المحتوى،
   المرآة الدقيقة للخلل الأصلي. انكشف بقياس المخرَج مش بقراءة الكود.
2. **وسم المخرَج بلا شرط كسر E3/E8** — الوسم على محتوى ما لمسناه
   ادّعاء غير متحقَّق كمان. القاعدة: منوسم لأننا حوّلنا، مش لأن
   الشكل أنضف.

### الخطوات الأصلية (للسجل)


**المرحلة أ — الأبعاد**
1. `cuts.probe` بتقرا `displaymatrix` وبتقلب · S1
2. `cuts.verify_source()` بنداء `showinfo`، وSAR بتقبل `0:1`/`1:1` · S2 S5 C8
3. وصل بـ`cli.main` مرة وحدة · S6
4. S3 S4 + فحوص الطفرة

**المرحلة ب — الألوان**
1. `cuts.probe` بترجّع وسوم الألوان من نفس النداء · C1
2. `graph` بتبني السلسلة **مشروطة** بـHLG/PQ · C2 C4 C5
3. الoverlay بعد الـtonemap · C6
4. وسوم `-color_*` على المخرَج · C3
5. حارس `zscale` · C7 + فحوص الطفرة
6. معايرة `npl` على لقطتك

**ولا تعديل على `render.segment_filter`.** لو طلع إنه لازم، التشخيص
ناقص — وقّف وسجّل.
