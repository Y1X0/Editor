# SOURCE-SPEC — افتراضات المصدر: طبّع قبل ما تفهرس

> مواصفة. **ما انكتب ولا سطر إنتاج على أساسها بعد.**
> كل رقم هون مقاس على `ffmpeg 7.0.2-static`، والأمر اللي أعطاه مذكور.

---

## ٠. الخلاصة بسطرين

المشروع بياخد حقائق عن المصدر (أبعاد، معدل إطارات، معدل عيّنات) وبيبني
عليها تعابير فلاتر. **حقيقتان منهن بينتطبّعوا قبل الاستعمال، وواحدة لأ** —
وهاي الوحيدة اللي فيها خلل.

والخلل ما بيفشل. بيطلّع `✅ خلص` وملف سليم بتأطير غلط.

---

## ١. المبدأ: طبّع قبل ما تفهرس

المشروع بيطبّق هالمبدأ مرتين، وما كان مكتوبًا كمبدأ:

| الحقيقة | التطبيع | وين | النتيجة |
|---|---|---|---|
| معدل الإطارات | `[0:v]fps={fps}` **قبل** `select` | `graph.video_stem` | ✅ VFR بيشتغل |
| معدل العيّنات | `[0:a]aresample={sr}` **قبل** `atrim` | `graph.audio_chain` | ✅ 44.1k بيشتغل |
| **أبعاد المصدر** | **ولا واحد** — `probe` بتقرا الخام وبتمرّره | `cuts.probe` | ❌ الدوران بيكسر |

الصياغة: **أي حقيقة عن المصدر بتدخل بتعبير فلتر لازم تنطبّع لصيغة
واحدة قبل ما ينبني عليها فهرس أو رقم.** الفهرسة على قيمة غير مطبَّعة
بتشتغل صح على المصادر اللي فرضياتنا بتنطبق عليها، وبتنكسر بصمت على
غيرها.

### ١.١ ليش الاتنين الأولانيين ما انكسروا (مقاس مش مستنتَج)

**VFR** — مصدر متفاوت فعلًا (فروقات ١٦.٦٧–٥٠ms، الحاوية بتعلن
`28.90 fps, 60 tbr`):

```
$ ffmpeg -i vfr.mp4 -vf showinfo ...   فروقات ms: [50.0, 50.0, 16.67, 33.33, ...]
$ python -m autoreel.cli vfr.mp4 ...   Σframe_plan = 120   إطارات المخرَج = 120   ✅
```

**معدل عيّنات ≠ ٤٨٠٠٠** — مصدر 44.1kHz فيه نقرات عند إطارات المصدر
٣٠/٩٠/١٥٠/٢١٠/٢٧٠:

```
النقرات المتوقَّعة بإطارات المخرَج: [6, 54, 102]
النقرات المقيسة:                   [6.00, 54.00, 102.00]   ✅ صفر انزياح
```

`graph.validate_fps` بترمي أصلًا على `fps` كسري وعلى `sr % fps ≠ 0`.
الحارس موجود.

**ملاحظة جانبية (مش خلل):** مخرَج الصوت ١٩٢٥١٢ عيّنة بدل ١٩٢٠٠٠ —
زيادة ٥١٢ (١٠.٦٧ms) **بذيل الملف**. مقيس على مصدر 48k: نفس الـ٥١٢
بالضبط. حشو AAC عند الفكّ، مستقل عن معدل المصدر، وما بيمسّ التزامن.

**HEVC** — `hvc1` + `bt709` + مصفوفة دوران: انفكّ وانرمّز بلا خطأ ولا
تحذير. الكودك برئ.

---

## ٢. الخلل: `probe` بترجّع الحجم المرمَّز

### ٢.١ القياس

```
$ ffmpeg -i rot90.mov
  Stream #0:0: Video: h264 ..., 640x360 [SAR 1:1 DAR 16:9], 30 fps
        displaymatrix: rotation of 90.00 degrees

$ ffmpeg -i rot90.mov -vf showinfo -frames:v 1 -f null -
  ... sar:1/1 s:360x640
```

`cuts.probe` بتقرا سطر `Stream` — يعني **٦٤٠×٣٦٠، الحجم المرمَّز**.
ffmpeg بيدوّر تلقائيًا عند الفكّ، فاللي بيوصل رسم الفلاتر **٣٦٠×٦٤٠**.

### ٢.٢ الأثر — الرسم غلط بالتلات مقاسات

مش «مقاسين مكسورين وواحد ناجي». حالة الآيفون (مرمَّز ١٩٢٠×١٠٨٠ +
مصفوفة ٩٠ -> معروض ١٠٨٠×١٩٢٠):

| مقاس | من المرمَّز (الآن) | من المعروض (الصح) | |
|---|---|---|---|
| `reel` | `(3413,1920)` x=**1166** y=0 | `(1080,1920)` x=0 y=0 | رسم مختلف |
| `square` | `(1920,1080)` x=420 y=**0** | `(1080,1920)` x=0 y=**252** | رسم مختلف |
| `wide` | `(1920,1080)` x=0 y=**0** | `(1920,3413)` x=0 y=**1166** | رسم مختلف |

```python
>>> G.size_chain(cfg, [99], [1.0], "v", "o", 640, 360) == \
...     G.size_chain(cfg, [99], [1.0], "v", "o", 360, 640)
reel False · square False · wide False
```

`reel` بيطلع صح **وقت التشغيل** لأن `crop` بتقصّ الإحداثي الخارج عن
الحدود لصفر. ffmpeg بيصلّح غلطنا. الخلل واحد، مكانه واحد، وتصليحه
بمكان واحد.

PSNR مقابل مرجع الدوران المخبوز:

```
reel   20.92   ← نفس الهندسة؛ الفرق ضجيج جيل ترميز HEVC->H.264
square  8.33   ← صورة تانية
wide    9.22   ← صورة تانية
```

### ٢.٣ الأخطر: المعاينة بتقول صح والترميز بيطلع غلط

```
                 reel   square   wide
--preview-frames 45.52   44.53   49.43     ← تأطير صحيح
الترميز الكامل   20.92    8.33    9.22     ← تأطير غلط
```

السبب معماري مش عرضي:

- `render.segment_filter` (المعاينة) بتكتب `x='(iw-W)/2'` و
  `y='(ih-H)*bias'` — **تعابير ffmpeg**. `iw`/`ih` بتنحلّ وقت ضبط
  الوصلة على الأبعاد **الحقيقية بعد الدوران**. فبتطلع صح.
- `graph.size_chain` (الترميز) بتكتب أرقامًا محسوبة بايثون من `probe`،
  **لأنها مضطرة**: مع `scale` متغيّر لكل مقطع، `iw` بتتقيّد مرة وحدة
  وما بتتبّع (موثّق بـ`size_chain` نفسها).

يعني المسار الموصى فيه بالREADME — `--preview-frames` ثم التصدير —
بيوري المستخدم تأطيرًا سليمًا ثم بيطلّع غيره.

---

## ٣. الإصلاح: تطبيع الأبعاد عند `probe`

### ٣.١ الشكل

`cuts.probe` بتقرا سطر `displaymatrix` من **نفس** مخرَج `ffmpeg -i`
اللي بتقراه أصلًا، وبتقلب `w`/`h` لما الدوران ربع دورة.

```
displaymatrix: rotation of {angle} degrees
swap  ⟺  round(abs(angle)) % 180 == 90
```

القياس على الأربع حالات (`-display_rotation` عند البناء):

| المطلوب | اللي بيطبعه ffmpeg | مرمَّز | حقيقي | قلب؟ |
|---|---|---|---|---|
| `90` | `rotation of 90.00 degrees` | 640x360 | 360x640 | ✅ |
| `180` | `rotation of -180.00 degrees` | 640x360 | 640x360 | ❌ |
| `270` | `rotation of -90.00 degrees` | 640x360 | 360x640 | ✅ |
| `-90` | `rotation of -90.00 degrees` | 640x360 | 360x640 | ✅ |

**الإشارة مش معلومة مفيدة هون** — ffmpeg بيطبّع الزاوية لـ‎(-180, 180]‎،
و`270` و`-90` بيطلعوا نفس السطر. القرار على `abs` وبس. (الإشارة بتلزم
لو احتجنا اتجاه الدوران يومًا؛ اليوم لأ.)

**ولا نداء `ffmpeg` إضافي بالمسار العادي**، فقاعدة «نداء `-i` واحد»
بتضل قائمة، وقاعدة «ولا `ffprobe`» ما بتنمسّ.

### ٣.٢ بديل انرفض: `-noautorotate`

```
بلا noautorotate:  s:360x640
مع  -noautorotate: s:640x360
```

بيخلّي المرمَّز = الواصل، فأرقامنا الحالية بتصير صح بلا أي تعديل.
**مرفوض:** الإطارات بتوصل بالاتجاه المرمَّز — محتوى عمودي مخزَّن أفقيًا
— فمنقصّ من صورة مقلوبة. الرقم بيزبط والصورة بتغلط.

### ٣.٣ ما فيه بهالإصلاح

- ولا تغيير على `graph.py` — بتاخد `src_w/src_h` زي ما هي.
- ولا تغيير على `render.segment_filter` — صحيحة أصلًا.
- `render.probe_source` و`cuts.probe_duration` الاتنين بينادوا `probe`،
  فتصليح واحد بيغطّيهن.

---

## ٤. المبدأ التاني: افحص الفرضية مش الحالة

حارس على «الدوران متصلّح» بيحمي من حالة وحدة. الحارس المطلوب على
**الفرضية**: كل رقم بناخده من `probe` ونبني عليه تعبير فلتر لازم يطابق
اللي ffmpeg بيسلّمه فعلًا.

### ٤.١ الصياغة

قبل أي ترميز، وبعد `probe`، نداء واحد:

```
ffmpeg -v info -i <src> -vf showinfo -frames:v 1 -f null -
  ->  ... sar:1/1 s:360x640
```

ونقارن `s:WxH` بالـ`(w,h)` اللي `probe` رجّعتها. اختلاف = رمي، بالرقمين
بالرسالة.

**الكلفة مقاسة** (متوسط ٥ نداءات على `hevc_rot.mov`):

```
ffmpeg -i            ->   5.5 ms
showinfo إطار واحد   ->  21.6 ms
```

٢١.٦ms مقابل دقايق ترميز. مرة وحدة لكل تشغيلة، مش لكل مقاس.

### ٤.٢ ليش هاد أوسع من الدوران

الفحص ما بيعرف شي عن الدوران. بيفحص **الفرضية**: «الأبعاد اللي حسبنا
عليها هي اللي ffmpeg رح يعطينا». فبيمسك كل شي بيكسرها:

- الدوران (اليوم)
- `SAR` مش ١:١ — قِسناه: مصدر `[SAR 2:1 DAR 1:1]` بيطلع
  `sar:2/1 s:320x640`. `size_chain` بتشتغل على أبعاد التخزين وبتتجاهل
  الـSAR، فالصورة بتنضغط. **آيفون SAR 1:1 فما هي حالتنا اليوم**، بس
  الفحص بيمسكها لما تصير.
- قصّ بالحاوية (`clap` atom)
- أي سلوك جديد بنسخة ffmpeg جاية

### ٤.٣ نطاق التطبيق — كل ما بترجّعه `probe`

`probe` بترجّع `(w, h, has_audio, duration)`:

| الرقم | بيدخل بتعبير فلتر؟ | مفحوص اليوم؟ |
|---|---|---|
| `w`, `h` | ✅ `size_chain` أرقام محسوبة | ❌ **هاد اللي بينضاف** |
| `has_audio` | فرع مش رقم | ✅ الرسم بيفشل بصوت لو غلط |
| `duration` | `frame_plan` عند `--no-cut` وبلا كلمات | ✅ E1: إطارات المخرَج = Σplan |

فالإضافة وحدة، مش ثلاثة. الجدول موجود عشان اللي جاي يعرف إن السؤال
انسأل عن كل رقم.

### ٤.٤ ليش مش فحص جوا الرسم

ما في فلتر `assert` بffmpeg. أقرب حيلة إن `crop` بتفشل لما المقاس أكبر
من المدخَل — بس هي بتقصّ الإزاحات بصمت، وهاي بالضبط اللي خبّت الخلل
بـ`reel`. فحص برّا الرسم أوضح وبيطبع الرقمين.

---

## ٥. اختبارات القبول المقترحة

الترقيم بيكمّل E1..E8 الموجودة. **ولا واحد منهن بيبدّل `probe`** — نفس
قاعدة `tests/test_probe.py`: الفحص اللي بيبدّل الجزء المكسور ما بيشوف
الكسر.

| # | بيفحص | كيف |
|---|---|---|
| **S1** | `probe` بترجّع أبعاد **العرض** | مصدر مبني بـ`-display_rotation` عند ٩٠/١٨٠/٢٧٠/‎-٩٠؛ المتوقَّع من جدول ٣.١ |
| **S2** | التطابق مع الواقع | `probe(src)[:2] == showinfo(src)` — على كل مصادر الطقم + المدوَّرة |
| **S3** | الترميز = المرجع المخبوز | `--sizes all` على مدوَّر ومخبوز؛ PSNR > ٤٠dB بالتلاتة |
| **S4** | المعاينة = الترميز | نفس الإطار من `--preview-frames` ومن الترميز؛ نفس النافذة |
| **S5** | الفحص بيرمي | `probe` مبدَّلة تعطي أبعادًا غلط -> رمي، والرسالة فيها الرقمين |
| **S6** | الفحص موصول | شيل النداء من `cli` -> S5 بيضل أخضر و**هاد** بيفشل |
| **S7** | HEVC ما بيغيّر شي | نفس S3 على `hvc1`+`bt709` |
| **S8** | VFR (انحدار) | فروقات ١٦.٦٧–٥٠ms -> إطارات المخرَج = Σplan |
| **S9** | 44.1k (انحدار) | نقرات معلَّمة -> صفر انزياح |

**S6 هو الحارس اللي فرضته حادثة `motion.pan_px`:** فحص موجود ومفصول
عن الكود بيضل أخضر للأبد.

### ٥.١ فحوص الطفرة المطلوبة قبل الإغلاق

| الطفرة | لازم يفشل |
|---|---|
| شيل قلب `w/h` من `probe` | S1 · S3 · S4 |
| اعكس شرط القلب (`% 180 == 0`) | S1 |
| شيل نداء التحقّق من `cli` | S6 |
| خلّي التحقّق يحذّر بدل ما يرمي | S5 |

---

## ٦. الفشل بصوت — البند الصريح

**مش مقبول `✅ خلص` على تأطير غلط.** الصياغة العملية:

> أي فرضية عن المصدر بينبني عليها تعبير فلتر، لازم يكون عليها فحص
> بيقارنها بالواقع **قبل** الترميز، والاختلاف **بيرمي** — ما بيحذّر
> ولا بيصلّح لحاله.

«بيرمي مش بيحذّر» مقصودة: التحذير على stderr بيضيع بين أسطر تقدّم
ffmpeg، والمستخدم بيشوف `✅ خلص` بالآخر. والتصليح التلقائي أسوأ —
بيخفي إن فرضيتنا كانت غلط.

---

## ٧. اللي المواصفة **ما** بتغطّيه

- **`SAR` مش ١:١** — الفحص بيمسكها ويرمي، بس ما في تعامل معها. مصادر
  الآيفون ١:١، والتعامل شغل مستقل. (يُسجَّل كتذكرة بعد الإغلاق.)
- **ملف الألوان** (HLG · Dolby Vision) — HEVC+bt709 مقاس وسليم. HLG
  و DV ما انقاسوا؛ بينستنوا الملف الحقيقي.
- **مصادر بلا مصفوفة دوران أصلًا** — ما بينتأثروا، والفحص بيمرق.

---

## ٨. القرار المطلوب

المقترَح: **مرحلة وحدة**، لأن الإصلاح مكان واحد والفحص مكان واحد.

1. `cuts.probe` بتقرا `displaymatrix` وبتقلب — و`S1`.
2. `cuts.verify_source(path, w, h)` بنداء `showinfo` — و`S2` `S5`.
3. وصل بـ`cli.main` مرة وحدة قبل الحلقة — و`S6`.
4. `S3` `S4` `S7` `S8` `S9` على مصادر مبنية.
5. فحوص الطفرة من ٥.١.
6. تحديث `CLAUDE.md` / `README.md` / `ISSUES.md`.

**ولا تعديل على `graph.py` ولا `render.segment_filter`.** لو طلع إنه
لازم، هاد معناه إن التشخيص ناقص — وقّف وسجّل بدل ما توسّع.
