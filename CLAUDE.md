# auto-reel — محرر ريلز أوتوماتيكي (عربي)

بياخد فيديو خام (talking head) وبيطلّع ريل جاهز: قص الصمت، زوم/punch-in عند
كل قطة، وكابشن عربي محروق بتلوين كاريوكي. كل شي بأمر واحد.

```bash
python -m autoreel.cli input.mp4 -o out.mp4
```

---

## ⚠️ اقرأ هذا قبل أي تعديل على الكابشن

**لا تستخدم `arabic_reshaper` ولا `python-bidi` بهذا المشروع.**

Pillow المبني مع **libraqm** بيعمل shaping + bidi لحاله. لو عملت reshape
قبله بيصير انعكاس مزدوج والنص بيطلع مقلوب، ولو الخط ما بيدعم صيغ العرض
القديمة (Tajawal ما بيدعمها) بتطلع **مربعات فاضية أو خلفية سودا**.

هاي بالضبط المشكلة المشهورة "العربي بيطلع الفيديو أسود".

الصح:
```python
draw.text(xy, raw_arabic_text, font=f, direction="rtl", language="ar")
```

تحقق قبل ما تبلّش:
```python
from PIL import features; assert features.check("raqm")
```

لو `False` على Termux:
```bash
pkg install libraqm harfbuzz fribidi
pip install --no-binary :all: --force-reinstall Pillow
```

**ليش ما منستعمل `python-bidi` حتى لحساب الترتيب بس؟**

خلّي المنطق واضح، لأن القرار بيبيّن مبالَغ فيه لحد ما تعرف سببه:
`python-bidi` **بتعطي ترتيب أصح من اللي عنا** — bidi كامل بمواصفة
UAX#9 بدل تقريبنا. الرفض مش تقني.

الرفض لأن **وجودها بملف التبعيات إغراء**. أي حدا بيجي بعدنا وبيشوفها
مثبّتة رح يستنتج إنها مسموحة، وأقرب استعمال لها هو تمرير النص عليها
قبل الرسم — وهاي بالضبط الوصفة اللي بتعطي "الفيديو بيطلع أسود" اللي
فوق. الخطر مش بشو بتعمله المكتبة اليوم، الخطر بشو بتغري فيه بعد ستة
أشهر لما ما يكون حدا فاكر ليش انحطت.

فالمقايضة: نقبل bidi تقريبي مكتوب عنا ومختبَر، مقابل إغلاق باب
الانحدار اللي كوى المشروع مرة.

**ترتيب RTL بالرسم اليدوي:** لما ترسم كلمة كلمة (للتلوين)، أول كلمة
بالسطر بتنرسم **أقصى اليمين** — بس هاد صحيح فقط للنص العربي الخالص.

**النص المختلط: `_bidi_runs()` بتحدد الترتيب، مش الرسم.**

bidi خوارزمية على مستوى **السطر**، والرسم كلمة كلمة (اللي بيلزمنا
للتلوين الكاريوكي) بيتخطاها. الانكسار محدَّد بدقة: **كلمتان لاتينيتان
متجاورتان أو أكتر**. `App Store` كان يطلع `Store App`، و`iOS 18`
يطلع `18 iOS`. كلمة لاتينية وحدة محاطة بعربي بتنجو لأنها run واحد
وبتحتل خانة RTL واحدة — فبيتصادف الصح.

الحل: نرتّب **الـruns مش الكلمات**. `_bidi_runs()` بترجّع ترتيب
التوكنات البصري من اليسار لليمين، وحلقة الرسم بتمشي عليه.

**قاعدة الأرقام والمحايدات — اقرأها قبل ما تعدّل:**

> التوكن اللي ما فيه ولا محرف قوي (أرقام لحالها، رموز، ترقيم)
> **بياخد اتجاه التوكن اللي قبله لو كان لاتيني، وإلا بيصير RTL.**
> والتوكن المحايد بأول السطر بياخد RTL (اتجاه القاعدة).

هاي أكتر قاعدة رح تنتنسى، وهاي أمثلتها:

| النص | التصنيف | ليش |
|---|---|---|
| `تحديث iOS 18 نزل` | `18` تصير LTR | قبلها `iOS` لاتينية -> بتنضم لنفس الـrun |
| `رقم 2024 و 2025 سوا` | `2024` و`2025` RTL | قبلهن عربي -> كل وحدة خانة RTL لحالها |
| `Flutter + Dart` | `+` تصير LTR | قبلها لاتينية -> run واحد |

**حدود التقريب (مقصودة):** مش UAX#9 كامل. ما بيتعامل مع المستويات
المتداخلة، ولا علامات الاتجاه الصريحة (LRM/RLM)، ولا الترقيم على حدود
الـruns بدقة تامة، وrun بينقسم على سطرين بينرتّب داخل كل سطر لحاله.
بيغطي الحالة العملية: عربي فيه أسماء منتجات وأرقام لاتينية.

**الكابشن ما بينقصّ أبدًا:** `_fit()` بتختار التخطيط اللي بيعطي **أكبر
حجم خط**، وعند التعادل أقل أسطر — مسح تنازلي من ١٠٠٪ لحد أرضية ٤٥٪،
وأول حجم بيسع بسطرين أو أقل بيفوز. تفضيل "أقل أسطر" بيجي لحاله لأن
`_wrap` جشعة. لا تعيدها لـ`min(W-60, ...)` — هيك كان النص بيطلع برّا
الصورة وبتنقص كلمات من الطرفين.

لو الحجم النهائي نزل تحت ٥٥ بيطلع تحذير على stderr: النص كامل بس صعب
يتقرا على الموبايل، والحل تصغير `captions.max_words` أو `captions.size`.

---

## البنية

```
autoreel/
├── transcribe.py   faster-whisper بتوقيت لكل كلمة · بديل from_srt()
├── cuts.py         خطة القص من فجوات الكلام + remap للتوقيتات
├── captions.py     رسم الكابشن (هون كل خطورة الـRTL)
├── graph.py        بناء رسم الفلاتر — دوال نقية، ولا نداء ffmpeg
├── render.py       تشغيل ffmpeg: تشغيلة وحدة لكل مخرَج
├── exports.py      اختيار المقاسات ودمج إعداداتها
├── sfx.py          خطة المؤثرات — نقية بلا استيراد ولا نداء ffmpeg
└── cli.py          نقطة التشغيل
config.json         كل الإعدادات — عدّل هون مش بالكود
fonts/              Tajawal + OFL.txt (رخصة الخط لحالها)
assets/sfx/         خمسة مؤثرات مولَّدة بالتركيب
pyproject.toml      بناء + إعداد pytest (ولا pytest.ini منفصل)
LICENSE             MIT — **الكود بس**، الخط تحت OFL
```

**التلات بيانات (`config.json`, `fonts/`, `assets/sfx/`) برّا الحزمة**،
فـ`pip install` بينسخ الكود بلا بياناته. لهيك ما في `[project.scripts]`
والتشغيل المدعوم `python -m autoreel.cli` من الجذر. التفصيل والقياس
بـ`ISSUES.md` / PKG-1.

## تدفق البيانات

```
input.mp4
  └─> transcribe()  -> [{word, start, end}]              (توقيت أصلي)
      └─> segments_from_words()  -> [(a,b), ...]         (مقاطع الكلام)
          ├─> frame_plan()   -> [n₀, n₁, …]      ← مصدر الحقيقة
          │                     عدد إطارات كل مقطع
          └─> remap_words()  -> توقيت جديد بعد القص  ← لا تنساها
              └─> group_words() -> build_caption_pngs()
                      │
                      └─> caption_frames() -> فهارس إطارات
                          └─> materialise_captions() -> تسلسل %06d.png
  
  graph.build_graph(خطة الإطارات) -> نص رسم الفلاتر
      └─> render.build_output() -> تشغيلة ffmpeg وحدة -> out.mp4
```

**أهم نقطتين منطقيتين:**

١. بعد القص التوقيتات بتتغير. أي كابشن لازم يمر على `remap_words()`
وإلا بيطلع مزحلق عن الصوت.

٢. **مصدر الحقيقة هو `frame_plan` مش الزمن.** ffmpeg بيرمّز عدد
إطارات، مش ثواني. الصورة والصوت والكابشن التلاتة بينقصّوا على **نفس**
خطة الإطارات: الفيديو بـ`select` على فهرس الإطار، والصوت بـ`atrim`
بفهرس العيّنة المشتقّ منها، والكابشن بتسلسل صور مفهرس بالإطار. أي
حساب بالثواني بمكان منهن بيفتح باب الانزياح اللي صرفنا مراحل نسدّه.

---

## ⚠️ المبدأ الجامع: طبّع قبل ما تفهرس

> **أي حقيقة عن المصدر بتدخل بتعبير فلتر لازم تنطبّع لصيغة واحدة قبل
> ما ينبني عليها فهرس أو رقم.**

مطبَّق أربع مرات بالمشروع. لما تضيف أي قراءة جديدة عن المصدر، مكانها
بهالجدول:

| الحقيقة | التطبيع | وين |
|---|---|---|
| معدل الإطارات | `[0:v]fps={fps}` **قبل** `select` | `graph.video_stem` |
| معدل العيّنات | `[0:a]aresample={sr}` **قبل** `atrim` | `graph.audio_chain` |
| أبعاد المصدر | قلب `w/h` من `displaymatrix` | `cuts.probe` |
| فضاء الألوان | `tonemap_chain` مشروطة بوسوم المصدر | `graph.video_stem` |

الاتنين الأولانيين انكتبوا لأسباب مستقلة، والتالت انكشف لما أول ملف
آيفون حقيقي دخل: `probe` كانت بتقرا الحجم **المرمَّز** (١٩٢٠×١٠٨٠)
بينما ffmpeg بيدوّر عند الفكّ ويسلّم **١٠٨٠×١٩٢٠**. `size_chain` بتحسب
نافذة القص من أرقام بايثون، فطلعت من مكان غلط.

القيمة غير المطبَّعة بتشتغل صح على المصادر اللي فرضياتنا بتنطبق عليها
(mp4 مولَّد: بلا دوران، CFR، 48kHz) وبتنكسر **بصمت** على غيرها.

### وأخوه: افحص الفرضية مش الحالة

حارس على «الدوران متصلّح» بيحمي من حالة وحدة. الحارس الصح على
**الفرضية**: كل رقم من `probe` بينبني عليه تعبير فلتر لازم ينتقارن
باللي ffmpeg بيسلّمه فعلًا — `showinfo` على إطار واحد (٢١.٦ms) قبل أي
ترميز، والاختلاف **بيرمي**.

هيك بينمسك الدوران، و`SAR` مش ١:١، والقصّ بالحاوية، وأي سلوك جديد
بنسخة ffmpeg جاية — بلا ما نعرفهن سلفًا.

**ومش مقبول `✅ خلص` على مخرَج غلط.** التحذير بيضيع بين أسطر تقدّم
ffmpeg والمستخدم بيشوف علامة النجاح بالآخر؛ والتصليح التلقائي أسوأ لأنه
بيخفي إن فرضيتنا كانت غلط. التفصيل والقياسات بـ`SOURCE-SPEC.md`.

### وأخطر من الخلل: المخرَج اللي بيكذب على قارئه

مصدر HLG كان بيمرق بلا إدارة ألوان، **وffmpeg بينسخ وسوم المصدر
للمخرَج**. فالنتيجة ملف H.264 ثمان بتّات موسوم `bt2020/arib-std-b67`:
بيدّعي HLG وجوّاه بايتات ما انتحوّلت.

**وهاد أسوأ من غياب الوسم، مش أهون.** بلا وسم في صورة وحدة غلط.
بوسم كاذب، **المشغّل هو اللي بيقرّر**: واحد بيحترم الوسم فبيطبّق
HLG EOTF، وواحد بيتجاهله فبيعرض الصورة الغامقة. مشاهدون مختلفون،
ألوان مختلفة، **من نفس الملف** — وشكوى ما بتنعاد ولا بتنصلّح، لأن
العطل مش بالصورة بل بالادّعاء عنها.

فالقاعدة: **أي وسم على المخرَج لازم يكون صحيحًا عن محتواه.** وعليها
حارس بيفشل: مخرَج ٨ بتّات ممنوع يحمل `bt2020` ولا `arib-std-b67`
ولا `smpte2084`.

---

## ⚠️ أداة القياس بتنكسر أكتر من الكود

**خمس مرات بهالمشروع كان الخلل بأداة القياس مش بالكود المقيس.**
هاد مش سوء حظ، هو نتيجة إن الكود مغطّى بمئات الفحوص وأداة القياس
بتنكتب مرة وبتنصدَّق فورًا.

| الحادثة | اللي طلع |
|---|---|
| فحص القتل بالمرحلة ٨ | كان بيقتل بعد ما ffmpeg خلص — فحص فاضي |
| رسالة خطأ ffmpeg | الفحص كان بيمرّ على صدى الأمر مش على stderr |
| فحص الجمود | ١٢ ثانية أثر = ٤٧ ك.ب < أنبوب ٦٤ ك.ب — بلا أسنان |
| مكسب الكلام ٠.٤٩٥ | أثر قياس: مرجع أحادي مقابل مزيج ستيريو |
| PSNR الدوران | ملوَّث بجيل ترميز من مرجع «مخبوز» |
| **`npl` الخاطئة** | **نتيجتان مريحتان، الاتنين غلط، نفس السبب** |
| **١٧dB «فرق هندسة»** | **علم `--no-captions`، والصورة كشفته لما الرقم خبّاه** |

> **أي رقم مفاجئ — وخصوصًا المريح — افحص الأداة قبل ما تستنتج.**
>
> **وقِس _وشوف_.** الرقم لحاله بيكذب بصمت.

**حادثة `npl` بالتفصيل، لأنها الأوضح:** قِسنا سلسلة الـtonemap عند
`npl=100` فطلعت نتيجتان مريحتان — «السلسلة no-op على مصدر SDR»
(‎ΔY=−0.1‎) و«أبيض الكابشن بينجو» (255,255,255). الاتنين غلط. عند
`npl=1000` — القيمة الصح لـHLG حقيقي — السلسلة **بتدمّر** SDR
(‎Y ١٠١→٣٨‎) والأبيض **بينسحق** لـ١٥١.

ولو ضلّت النتيجتان، كانت السلسلة انطبقت **بلا شرط** ودمّرت كل مصدر
SDR بصمت.

**واللي كشفهن إن نتيجتنا خالفت بحثًا مستقلًا.** الدرس مش «صدّق
البحث» — الدرس إن **الاختلاف إشارة تعيد القياس، مش حكم لصالح طرف**.
رجعنا قِسنا الاتنين بأنفسنا وطلع الغلط عنا.

**والحادثة السادسة درسها مختلف عن الخمسة: قِس _وشوف_.**

قِسنا «المعاينة ↔ الترميز» فطلعت ١٧dB، والاستنتاج المباشر إن في فرق
هندسة باقي. السبب الحقيقي: الترميز انشغّل بـ`--no-captions` والمعاينة
بتحرق الكابشن — **الفرق كله كان الكابشن**. لما انطابقوا: ٥٠dB.

الرقم ما كان بيكذب، بس ما كان بيقول عن شو بيحكي. والصورة قالت
بثانية. كل هالمشروع مبني على القياس الآلي — وهون الصورة أنقذت القياس،
مش العكس. فلما تقارن مخرجات، **افتح الصورتين قبل ما تصدّق الفرق**.

---

## قرارات تصميم مقصودة (لا تعكسها بدون سبب)

- **القص من توقيت الكلمات مش من `silencedetect`** — أدق بكتير لفيديو
  الكلام. كان في `parse_silencedetect()` كبديل احتياطي، وانحذفت: ضلّت
  سنة بلا ولا مستدعي، وكود ما بينشتغل ما بينفحص.
- **الزوم ثابت داخل المقطع** مش zoompan متحرك — هيك الشكل المعروف
  بالريلز، وأخف على المعالج. بينتطبّق بتعبير `between(n,…)` على فهرس
  الإطار مع `eval=frame`، بمرساة قصّ **رقمية**: `crop` بتثبّت `iw`/`ih`
  عند إعداد الوصلة وما بتتتبّع مقاسًا متغيّرًا لكل إطار، فمرساة
  `(iw-W)/2` بتقصّ من المكان الغلط بالمقاطع المكبّرة.
- **بنمط `pad` الزوم بينطبق على الخلفية بس، ما في punch-in على الشخص.**
  سببين: (١) لو كبّرنا المقدّمة مع الزوم بترجع تنقصّ، وهاد بيلغي سبب
  وجود `pad` من أساسه — النمط موجود عشان ما ينضيع ولا بكسل. (٢) الـ
  punch-in لغة ريلز مش لغة يوتيوب؛ فيديو مبطّن والشخص بينطّ فيه شكله
  رديء. لو بدك punch-in على العريض، هاد بيعني قبول قصّ — يعني `crop`.

- **الكابشن PNG overlay مش ASS/libass** — libass بيعتمد على fribidi
  وبتصير مفاجآت مع العربي. PNG بيضمن الشكل ١٠٠٪ لأننا احنا اللي رسمناه.

- **الكابشن تسلسل صور مفهرس بالإطار وoverlay واحد** — مش overlay لكل
  كابشن ولا دفعات. سلسلة overlay طويلة بتاكل الذاكرة مع العدد (٩٤١ MiB
  عند ٤٠ كابشن، ٢٧٧١ MiB عند ٢٠٠)؛ التسلسل ثابت عند ~٨٠ MiB. الملف رقم
  `n` هو الكابشن المعروض بالإطار `n` — فالفهرس هو الزمن، وما في تقريب.

  **وكل صور التسلسل لازم تكون بنفس المقاس بالضبط.** مُدخل الصور بياخد
  أبعاد التيار من **أول ملف**، وأي اختلاف بعده بيقطع المخرَج بصمت:
  ٤٠٧×٢٠٨ و٤٠٨×٢٠٨ — فرق بكسل واحد — أعطت ٧٣ إطار من ١٤٤. لهيك في
  `pad_to_box` بتبطّن كل كابشن لنفس الصندوق قبل ما ينربط.

  وليش مش `concat` demuxer للصور: قاعدة زمنه ثابتة على ١/٢٥ ثانية، فكل
  حدّ كابشن بيتقرّب لمضاعف ٤٠ms = نص إطار عند ٣٠fps.

- **`remap_words` بتقبل التداخل الجزئي** (٤٥٪ من مدة الكلمة) — لو
  اشترطنا الكلمة كاملة جوا المقطع، كلمات حدود القص بتضيع.

- **إطار الكاريوكي بينتهي عند بداية الكلمة الجاية، مش عند نهاية كلمته.**
  Whisper بيرجّع فراغ صغير بين كل كلمتين. لو أنهينا الإطار عند
  `w["end"]` بيصير ما في ولا كابشن معروض بهالفراغ، والكابشن بيرفرف
  بنص الجملة. مقاس على توقيت واقعي: التغطية ٦٣٪ -> ٨١٪.

- **جسر الفجوة بين المجموعات = `cuts.min_gap` بالضبط.** فجوة ≤ منها:
  آخر إطار بالمجموعة بيمتد لبداية المجموعة الجاية. أطول: الكابشن
  بيختفي وهاد مقصود.

  **ليش مربوطة بـ`min_gap` مش رقم ثابت؟** القص بيشيل أي صمت **أطول**
  من `min_gap`، يعني كل فجوة بتنجى بعد القص ≤ `min_gap`. فلو حطينا
  عتبة أقل (مثلًا ٠.٤٠ مع `min_gap=٠.٤٥`) بتضل نافذة ٠.٤٠–٠.٤٥ فيها
  فجوات ناجية وما بتنجسر — يعني طفي باقي. الربط بيقفل النافذة لحاله
  مهما غيّرت `min_gap`. `cli.py` بتمرّرها؛ `DEFAULT_BRIDGE_GAP`
  بـ`captions.py` بس للنداء المباشر.

  المقارنة `<=` مش `<`: الفجوة اللي بتساوي `min_gap` بالضبط بتنجى من
  القص (الشرط هناك `> min_gap`) فلازم تنجسر كمان.

---

## الإعدادات المهمة (`config.json`)

| المفتاح | الأثر |
|---|---|
| `cuts.min_gap` | أي صمت أطول منه بينشال. صغّره = إيقاع أسرع |
| `cuts.pad` | هامش قبل/بعد كل مقطع. صغّر كتير = بتنقص حروف |
| `motion.zoom_cycle` | دورة الزوم بين المقاطع |
| `captions.max_words` | كلمات الكابشن الواحد. ٣-٤ الأفضل للريلز |
| `captions.y_ratio` | ارتفاع الكابشن. ٠.٧٢ بيسيب ~١٠٥px فوق واجهة إنستغرام |

---

## التشغيل بدون Whisper

```bash
python -m autoreel.cli in.mp4 --srt subs.srt -o out.mp4
```
`from_srt()` بتوزّع الوقت بالتساوي على كلمات السطر — تقريبي بس بيكفي
للتجريب أو لو عندك ترجمة جاهزة.

## أعلام مفيدة

`--no-cut` · `--no-captions` · `--no-motion` · `--keep` (يخلي الملفات المؤقتة)

`--sizes reel,square,wide` أو `--sizes all` — تصدير متعدد المقاسات.
بدون العلم بيطلع مقاس واحد بالاسم اللي كتبته.

`--sfx` · `--no-sfx` — المؤثرات الصوتية. شوف القسم تحت.

**`--preview-frames` قبل أي تصدير على فيديو جديد.** بيطلّع إطار PNG
من كل مقاس (منتصف أول مقطع) وبيوقف بلا ترميز.

> **`geometry.crop_bias` قيمة أولية مش رقم عام.** ٠.٣٠ معايَرة على
> عيّنة وحدة: رأس بين ١٦٪ و٤٤٪ من ارتفاع الإطار. تأطير أقرب أو أبعد
> بده رقم تاني، و٠.٣٨ اللي كانت قبلها كانت بتقصّ ١٣px من أعلى الرأس
> على نفس العيّنة. شوف الإطار قبل ما تصرف دقايق ترميز على مقاس
> بيقصّ الوجه.

`--dry-run` بيطبع أوامر ffmpeg بدون ما يشغّلها. باقي المنطق بيشتغل
عادي — خطة القص، الهندسة، رسم الكابشن، أسماء الملفات — فالمطبوع هو
الأمر الحقيقي مش تقريب إله. هاد اللي بيخلي `render.py` قابلة للاختبار
أصلًا، وبيخلّيك تشوف ليش طلع مقطع غلط بدون ما تستنى ترميز كامل.

---

## المؤثرات الصوتية (SFX)

```bash
python -m autoreel.cli in.mp4 --sfx -o out.mp4
```

**مطفية افتراضيًا** (`sfx.enabled: false`)، و`--sfx` بتشغّلها،
و`--no-sfx` بتغلبها حتى لو الconfig مشغّلها.

**ليش الافتراضي مطفي — مش تفضيلًا:** تشغيلها بيضرب الكلام بـ٠.٧٠
وبيضيف أصواتًا، يعني بيغيّر صوت **كل ريل موجود**. وجرّبناها مشغّلة
افتراضيًا فكسرت E2 فعلًا: الكاشف عدّ مؤثرًا كنقرة مصدر (٩ بدل ٨).

### خريطة الأحداث — `config.json` هو المصدر

```json
"sfx": {
  "enabled": false, "min_gap": 0.12, "speech_gain": 0.70,
  "events": {
    "start":   {"asset": "impact", "gain": 0.30, "enabled": true},
    "finale":  {"asset": "riser",  "gain": 0.22, "enabled": true},
    "cut":     {"asset": "whoosh", "gain": 0.22, "enabled": true},
    "zoom":    {"asset": "whoosh", "gain": 0.18, "enabled": true},
    "caption": {"asset": "pop",    "gain": 0.25, "enabled": true},
    "word":    {"asset": "tick",   "gain": 0.12, "enabled": false}
  }
}
```

| الحدث | مصدره | ملاحظة |
|---|---|---|
| `start` | الإطار ٠ | مرة وحدة |
| `finale` | بداية آخر مقطع | مرة وحدة |
| `cut` | حدود `frame_plan` التراكمية | مش عند الإطار ٠ |
| `zoom` | حدّ مقطع تغيّرت عنده قيمة الزوم | بيتطابق مع `cut` غالبًا |
| `caption` | أول إطار بكل مجموعة كابشن | الأكثر تكرارًا |
| `word` | إطار بداية كل كلمة كاريوكي | **مطفي** — ٢١١ مؤثر/دقيقة مقابل ٥٨ |

**ترتيب الأولوية عند التزاحم:**

```
start > finale > cut > zoom > caption > word
```

مش تزيينًا: قِسنا على كلام واقعي إن **تغيّرات الزوم بتقع على نفس
إطارات حدود المقاطع** (١٥ و١٥ بنفس السيناريو). `min_gap` بتاخد من كل
نافذة **الأعلى أولوية** وعند التعادل الأبكر — فالقطة بتغلب الزوم بدل
ما "الأول بيفوز" يعطي نتيجة عشوائية.

**`sfx.DEFAULTS` بـ`sfx.py` احتياطي موثَّق** للنداء المباشر بلا
`cfg` (وحدات وتجارب). بالمنتج الconfig بيفوز على كل مفتاح، وعليه
حرّاس: كل نوع لازم يكون بالconfig، وكل أصل لازم يكون موجودًا، وتبديل
أي `asset`/`gain`/`enabled` لازم **يغيّر الأمر المرسَل لffmpeg**.
وحارس على شجرة الكود بيمنع أي ربط حدث→أصل برّا `sfx.py`.

### القواعد اللي ما بتتغيّر

- **الفهرس هو الزمن.** `sample = frame × (sr // fps)` — ضرب صحيح.
  `adelay=<عيّنة>S` صفر خطأ؛ الميلي الصحيح ±٢٣ عيّنة.
- **`normalize=0` إلزامية** بـ`amix`. الافتراضية بتخفت الكلام لـ٠.١١×
  عند ٢٠ مؤثرًا **وبتنفّسه** مع انتهاء كل مؤثر.
- **`all=1` و`aformat` قبل `adelay`** — فخّان صامتان: أصل ستيريو بلا
  `all=1` بيقع عند العيّنة ٠، وأصل 44.1k بلا `aformat` بيقع بعد
  ٨٨ms من مكانه. ولا تحذير من ffmpeg بالحالتين.
- **ولا `alimiter`** — بيأخّر التيار ٢٣٩ عيّنة (٤.٩٨ms).
- **الهامش بالحساب:** ٠.٧٠ (كلام) + ٠.٩٠ (أعلى ذروة أصل) × ٠.٢٥ =
  **٠.٩٢٥ < ١.٠**. ولا عيّنة مقصوصة.

### الأصول

`assets/sfx/` — خمسة، **مولَّدة بالتركيب** من `build_assets.py`، ولا
طرف تالت فما في سؤال ترخيص. قابلة لإعادة الإنتاج بالبايت. التفصيل
بـ`assets/sfx/README.md`.

---

## الاختبارات

```bash
pip install -r requirements-dev.txt
pytest                    # الكل
pytest -m "not golden"    # بدون الصور المرجعية (مكدّس خطوط مختلف)
```

### قاعدة: نتيجة الاختبارات بتنذكر مع نسخة ffmpeg

**«٧٦٦ ناجحة» ادعاء ناقص. «٧٦٦ ناجحة على ffmpeg 7.0.2» ادعاء.**

الحادثة: انقال «٧٤١ ناجحة» بلا نسخة، وشغّلها المالك على **6.1.1**
فطلعت **٤ فاشلة** — `assert 382720 == 384000`، الصوت أقصر ١٢٨٠ عيّنة
(٢٦.٧ms). السبب إن `amix=duration=first` بتحسب الطول غير بين 6.x
و7.x، وما كان في **ولا فحص نسخة بأي مكان بالمشروع**.

انتصلّح بترتيب مقصود: **الطول قبل الفحص.** لو الطول انتثبّت بالبناء
(`apad,atrim=end_sample=N` بـ`graph.sfx_chain`) الفرق ما بيقدر يظهر
أصلًا، فـ`cuts.check_ffmpeg` بتصير شبكة أمان مش الحلّ. مقاس على
7.0.2: `duration=first`/`longest`/`shortest` التلاتة بتعطي ٣٨٤٠٠٠
بعد التثبيت، وفرق الصوت `0.00000000`.

`VERIFIED_FFMPEG = (7, 0)` · `MIN_FFMPEG = (6, 0)` — تحت الأدنى بيرمي،
وبين الاتنين بيحذّر.

### قاعدة: كل مفتاح بالconfig عليه فحص بيقرا الملف الحقيقي

**أي إعداد بينقرا من `config.json` لازم يكون عليه فحص واحد على الأقل
بيفتح `config.json` نفسه — مش config مبني جوا الاختبار.**

السبب من حادثة حقيقية: قراءة `motion.pan_px` انتقلت لقسم `geometry`
بينما المفتاح ساكن بـ`motion`، فالـpan صار صفر بصمت. كان في **١٩٠ فحص
هندسة** وما مسك ولا واحد فيهن الانحدار — لأن كلهن بيبنوا config خاص
فيهن، فبيتفقوا مع حالهم ويغلطوا مع الواقع.

`tests/test_config_wiring.py` بيغطي كل المفاتيح: بيغيّر قيمة المفتاح
بالconfig المقروء من القرص وبيتأكد إن **المخرَج تغيّر**. مفتاح ما بيغيّر
شي = إما ميت أو مفصول عن الكود، والاختبار بيفشل. لما تضيف مفتاح
جديد، ضيف صفّه هناك.

### الصور المرجعية

`tests/golden/` صور مرجعية للكابشن العربي — هون بيتمسك الانحدار اللي
ما بينمسك بالأرقام: حروف مقلوبة، مربعات فاضية، كلمة بالمكان الغلط.
بتتقارن بتسامح ٢٪ لأن رندر الخطوط بيختلف بين إصدارات Pillow/HarfBuzz،
و`tests/golden/ENV.json` بيسجّل بيئة التوليد عشان تميّز الانحدار عن
اختلاف المكدّس. بعد تغيير مقصود بالرسم:

```bash
AUTOREEL_REGEN_GOLDEN=1 pytest tests/test_golden_caption.py
```
**وشوف الصور بعينك قبل ما تكمّتها** — مرجع غلط أسوأ من لا مرجع.

---

## حالة المشروع

**مفحوص على ناتج ffmpeg نفسه** (`tests/test_acceptance.py` — مصدر
اختبار فيه شبكة وبقعة هوية ونقرات صوت):

| بيفحص | كيف |
|---|---|
| عدد إطارات المخرَج = `Σ frame_plan` | عدّ إطارات فعلي |
| ولا إطار ضايع ولا مكرّر | هوية كل إطار من بقعة لون بالمصدر |
| انزياح الصوت | نقرات مقيسة مقابل الإطار اللي بيبيّنها |
| إطار بداية كل كابشن | فحص الإطارات المستخرجة |
| الزوم لكل مقطع | ملاءمة تردّد على شبكة المصدر |
| ما بيضل ملف تالف بعد قتل ffmpeg | قتل حقيقي (`tests/test_failure.py`) |

و`--dry-run` ضلّ للطبقة اللي تحت: شكل سلسلة الفلاتر ومكان الملفات.

**مشتغّل بس بلا اختبار آلي:** `transcribe()` — يحتاج Whisper وملف صوت.
وجودة الصورة نفسها (شكل الترميز، هل التأطير حلو) لسا حكم بشري.

**ولا اعتماد على `ffprobe` — وهاد قرار مثبَّت مش صدفة.** `cuts.probe()`
بتقرا الأبعاد ووجود الصوت والمدة من نداء `ffmpeg -i` **واحد**:

- **لا ترجّع `ffprobe`.** تثبيتات ffmpeg الثابتة (الشائعة على Termux،
  وحال بيئة التطوير) ما فيها إياه، والمسار كان بينهار عند أول سطر. في
  حارس بيمنع الثابت `"ffprobe"` يرجع كوسيط أمر.
- **`Duration: N/A` فشل صريح، بلا تخمين.** رقم مخترَع بينتشر لخطة
  القص كلها.
- **المدة مقرَّبة لجزء المئة** (سطر `Duration: HH:MM:SS.ss`) — فرق
  **≤٥ms** عن `ffprobe`. **قرار مقصود، مش خلل**، وما في ضمان تطابق
  ميكروثاني ولا بينطلب. الأثر محصور بـ`--no-cut` وبلا-كلمات وحدّ آخر
  مقطع؛ بالمسار المعتاد صفر.
- **ولا demuxer خاص** لقراءة `mvhd` ولا غيره — مرفوض بقرار.

البدائل انقاست وانرفضت (`-progress` بيقيس نهاية أطول تيار مش مدة
الحاوية، فرق لحد ١٨٦ms). التفصيل بـ`cuts.probe` وبـ`REDESIGN-SPEC.md`
§8.5.

**باقي معروف:** شوف `ISSUES.md` — أهمّه إن مسار الـM مخرجات برسم
ffmpeg واحد مبني بـ`graph.py` ومختبَر بالوحدات، بس **مش موصول**:
`cli` بتشغّل ffmpeg مرة لكل مقاس، يعني فكّ ترميز المصدر وقصّه
بينعادوا M مرة.

**لسا ما انبنى:** موسيقى خلفية، b-roll أوتوماتيك، كشف اللقطات الضعيفة.
