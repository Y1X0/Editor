# REDESIGN-SPEC — المسار الواحد وإصلاح الصوت (CR-2)

> **حالة الوثيقة:** مواصفات للمراجعة، **مع تصحيحات المرحلة ٠** (§0.1).
> **ولا سطر كود إنتاج تغيّر.** كل رقم بهالملف **مقاس** على
> **ffmpeg 7.0.2-static** بهالبيئة، مش مستنتَج. سكربتات التجارب
> بـ`experiments/redesign/` وملخّصها بالملحق (§12).

## ٠.١ تصحيحات المرحلة ٠ — اقرأها قبل الباقي

المرحلة ٠ (إثبات الزوم) ما اكتفت بحلّ الزوم؛ **كشفت غلطتين بالمواصفة
نفسها**. الصفحات اللي بعد معدَّلة، بس التصحيحات مجموعة هون لأن وحدة
منهن بتغيّر الرسم اللي وافقت عليه:

| # | الغلط | التصحيح |
|---|---|---|
| ١ | كتبت "ffmpeg 6.1" | البيئة **7.0.2-static**. الرقم كان خطأ نقل مني، والتجارب كلها كانت أصلًا على 7.0.2 — بس ادعاء نسخة غلط بمواصفة يعني ما حدا يقدر يعيد إنتاج النتائج. |
| ٢ | `setpts=N/FPS/TB` بالجذع | **غلط فعلي**: بتسقّط إطارًا وبتكرّر تاني (٢ من ٣٣٦) مع بقاء **العدد صحيح**. البديل المقاس: `settb=1/FPS,setpts=N`. تفاصيل §2.3 لغم ٤. |
| ٣ | خيار أ٢ (crop بـeval) بالمخاطرة ١ | **مش موجود بهالبناء**: فلتر `crop` ما عنده خيار `eval`. الخيار أ١ هو الوحيد المتاح، وهو اللي نجح. |
| ٤ | E1 (عدد الإطارات) كافية للفيديو | **مش كافية**. الغلط ٢ فوق بيمرق من E1 كامل. لازم اختبار **هوية كل إطار**، مش عدّه — E7 بـ§9.1. |

الغلط ٢ هو الأهم: لو نفّذنا المواصفة كما وافقت عليها، كان الناتج فيه
إطار مكرر وإطار ضايع كل ~١٧٠ إطار، وكل اختبارات القبول اللي كتبتها
كانت رح تمرق. انكشف لأن المرحلة ٠ لزّمتني أبني مصدرًا كل إطار فيه
بيعرّف عن حاله.

---

## ٠. ليش هالمواصفة موجودة

المستخدم حدّد المبدأ:

> الـsource of truth لازم يكون الـactual frame count، مش الزمن المحسوب في Python.

هالمبدأ صحيح، وتطبيقه بيقلب المعمارية مش بس بيصلّح باگ. المواصفة بتغطّي
الطلبات التسعة بالترتيب: الرسم المبرهن (§2)، مصدر الحقيقة (§1)، الصوت
(§4)، ضمان عدد الإطارات (§3)، الكابشن (§5)، الأربع مخرجات (§6)، الذاكرة
(§7)، دلالات الفشل (§8)، اختبارات القبول (§9).

**قبل أي شي: تحذير منهجي.** كتبت أول رسم فلتر افتراضًا إنه صحيح،
وشغّلته، فطلع **١٨٦ إطار بدل ٢٢٣**. لو كنت كتبت المواصفة على الرسم
المفترَض بدل المقاس، كانت المواصفة كلها مبنية على غلط. الأربع فقرات
الجاية كلها نتيجة تفكيك هداك الفشل.

---

## ١. مصدر الحقيقة: خطة الإطارات

**القرار: `frame_plan` هي المرجع الوحيد لحدود القص، والزمن مشتقّ منها.**

```python
frame_plan(segs, fps) -> [n_0, n_1, ...]        # موجودة بـcuts.py
start_frames(segs, fps) -> [s_0, s_1, ...]      # جديدة: round(a * fps)
```

من هالاتنين بينشتق **كل** شي تاني:

| الكمية | الاشتقاق | ملاحظة |
|---|---|---|
| بداية المقطع i بالمصدر | `s_i` (إطار) | صحيح، بلا كسور |
| مدّة المقطع i | `n_i` (إطار) | صحيح |
| طول المخرَج | `Σ n_i` إطار | **العقد**: أي مخرَج بيخالفه = فشل |
| بداية المقطع i بالمخرَج | `Σ_{j<i} n_j` | صحيح |
| زمن أي إطار | `frame / fps` | مشتقّ، مش مخزَّن |
| حدود الصوت للمقطع i | `[s_i/fps, (s_i+n_i)/fps]` | مشتقّ |
| توقيت الكابشن | فهرس إطار مخرَج | مشتقّ (§5) |

**القاعدة الصارمة:** ما في مكان بالمسار بيخزّن حدّ قص كـ`float` ثواني
وبيرجع يقرأه. الثواني بتنحسب عند لحظة تركيب أمر ffmpeg وبس.

### ١.١ قيد لازم ينفحص عند التشغيل

الصوت بينقصّ على حدود إطارات الفيديو (§4). عشان الحدّ يطلع **عدد عيّنات
صحيح**، لازم `sample_rate % fps == 0`:

```
 24fps -> 2000.0000 عيّنة/إطار  ✅      29.97fps -> 1601.6016  ❌
 25fps -> 1920.0000             ✅      59.94fps ->  800.8008  ❌
 30fps -> 1600.0000             ✅
 50fps ->  960.0000             ✅
 60fps ->  800.0000             ✅
```

`config.json` حاليًا `fps: 30` (صحيح). **المواصفة بتشترط fps صحيح**؛
معدّلات NTSC الكسرية (29.97/59.94) خارج نطاق هالإعادة، ولازم `cli.py`
ترفع خطأ واضح بدل ما تنتج انزياح صامت.

---

## ٢. الرسم المقترح — مبرهَن، مش مفترَض

### ٢.١ الشكل النهائي

```
                      ┌─────────────────────────────────────────┐
  input.mp4  ─────────┤ [0:v] fps=FPS                           │
                      │       select='between(n,s0,e0)+…'       │   ← فيديو
                      │       settb=1/FPS                       │
                      │       setpts=N          ← مش N/FPS/TB!  │
                      │       fps=FPS            ← لا تشيلها!   │
                      │       split=M  [z0]…[zM]                │
                      └─────────────────────────────────────────┘
                      ┌─────────────────────────────────────────┐
                      │ [zn] scale=w='Σ sw_i·between(n,a_i,b_i)'│   ← زوم
                      │            h='Σ sh_i·between(n,a_i,b_i)'│   لكل مقطع
                      │            :force_original_aspect_ratio=│
                      │             increase:eval=frame         │
                      │       crop=W:H:x='Σ…':y='Σ…',setsar=1   │
                      └─────────────────────────────────────────┘
                      ┌─────────────────────────────────────────┐
                      │ [0:a] aresample=SR                      │
                      │       asplit=K [d0]…[dK]                │   ← صوت
                      │ [di]  atrim=start=si/FPS:end=(si+ni)/FPS│
                      │       asetpts=PTS-STARTPTS [ai]         │
                      │ [a0][a1]… concat=n=K:v=0:a=1 [acat]     │
                      │ [acat] asplit=M [ao0]…[aoM]             │
                      └─────────────────────────────────────────┘
  seq_reel/%06d.png ──┐
  seq_square/%06d.png ├─ -framerate FPS -start_number 0          ← كابشن
  seq_wide/%06d.png   │                                            (§5)
  seq_story/%06d.png ─┘
                      ┌─────────────────────────────────────────┐
                      │ [gn][n+1:v] overlay=x:y:eof_action=pass  │  لكل مقاس
                      │                                    [mn]  │
                      └─────────────────────────────────────────┘
  -map [mn] -map [aon]  ×M   ->   M ملف نهائي، M ترميز
```

**تشغيلة ffmpeg واحدة. فك ترميز واحد للمصدر. M ترميز نهائي. صفر ملف
فيديو وسيط.**

### ٢.٢ نتيجة التكامل المقاسة (`e16.py`)

| | ٣٠ مقطع · ١٢٠ كابشن | ٦٠ مقطع · ٣٠٠ كابشن |
|---|---|---|
| عدد الإطارات (٤ مقاسات) | ٣٦٠/٣٦٠ ✅ | ٧٢٠/٧٢٠ ✅ |
| انزياح الصوت الأقصى | ١.٩٨ms (= أرضية القياس) ✅ | ١.٩٨ms ✅ |
| تراكم الانزياح | +١.٩٨ms ✅ | +١.٩٨ms ✅ |
| كابشن على إطاره | ١٢٠/١٢٠ ✅ | ٣٠٠/٣٠٠ ✅ |

الثلاث ثوابت تحقّقوا سوا بنفس التشغيلة، مش كل واحد لحاله.

### ٢.٣ ثلاث ألغام انكشفت بالطريق

هدول مش تفاصيل تنفيذ — هدول أسباب **لازم تكون بالتعليقات بالكود**،
لأن كل واحد منهن بيرجع بصمت لو حدا "نظّف" الرسم.

#### لغم ١: `setpts` بتمسح معدّل الإطارات (`e4.py`)

```
null (بلا شي)     fps المخرَج=30  إطارات=600     ✅
setpts لحاله      fps المخرَج=25  إطارات=501     ❌  ← هون
trim لحاله        fps المخرَج=30  إطارات= 60     ✅
trim+setpts       fps المخرَج=25  إطارات= 51     ❌
trim+setpts+fps=30 fps المخرَج=30 إطارات= 60     ✅
scale لحاله       fps المخرَج=30  إطارات=600     ✅
concat لحاله      fps المخرَج=30  إطارات=1200    ✅
```

`setpts` بتخلّي معدّل الإطارات "مجهول"، فffmpeg بيرجع لافتراضه **٢٥**،
وبما إن `-fps_mode` الافتراضي `cfr` بيعيد التشكيل ٣٠→٢٥ = فقدان ١٧٪ من
الإطارات. هاد كان سبب الـ١٨٦ بدل ٢٢٣.

> **`fps=FPS` بعد `setpts` إلزامية. مش تكرار.**
> بديلها `-fps_mode passthrough` (مقاسة كمان، بتشتغل) بس بتخلّي الصحة
> معتمدة على علم مخرَج بعيد عن الرسم — التكرار داخل الرسم أوضح.

#### لغم ٢: `trim` الزمنية ما بتعطي المخطط (`e2.py`)

دلالة `trim=start=a:end=b` هي **`ceil(b*fps) − ceil(a*fps)`**، مش
`round((b−a)*fps)`:

| المقطع | المخطط | دلالة trim | فرق |
|---|---|---|---|
| (1.237, 2.981) | 52 | 52 | 0 |
| (4.512, 5.833) | 40 | 39 | −1 |
| (8.104, 9.677) | 47 | 47 | 0 |
| (11.05, 12.30) | 38 | 37 | −1 |
| (14.9, 16.44) | 46 | 47 | +1 |
| **Σ** | **223** | **222** | **−1** |

فرق واحد بس، لكنه فرق **غير قابل للتنبؤ** وبيتغيّر مع كل مقطع. الحل:
القصّ بفهارس الإطارات مباشرة، لا بالأزمنة (§3).

#### لغم ٤: `setpts=N/FPS/TB` بتحسب بعائمة (اكتُشف بالمرحلة ٠)

**هاد أخطر لغم بالملف، لأنه بيمرق من كل فحص بيعدّ.**

الجذع اللي وافقتَ عليه بيعطي **عدد الإطارات الصح** بس محتوى إطارين من
كل ٣٣٦ **غلط**: إطار مصدر بينضيع وإطار تاني بينكرّر، والاتنين بيلغوا بعض
بالعدّ.

مقاس على مصدر كل إطار فيه بيحمل رقمه مشفّرًا بصريًا:

```
n=241 -> 370   n=242 -> 371   n=243 -> 372
n=244 -> 374   ← 373 ضاع
n=245 -> 375   n=246 -> 376   n=247 -> 376   ← 376 تكرّر
```

الآلية: `setpts=N/FPS/TB` بتنحسب بـdouble. قاعدة الزمن بالmp4 غالبًا
١/١٥٣٦٠، فالقيمة = N·512. أحيانًا بتطلع `100863.99999999999` بدل
`100864`، وffmpeg بيقصّها لصحيح **أقل**، فالإطار بيقع قبل خانته بتكّة،
والـ`fps` اللي بعدها بتحطّه بالخانة السابقة.

الحل المقاس: **شيل العائمة من المعادلة**.

```
settb=1/FPS, setpts=N          ← تكّة = إطار، وPTS عدد صحيح بلا قسمة
```

جدول القياس (٣٣٦ إطار، ٨ مقاطع، كل إطار متحقَّق منه بالباركود):

| الصياغة | إطارات | محتوى غلط | fps المخرَج | |
|---|---|---|---|---|
| `fps,select,setpts=N/FPS/TB,fps` | 336 ✅ | **4** | 30 | ❌ |
| `fps,select,settb=1/FPS,setpts=N,fps` | 336 ✅ | **0** | 30 | ✅ |
| `fps,select,settb,setpts=N` (بلا fps أخيرة) | 281 | 274 | **25** | ❌ لغم ١ |
| `fps,select,setpts=N/FPS/TB` + `-fps_mode passthrough` | 336 | 0 | **30.09** | ❌ |
| `fps,select,settb,setpts=N` + `-fps_mode passthrough` | 336 | 0 | **30.09** | ❌ |
| `fps,select,setpts=PTS-STARTPTS,fps` | **561** | 294 | 30 | ❌ |

ثلاث خلاصات:

1. **`-fps_mode passthrough` مرفوض نهائيًا.** المحتوى بيطلع صح بس الحاوية
   بتعلن **30.09 fps**. المواصفة الأولى كانت بتعتبره بديلًا مقبولًا —
   مش مقبول. `fps=FPS` بآخر الجذع إلزامية.
2. **`setpts=PTS-STARTPTS` كارثة**: بتحافظ على فجوات التوقيت الأصلية،
   فالـ`fps` بتعبّيها بتكرار — ٥٦١ إطار بدل ٣٣٦.
3. الجذع الوحيد الصحيح:
   `fps=FPS, select='…', settb=1/FPS, setpts=N, fps=FPS`

#### لغم ٦: تسلسل الصور لازم يكون **موحّد المقاس** (اكتُشف بالمرحلة ٧)

المواصفة قالت "تسلسل صور مفهرس بالإطار" وما قالت شي عن الأبعاد. صور
الكابشن بالإنتاج بتفرق ببكسل أو اتنين حسب عرض النص.

**الفرق ببكسل واحد بيقطع المخرَج.** مقاس على نفس السيناريو (١٤٤ إطار):

| التسلسل | الناتج |
|---|---|
| صور ٤٠٧×٢٠٨ و٤٠٨×٢٠٨ مخلوطة | **٧٣**/١٤٤ ❌ |
| نفسها + صورة فارغة بمقاس الصندوق | **٧٣**/١٤٤ ❌ |
| الكل موحّد بمقاس الصندوق | **١٤٤**/١٤٤ ✅ |

الآلية: `image2` بياخد أبعاد التيار من **أول ملف**، وأي تغيّر مقاس
بالنص بيجبر إعادة ضبط الوصلة وسط التشغيل، والمخرَج بيتقطع.

الحل: `captions.pad_to_box` بتوسّط كل كابشن داخل لوحة شفافة بمقاس
`max(عرض) × max(ارتفاع)`. التوسيط بيحافظ على الشكل بالضبط لأن الoverlay
بيوسّط الطبقة أصلًا (`x=(W-w)/2`, `y=Y-h/2`). والتبطين بيصير **مرة لكل
كابشن مميّز** فبتضل الوصلات هي اللي بتغطي التكرار.

> **وكيف انخدعت أول مرة:** فحصت "هل تسلسل الصور بيقبل أبعادًا مختلفة؟"
> بفكّ ترميز التسلسل لحاله — ورجع ١٢/١٢ إطار، فاستنتجت إن المخلوط
> مقبول. بس الفكّ لحاله مش المسار: الانكسار بيصير عند `overlay`
> والمخرَج. **قِست الشي الغلط** — نفس نمط `crop.iw` بالمرحلة ٥.
>
> وأول عرَض شفته كان تحذيرات صوت (`Non-monotonic DTS`)، فطاردتها
> شوي قبل ما أكتشف إن القطع بيصير **حتى بلا أي صوت**. العرَض الأعلى
> صوتًا مش دايمًا السبب.

#### لغم ٥: مدايات `select` لازم تكون منفصلة

`select` بتمرّر إطار المصدر **مرة وحدة** مهما تداخلت المدايات، فتداخل
مدايتين بيعطي إطارات أقل من `Σ n_i` بصمت. `segments_from_words` بتدمج
المتداخلة أصلًا، بس الرسم صار **يعتمد** على هالضمانة — فلازم
`assert` صريحة عند البناء، مش افتراض.

#### لغم ٣: تسمية مخرَج الفلتر بتنربط مرة وحدة

```
Output with label 'ao' does not exist in any defined filter graph,
or was already used elsewhere.
```

٤ مخرجات = ٤ نسخ صوت. `[acat]asplit=4[ao0][ao1][ao2][ao3]` إلزامية.
ما إلها بديل.

---

## ٣. الفيديو: كيف بينضمن عدد الإطارات (الطلب ٤)

### ٣.١ الصياغات اللي جرّبناها

الاختبار: `counter.mp4` (٦٠٠ إطار، ٢٠s، ٣٠fps)، ٥ مقاطع، **المخطط ٢٢٣ إطار**.

| الصياغة | الناتج | |
|---|---|---|
| `trim=start:end` + concat (افتراضي) | 186 | ❌ لغم ١ + لغم ٢ |
| `trim=start_frame:end_frame` + concat | 187 | ❌ لغم ١ |
| `trim` زمنية + concat + `fps` | 222 | ❌ لغم ٢ |
| `fps`→`split`→`trim` بالإطارات→concat→`fps` | **223** | ✅ |
| نفسها مع `-fps_mode passthrough` | **223** | ✅ |
| `trim` زمنية بحدود على شبكة الإطارات + `fps` | **223** | ✅ |
| `select` + `setpts=N/FPS/TB` + `fps` | **223** | ⚠️ العدد صح والمحتوى غلط — لغم ٤ |
| **`select` + `settb` + `setpts=N` + `fps`** | **223** | ✅ **المختارة** |

> الصفّ قبل الأخير هو سبب وجود §0.1: العدّ لحاله ما بيميّز بين
> "٢٢٣ إطار صح" و"٢٢٣ إطار فيهن واحد مكرر وواحد ضايع".

### ٣.٢ ليش `select` مش `split`+`trim`

الاتنين بيعطوا العدد الصح. الفرق بالذاكرة، وهو فرق قاطع:

| الصياغة | ذروة الذاكرة |
|---|---|
| `split=K` + `trim` لكل فرع | **٢٣٢١ MiB** |
| `select` بتعبير اتحاد | **٢١٠ MiB** |

السبب: `split=K` بيغذّي K فروع، كل فرع بيستهلك من مكان مختلف بالخط
الزمني، فالفلتر مجبور يخزّن كل المدى بين أول مستهلك وآخره — عمليًا
**الفيديو كامل بالذاكرة**. `select` بتمرّ مرة وحدة وبتقرّر إطار إطار:
ذاكرة O(1).

### ٣.٣ الصياغة النهائية

```
[0:v] fps=FPS,
      select='between(n,s0,s0+n0-1)+between(n,s1,s1+n1-1)+…',
      settb=1/FPS,
      setpts=N,
      fps=FPS
```

* `fps=FPS` **الأولى**: بتضمن CFR قبل ما نعتمد على `n` كفهرس إطار.
  بدونها `n` بيعني رقم الإطار المفكوك، وهاد بيفترق عن فهرس الشبكة مع
  مصدر VFR (تسجيل موبايل مثلًا).
* `select`: بتمرّر الإطار لو فهرسه بأي مدى محفوظ. المدايات **مغلقة من
  الطرفين** — `s` لحد `s+n−1` = `n` إطار بالضبط، و**لازم تكون منفصلة**
  (لغم ٥).
* `settb=1/FPS`: التكّة = إطار. بعدها `setpts=N` عدد صحيح بلا أي قسمة
  عائمة — لغم ٤.
* `fps=FPS` **التانية**: لغم ١.

### ٣.٤ برهان القياس (`e13.py`)

| مقاطع | مخرجات | إطارات المخطط | الناتج | ذروة الذاكرة |
|---|---|---|---|---|
| 30 | 1 | 1080 | 1080 ✅ | ~٢٤٠ MiB |
| 30 | 4 | 1080 | 1080 ✅ | ٥٠٣ MiB |
| 120 | 1 | 4320 | 4320 ✅ | ٢١٠ MiB |
| 120 | 4 | 4320 | 4320 ✅ | ٥١٢ MiB |
| 300 | 1 | 10800 | 10800 ✅ | ٢٣٧ MiB |
| 300 | 4 | 10800 | 10800 ✅ | ٤٩٢ MiB |

الذاكرة **مستقلة عن عدد المقاطع** (٣٠ ← ٣٠٠) — وهاي بالضبط الخاصية
اللي بدنا إياها للريلات الطويلة.

### ٣.٥ طول التعبير

التعبير بيكبر خطّيًا: ٣٣ KB عند ٣٠٠ مقطع. حدود سطر الأوامر على لينكس
~٢ MB بس على أندرويد أضيق. **المواصفة بتلزم `-filter_complex_script`
مع ملف** (مقاس، بيشتغل: ٤١+٤١ = ٨٢ إطار كما مخطّط) بدل تمرير الرسم
بالسطر. فايدة تانية: الملف بينحفظ مع `--keep` فبيصير قابل للفحص.

---

## ٤. الصوت: ترميز AAC واحد، ووين بالضبط (الطلب ٣)

### ٤.١ المشكلة مقاسة، مش مستنتَجة

مصدر بنقرة كل ٠.٥s، كاشف قمم متحقَّق منه (أرضية ٢ms). القياس: وين وقعت
كل نقرة مقابل وين المفروض حسب خطة الإطارات.

**المعمارية الحالية** — ترميز AAC لكل مقطع، ثم `concat -c copy`:

| عدد المقاطع | أول نقرة | آخر نقرة | الميل |
|---|---|---|---|
| 5 | +8.00ms | +31.31ms | +5.83 ms/قطعة |
| 15 | +8.00ms | +84.65ms | +5.47 ms/قطعة |
| 30 | +8.00ms | +164.65ms | +5.40 ms/قطعة |
| 60 | +8.00ms | +324.65ms | +5.37 ms/قطعة |

**خطّي تمامًا: ≈ ٥.٤ms لكل قطعة، وإزاحة ثابتة +٨ms.** الفيديو بنفس
التجربة كان ٣٦٠/٣٦٠ إطار — يعني الرقم هو **انزياح صوت عن صورة**، مش
مجرد إزاحة عامة. عند ٢٠٠ قطعة (ريل ٣ دقايق) = **~١.٠٨ ثانية**.
مزامنة الشفايف بتنكسر عند ٤٥–١٠٠ms، فالانكسار بيصير محسوس عند ~١٥ قطعة.

**المقترحة** — `atrim` + `concat` فلتر + ترميز AAC واحد:

| | أول | آخر | تراكم |
|---|---|---|---|
| كل الأحجام | −0.00ms | +1.98ms | **+1.98ms** |

+١.٩٨ms = عرض النقرة نفسها = **أرضية القياس**. يعني: صفر انزياح قابل
للقياس بهالأداة.

### ٤.٢ وين بالضبط بيصير ترميز AAC

```
مرة وحدة، بمكان واحد: عند كتابة كل ملف مخرَج نهائي.
```

بالتفصيل، الصوت بيمرق هيك:

| المرحلة | الصيغة | ترميز؟ |
|---|---|---|
| `[0:a]` | فكّ ترميز المصدر مرة وحدة | فكّ |
| `aresample=SR` | PCM داخلي (`fltp`) | لا |
| `asplit=K` → `atrim` → `asetpts` | PCM | لا |
| `concat=n=K:v=0:a=1` | PCM | لا |
| `asplit=M` | PCM | لا |
| `-c:a aac` لكل مخرَج | AAC | **نعم — هون وبس هون** |

M ترميز AAC (واحد لكل مقاس)، وكلهن على **نفس** تيار الـPCM الموصول
مسبقًا. ما في ولا ترميز/فكّ ترميز وسيط، فما في priming padding يتراكم.

**الفرق عن الحالي:** الحالي بيرمّز AAC **K مرة** (مرة لكل مقطع) وبعدين
بيلزق الحاويات بلا إعادة ترميز. كل ترميز بيحقن priming delay بأول
المقطع، والـ`concat` demuxer ما بيقدر يشيله لأن المقاطع صارت حاويات
مستقلة. هاد مصدر الـ٥.٤ms.

### ٤.٣ ليش `atrim` مش `aselect`

جرّبنا اتنينهن بنفس الرسم:

| | أول | آخر | أقصى | ملاحظة |
|---|---|---|---|---|
| `aselect='between(t,…)'` | −17.33ms | −16.69ms | **35.35ms** | ❌ |
| `atrim=start:end` | −0.00ms | +1.98ms | **1.98ms** | ✅ |

`aselect` بتقرّر على مستوى **إطار الترميز** (١٠٢٤ عيّنة ≈ ٢١.٣ms)،
فحدودها بتنطّ لأقرب إطار: ضجيج ±٢١ms. مش متراكم، بس مرئي.
`atrim` بتقصّ على مستوى **العيّنة**.

### ٤.٦ حدود الصوت بفهرس العيّنة (تعديل المرحلة ٤)

المواصفة كانت بتقول `atrim=start={s/fps:.9f}:end=…`. `atrim` عندها
`start_sample`/`end_sample` — نظير `start_frame`/`end_frame` بالفيديو —
وهي المستعملة هلأ:

```
atrim=start_sample={s·(sr//fps)}:end_sample={(s+n)·(sr//fps)}
```

**بس بأمانة: الفرق مش مقاس.** قِسنا الصياغتين على ٤٠ مقطع:

| | إطارات | نقرات | أقصى انزياح | تراكم | مجموع العيّنات |
|---|---|---|---|---|---|
| `start=`/`end=` بالثواني | 480/480 | 40/40 | ١.٩٨ms | ٠.٠٠ms | ٧٦٨٠٠٠ ✅ |
| `start_sample`/`end_sample` | 480/480 | 40/40 | ١.٩٨ms | ٠.٠٠ms | ٧٦٨٠٠٠ ✅ |

متطابقين تمامًا. `37/30` بتنطبع `1.233333333` = ٥٩١٩٩.٩٨٤ عيّنة، بس
تقريب ffmpeg الداخلي بيمتصّ الفرق.

المكسب بنيوي مش مقاس: الضبط بيصير **بالبناء** بدل ما يعتمد على طباعة
عشرية وتحليل مدّة داخل ffmpeg. تبنّيناه لأنه أرخص من إثبات إن التقريب
الداخلي رح يضل هيك بكل نسخة ومعدّل.

### ٤.٤ ليش `asplit=K` مقبولة رغم درس §3.2

`split` بالفيديو كانت كارثة ذاكرة لأن الإطار ثقيل. الصوت مونو 48k =
٩٦ KB/s؛ تخزين ٦٠٠ ثانية كاملة = ٥٧ MB بأسوأ حالة، وعمليًا أقل بكتير
لأن الفروع بتستهلك بترتيب متقارب. القياس بـ`e13` أكّد: الذاكرة بقيت
~٥٠٠ MiB عند ٣٠٠ مقطع — يعني `asplit=300` ما نفخت شي.

**فالتقسيم مقصود:** `select` للفيديو (دقة الإطار كافية، الذاكرة حرجة)،
`atrim` للصوت (دقة العيّنة لازمة، الذاكرة رخيصة).

### ٤.٥ مقاطع الصوت بتنطبق على العيّنة بالضبط

عند `fps=30`, `SR=48000`: كل إطار = **١٦٠٠ عيّنة بالضبط**. حدّ المقطع
`(s_i + n_i)/fps` بيعطي عدد عيّنات صحيح، فـ`atrim` ما بتقرّب. مجموع
مدد الصوت = `Σ n_i / fps` = مدة الفيديو بالضبط. هاد سبب شرط §1.1.

---

## ٥. الكابشن: من وين بياخد وقته وترتيبه (الطلب ٥)

### ٥.١ العقد

`captions.py` **ما بيتغيّر ولا سطر** بمنطق الرسم. اللي بيتغيّر هو
**التوقيت اللي بينسلّم إله** والطريقة اللي بينحقن فيها بffmpeg.

الأربع معلومات اللي بيحتاجها الرسم بتوصله هيك:

| المعلومة | من وين | ملاحظة |
|---|---|---|
| **فهرس الكلمة المنطقي** | `group["raw"][i]` — نفس المصدر الحالي | ما بيمرق على bidi أبدًا |
| **بداية/نهاية الكلمة** | `remap_words(words, segs, durations=[n/fps …])` | مثل اليوم بالضبط |
| **الترتيب البصري bidi** | `_bidi_runs(tokens)` جوا `render_caption` | ما بينلمس |
| **ختم الزمن العالمي** | **جديد: فهرس إطار مخرَج** | §5.2 |

`remap_words` **لسا لازمة وما بتنشال**. الجديد إنها بتاخد `durations`
مشتقّة من خطة الإطارات (`n_i/fps`) مش من `b−a` — وهاد أصلًا مطبَّق
ومغطّى بـ`test_remap_uses_the_durations_it_is_given`.

### ٥.٢ التوقيت بينتحوّل لفهارس إطارات

```python
def caption_frames(caps, fps, total_frames):
    """[(png, start_s, end_s)] -> [(png, start_frame, end_frame)] بلا فجوات ولا تراكب."""
    out = []
    for png, s, e in caps:
        a = min(total_frames, max(0, round(s * fps)))
        b = min(total_frames, max(a + 1, round(e * fps)))
        out.append((png, a, b))
    # المقطع [a,b) نصف مفتوح؛ التراكب بينحلّ لصالح الأحدث
    return out
```

بعد هالتحويل، **الزمن ما بيرجع يظهر بمسار الكابشن أبدًا**. الفهرس هو
الزمن. هاد هو تطبيق مبدأ §1 على الكابشن.

### ٥.٣ الحقن: تسلسل صور مفهرس بالإطار

ثلاث طرق جرّبناها. الاتنين الأولى **مرفوضتين**، والسبب مهم:

#### ❌ سلسلة `overlay` لكل كابشن (اللي بيعملها الكود اليوم)

صحيحة بالبناء (`enable='between(n,a,b-1)'` فهارس صحيحة)، بس ما بتتوسّع:

| عدد الكابشن | زمن | ذروة الذاكرة | مدخلات |
|---|---|---|---|
| 40 | 7.1s | ٩٤١ MiB | 161 |
| 200 | 24.8s | **٢٧٧١ MiB** | 801 |

#### ❌ مسار كابشن عبر `concat` demuxer

**قاعدة زمن الـconcat demuxer للصور ثابتة على ١/٢٥ ثانية.** مقاس:

```
المتوقع 1.233333s -> صار 1.24    (= 31/25)
المتوقع 6.300000s -> صار 6.28    (= 157/25)
```

كل حدّ بينقرّب لمضاعف ٤٠ms. عند ٣٠fps هاد لحد **٢٠ms = نص إطار**،
فخُمس الكابشنات بلّشت إطار بدري (٢٠/٢٥ صح بس). جرّبنا `-framerate` (ما
بتنقبل)، `-r 30` (بتلغي المدد أصلًا)، `fps=…:round=up` (٣ مزحلقين بدل
٥)، وتصحيح المدد تراكميًا (ما تغيّر شي). **القاعدة الزمنية ما بتنغيّر،
فالطريق مسدود.**

#### ✅ تسلسل صور مفهرس بالإطار — المختارة

لكل مقاس، مجلد فيه **وصلة رمزية لكل إطار مخرَج**، اسمها فهرس الإطار،
بتأشّر على الـPNG اللي المفروض يظهر فيه:

```
seq_reel/000000.png -> ../png_reel/0000.png
seq_reel/000001.png -> ../png_reel/0000.png
…
seq_reel/000012.png -> ../png_reel/0001.png     ← الكابشن الجاي بلّش هون
```

```
-framerate FPS -start_number 0 -i seq_reel/%06d.png
```

**حتمي بالبناء: ما في ولا عملية فاصلة بالمسار. الفهرس هو الزمن.**

القياس (`e15.py`):

| إطارات | كابشن | وصلات | حجمها | زمن الإنشاء | ذروة الذاكرة | دقة |
|---|---|---|---|---|---|---|
| 296 | 25 | 296 | ٨ KiB | ٠.٠٢s | ٧٨ MiB | ٢٥/٢٥ ✅ |
| 1800 | 200 | 1800 | ٥٣ KiB | ٠.١٠s | ٨٠ MiB | ٢٠٠/٢٠٠ ✅ |
| 5400 | 600 | 5400 | ١٥٨ KiB | ٠.٢٩s | ٨٥ MiB | ٦٠٠/٦٠٠ ✅ |
| 10800 | 1200 | 10800 | ٣٣٨ KiB | ٠.٦٩s | ٨٢ MiB | ١٢٠٠/١٢٠٠ ✅ |

الذاكرة **ثابتة**، والكلفة القرصية مهملة (وصلة رمزية ≈ ٣٢ بايت).

**التكلفة الحقيقية: عدد الملفات.** ريل ٩٠ ثانية × ٤ مقاسات = ١٠٨٠٠
وصلة. على تيرمكس مقبول (قِسناها: ٠.٦٩s لـ١٠٨٠٠)، بس لازم:
* الوصلات جوا `workdir` اللي بينمسح، مش جنب المخرَج.
* fallback لنسخ حقيقي (`shutil.copy`) لو نظام الملفات ما بيدعم الوصلات
  الرمزية (بعض تخزين أندرويد المشترك). التكلفة وقتها = حجم PNG × إطارات
  — **لازم تحذير صريح** لأنها ممكن توصل مئات الميغا. البديل عند الفشل:
  الرجوع لسلسلة `overlay` على دفعات.

### ٥.٤ ما اللي ما بيتغيّر

* `_bidi_runs` و`_resolve_dirs` وحلقة الرسم — **ولا حرف**. سلوك fd8b76e
  محفوظ لأن الرسم ما إله علاقة بالتوقيت.
* `_fit` و`_layout` و`assert_fits_frame` — ما إلهن علاقة.
* الصور المرجعية بـ`tests/golden/` لازم تضل **متطابقة بايت-ببايت** بعد
  الإعادة. أي تغيّر فيها = المواصفة انكسرت.
* توقيت إطار الكاريوكي (`e = بداية الكلمة الجاية`) وجسر
  `bridge_gap = cuts.min_gap` — بينضلّوا بالثواني جوا `build_caption_pngs`،
  والتحويل لفهارس بيصير **بعدها** (§5.2). المنطق ما بينلمس.

---

## ٦. الأربع مخرجات = أربع ترميزات (الطلب ٦)

### ٦.١ العدّ

| | الحالية | المقترحة |
|---|---|---|
| فكّ ترميز المصدر | M مرة | **١** |
| ترميز فيديو | `M × (K + ⌈C/60⌉)` | **M** |
| ترميز AAC | `M × K` | **M** |
| ملفات فيديو وسيطة | `M × (K + ⌈C/60⌉)` | **٠** |

عند M=4, K=60, C=240: **٢٥٦ ترميز ← ٤**.

### ٦.٢ القياس المباشر (`e18.py` — ٦٠ مقطع، ٢٤٠ كابشن، ٤ مقاسات، `veryfast crf=20`)

| | الحالية | المقترحة |
|---|---|---|
| زمن الجدار | **١٤٩.١s** | **٢٢.٨s** (٦.٥×) |
| ترميزات فيديو | ٢٥٦ | ٤ |
| قرص وسيط | ١١٩.٠ MB | **٠** |
| ذروة الذاكرة | ٩١٣ MiB | ١٢٧١ MiB |
| عدد الإطارات | ٧٢١ ❌ | ٧٢٠ ✅ |

الذاكرة أعلى — ومعالجتها بـ§7.

### ٦.٣ الصفّ ❌ اللي بالجدول: باگ جديد بكود الإنتاج

المعمارية الحالية طلّعت **٧٢١ إطار والمخطط ٧٢٠**. تتبّعناه:

```
مجموع إطارات المقاطع المفردة : 720
بعد concat -c copy (base)    : 720   ✅
بعد أول دفعة حرق كابشن       : 721   ❌ ← هون
بعد باقي الدفعات             : 721
```

وأكّدناه على **كود الإنتاج نفسه** مش على إعادة كتابتنا:

```
$ python -m autoreel.cli long.mp4 --srt many.srt --sizes reel
  خطة الإطارات: 538 إطار (24 مقطع، 30fps)
  المخرَج الفعلي: 539 إطار                        ❌ +1

$ … --no-captions
  مخطط 538 · فعلي 538 · فرق +0                    ✅
```

**`burn_captions` بتزيد إطار واحد.** السبب: مدخلات الـPNG تيارات صورة
بإطار واحد ومدة مجهولة، وما في `-shortest` ولا `-frames:v` يحدّ المخرَج،
فالـoverlay بيطلّع إطار زيادة عند EOF.

هاد **CR-4** — باگ من نفس عائلة CR-1 (المخرَج بيخالف المخطط)، وما كان
مكشوف لأن ولا اختبار كان بيقارن عدد إطارات المخرَج بـ`frame_plan`.
الإعادة بتلغيه بالبناء؛ **بس لازم يتسجّل بـISSUES.md حتى لو الإعادة
تأجّلت**، لأنه موجود اليوم بكل مخرَج.

### ٦.٤ الترتيب vs التوازي

المخرجات بتنكتب **متوازية بنفس التشغيلة** (فك ترميز مشترك). لكن الذاكرة
بتكبر مع عددهن (§7)، فالمواصفة بتلزم سقف قابل للضبط:

```
--max-parallel-outputs N     (افتراضي: ٢)
```

فوق السقف: تشغيلات متتابعة، كل وحدة فيها ≤N مخرَج. المصدر بينفك ترميزه
مرة لكل تشغيلة (يعني `⌈M/N⌉` مرات مش M)، ولسا أرخص بكتير من الحالي.

---

## ٧. الذاكرة والملفات الوسيطة (الطلب ٧)

### ٧.١ شو بياكل الذاكرة فعلًا

القياس (`e18.py B`، مصدر ١٠٨٠p، `veryfast crf=20`):

| مخرجات متزامنة | ذروة الذاكرة | زمن |
|---|---|---|
| ١ | **٤٨٥ MiB** | ٧.٨s |
| ٢ | **٦٨٩ MiB** | ١١.٢s |
| ٤ | **١٣١٠ MiB** | ٢٤.٠s |

وبالمقابل، القياس بـ`e13.py`: الذاكرة **ما بتتأثر بعدد المقاطع**
(٣٠ ← ٣٠٠) ولا **بعدد الكابشنات** (٤٠ ← ٢٠٠). المتغيّر الوحيد المهم هو
**عدد المرمِّزات الشغالة سوا**.

هاي نتيجة مريحة: الريل الأطول ما بيكلّف ذاكرة أكتر، بيكلّف وقت أكتر بس.

### ٧.٢ سياسة أندرويد/تيرمكس

| البند | القرار |
|---|---|
| السقف الافتراضي | `--max-parallel-outputs 2` (~٦٩٠ MiB) |
| المخرَج الواحد | ~٤٨٥ MiB — يزبط على أي جهاز حديث |
| الرسم | `-filter_complex_script` ملف، مش سطر أوامر (§3.5) |
| الملفات الوسيطة | صفر ملف فيديو. الوصلات + PNG بس |
| قرص الـPNG | نفس اليوم بالضبط (`build_caption_pngs` ما بيتغيّر) |
| قرص الوصلات | ٣٣٨ KiB لـ١٠٨٠٠ إطار — مهمل |
| `--keep` | بيخلّي الوصلات والرسم — يعني صار في شي يتفحّص |

**الربح الصافي مقابل اليوم:** ١١٩ MB ملفات فيديو وسيطة ← صفر، على حساب
ذاكرة أعلى بـ٤ مخرجات (وهاد محكوم بالسقف).

### ٧.٣ ملاحظة مقاسة عن `-loop 1`

للتسجيل، عشان ما حدا يجرّبها بعدين: تغذية الـPNGات كمدخلات
`-loop 1 -t <مدة المخرَج>` (الشكل "الطبيعي") **بتقتل العملية**:

```
١٦٠ مدخل -loop 1 -> ذروة ١٣٥٥٨ MiB -> SIGKILL
```

كل مدخل بيولّد تيار بطول المخرَج كامل. لا تعملها.

---

## ٨. دلالات الفشل (الطلب ٨)

### ٨.١ الوضع المقاس

قتلنا تشغيلة بعد ١٦٪ (`e17`):

```
-progress pipe:1 : ١٦ تحديث بـ٨ ثواني، آخر إطار=698 من 4320 (16%)
reel.mp4   : موجود، ١٥.٢MB، غير قابل للقراءة (mp4 بلا moov)
square.mp4 : موجود، ٢٢.٨MB، غير قابل للقراءة (mp4 بلا moov)
```

نتيجتان:
1. **`-progress pipe:1` بيعطي تقدّم على مستوى الإطار** — أول مرة بيصير
   عندنا شريط تقدّم حقيقي للمهمة كاملة. الحالي ما بيقدر يعطيه لأن
   المهمة مقسومة على ٢٥٦ عملية منفصلة.
2. **الفشل بيخلّي ملفات بتبيّن مخرجات وهي تالفة.** هاد أخطر من الفشل
   نفسه.

### ٨.٢ ما تلزمه المواصفة

| البند | القرار |
|---|---|
| كتابة ذرّية | ffmpeg بيكتب `out.mp4.part`؛ `os.replace` للاسم النهائي بس بعد `returncode == 0` |
| ملفات جزئية | تنمسح عند الفشل. **ما بينضل ولا ملف بشكل مخرَج** |
| تقدّم | `-progress pipe:1` -> نسبة مئوية من `Σ n_i` (المخطط معروف مسبقًا!) |
| الرسالة | آخر ٢٠ سطر stderr + مسار ملف الرسم المحفوظ |
| المقاطعة (Ctrl-C) | نفس مسار الفشل: مسح الجزئي، رسالة نظيفة |
| استئناف | **مش مدعوم، وهاد مقصود** — §8.3 |

### ٨.٣ ليش لا استئناف (والمقايضة صريحة)

السؤال كان: "إذا فشل FFmpeg في الدقيقة ٨٠". المعمارية الحالية بتخسر
**دفعة وحدة** (~٦٠ كابشن)؛ المقترحة بتخسر **التشغيلة كاملة**.

بس الأرقام بتقلب المقارنة:

* المقترحة أسرع **٦.٥×** (١٤٩s ← ٢٣s). إعادة تشغيل كاملة أرخص من
  إكمال جزئي بالمعمارية القديمة.
* الحالية "بتحفظ" تقدّمها بملفات وسيطة بتاكل ١١٩ MB، **وما فيها منطق
  استئناف أصلًا** — يعني الفشل بيرجّعك للصفر كمان، بس مع نفايات على
  القرص.
* الاستئناف الحقيقي بده تجزئة المخرَج وترميز لكل جزء ثم `concat` —
  وهاد بيرجّع بالضبط المشكلة اللي عم نصلّحها (§4.1).

**فالقرار: لا استئناف، و`--max-parallel-outputs` ما إلها داعي — بس مش
للسبب اللي كتبته أول مرة.**

كتبت بتقرير المرحلة ٨ إن الـM مخرجات صاروا تشغيلة ffmpeg وحدة عبر
`split`/`asplit` فما ضلّ في شي ينتسقّف. **هاد غلط.** `build_graph`
بتدعم M مخرجات برسم واحد فعلًا، بس `cli._one_export` بتنادي
`build_output` **لكل مقاس لحاله**، وكل نداء بيمرّر `[(اسم, cfg)]` —
مقاسًا واحدًا. `--sizes all` = **٣ تشغيلات ffmpeg متتابعة**، مقيسة
بـ`--dry-run`.

السبب الصح إنه ما إلها داعي: المقاسات بتنعمل **بالتتابع** بحلقة، وحدة
بعد وحدة. سقف التوازي بيسقّف شيئًا بيشتغل بالتوازي، وما في شي بيشتغل
بالتوازي. و"فشل مخرَج ما بيسقّط الباقي" ضلّت صحيحة: `cli.main` بتمسك
الاستثناء لكل مقاس وبتكمّل، وكود الخروج بيصير ≠ ٠.

مسار الـM مخرجات برسم واحد **مبني ومختبَر بالوحدات بس، وغير موصول
بالإنتاج** — شوف `ISSUES.md`. وصله بيوفّر فكّ ترميز المصدر وقصّه M
مرة، وهاد شغل معماري مش تنظيف.

لو طلع لاحقًا إن ريلات ٢٠+ دقيقة شغل يومي، بينعاد فتح الموضوع بقياس
مش بتخمين.

### ٨.٤ اللي انبنى فعلًا (المرحلة ٨)

`tests/test_failure.py` — ١٠ فحوص. الجدول بـ§8.2 انبنى كامل، وهاي
النقاط اللي القياس غيّر فيها شي:

**منحنى نمو الملف الجزئي.** §8.1 مقاسة على تشغيلة كبيرة (١٥.٢MB
جزئي). على سيناريو صغير (١٠٨ إطار) الملف بياخد **حالتين بس**:

```
0.178s → 0 بايت   (انفتح)
0.219s → 48       (ftyp بس)
0.560s → 191823   (كل الباقي دفعة وحدة عند الإغلاق)
```

يعني "حجم معقول ومقصوص" مش حالة نقدر نصنعها بأي مقاس. الخاصية
المفحوصة مش حجم الجزئي — هي إن **ما في ولا لحظة** بيكون فيها الاسم
النهائي موجودًا وهو ناقص.

**stderr لملف مش لأنبوب.** الأنبوب بيسع ٦٤ ك.ب؛ منقرا stdout (التقدّم)
بلا خيوط، فأنبوب stderr ما حدا بيقراه بيتعلّق أول ما يمتلي. الفحص
بيشغّل `-loglevel trace` على ٣٠ ثانية (~١٠٢ ك.ب مقيسة) **بعملية
منفصلة بمهلة**، عشان التعليق يطلع فشلًا مش جلسة عالقة. أقل من هيك ما
بيثبت شي: ١٢ ثانية بتطلّع ٤٧ ك.ب وبتخلص سالمة حتى مع أنبوب.

**ثلاث صيغ فحص انكشفت فاضية قبل ما تثبت** — كلها من نفس الجذر: الرندر
أقصر من عمر الفحص، فـ"صار" و"انقتل" بتتشابه.

| الصيغة الفاضية | ليش مرقت | الصيغة اللي بتمسك |
|---|---|---|
| قتل ffmpeg من `wait()` | `run()` بيسحب stdout لحد ما ينسدّ، يعني `wait()` بينادى **بعد** ما ffmpeg خلّص — فالقتل على جثّة | القتل من خيط جنبي بيراقب الملف الجزئي |
| `assert p.poll() is not None` بعد المقاطعة | بيتحقّق لحاله لأن الرندر بيخلص وحده — حذف `p.kill()` كان بيمرق | `assert rc < 0` — سالب = إشارة، صفر = خلّص لحاله |
| `"مش-موجود" in str(err)` | الاسم موجود بصدى الأمر (`preview(cmd[:12])`) بلا ولا حرف من stderr | `"No such file" in str(err)` — كلام ffmpeg بس |

كل واحدة منهن كانت خضرا وهي ما بتفحص شي. الطفرات اللي بتثبتهن الآن:
شيل `.part`، شيل التنظيف عند الفشل، شيل `p.kill()`، شيل `-progress`،
تحويل stderr لأنبوب — الخمسة بيفشّلوا فحصًا واحدًا على الأقل.

### ٨.٥ قراءة معلومات المصدر — **`ffprobe` مش مطلوب** (PR-1)

قرار مثبَّت. البنود السّتة:

| البند | القرار |
|---|---|
| ١ | **`ffprobe` مش مطلوب** ولا مسموح يرجع. حارس على شجرة الكود بيمنع الثابت `"ffprobe"` كوسيط أمر بـ`autoreel/` |
| ٢ | **مدة المصدر بتنقرا من `ffmpeg -i`** — سطر `Duration: HH:MM:SS.ss`، بنفس النداء اللي بيعطي الأبعاد ووجود الصوت (`cuts.probe`) |
| ٣ | **`Duration: N/A` = فشل صريح.** بترمي `RuntimeError`، وما بتخمّن — الرقم المخترَع بينتشر لخطة القص كلها |
| ٤ | **دقة `ffmpeg` أقل من `ffprobe`** — تقريب لجزء المئة. الفرق المقاس **≤ ٥ms** |
| ٥ | **ما في ضمان تطابق ميكروثاني مع `ffprobe`**، ولا بينطلب |
| ٦ | **ما منضيف demuxer خاص لقراءة `mvhd`** ولا لغيره |

**الفرق ≤٥ms قرار مقصود وموثّق، مش خلل.** `ffmpeg -i` هو المصدر
المعتمد ببيئة التشغيل، ومدّته صحيحة — بس أخشن. اللي انرفض صراحة:

| البديل | ليش انرفض |
|---|---|
| `-progress` + `out_time_us` | بيقيس نهاية أطول تيار مش مدة الحاوية — لحد **١٨٦ms** فرق، أسوأ ٣٧× |
| `-map 0 -c copy` + `-progress` | نفسها |
| `-loglevel verbose/debug/trace` | ولا واحد بيطلّع مدة أدقّ من جزء المئة |
| قراءة `mvhd` بأنفسنا | demuxer مصغّر لكل حاوية مدعومة — **مرفوض بقرار** |

`mvhd` انقرا **مرة وحدة كأداة قياس** (خارج المستودع) عشان نعرف رقم
`ffprobe` بلا `ffprobe`. ما دخل الإنتاج ولا بيدخل.

**نطاق الأثر:** `duration` بتوصل للمخرَج بتلات أماكن بس — `--no-cut`،
ولا كلمات أصلًا، وحدّ `min(duration, prev_end + pad)` اللي بيشتغل بس
لما آخر كلمة تخلص على بعد أقل من `pad` من نهاية الملف. بالمسار المعتاد
ما إلها أثر. بالتلاتة بتغيّر `frame_plan` بإطار واحد لـ٨.٢٥٪ من المدد
عند ٣٠fps — وE1 (المخرَج = الخطة) بيضل صحيحًا بالحالتين، فما في تعريف
صحة بينكسر.

**ليش انكشفت متأخرة:** كل فحص بيمرق على المسار كان بيبدّل
`probe_duration`، فالطقم كان أخضر والبرنامج ما بيقلع على بيئة بلا
`ffprobe`. الحارس `tests/test_probe.py` **ما بيبدّل ولا شي**، وبيشغّل
الـCLI على `PATH` مقصوص منه `ffprobe`.

---

## ٩. اختبارات القبول (الطلب ٩)

الشرط: تنتهي المواصفة باختبارات end-to-end حقيقية، مش وحدات بس.

### ٩.١ الثوابت الثلاثة — اختبارات ترميز حقيقي (`@pytest.mark.slow`)

| # | الاختبار | التأكيد | مصدر |
|---|---|---|---|
| E1 | `test_output_frames_equal_frame_plan` | `frames(out) == sum(frame_plan(segs,fps))` لكل مقاس | `click.mp4` |
| E2 | `test_audio_has_no_cumulative_drift` | نقرات معروفة: أقصى انزياح ≤ ٥ms، وتراكم ≤ ٥ms عبر ≥٣٠ قطعة | `click.mp4` |
| E3 | `test_every_caption_starts_on_its_frame` | كابشن ملوّن لكل مجموعة؛ أول إطار ملاحَظ == الفهرس المخطَّط، ١٠٠٪ | مولَّد |
| E4 | `test_no_intermediate_video_files` | بعد النجاح: صفر `.mp4` بالـworkdir غير المخرجات | — |
| E5 | `test_failure_leaves_no_output_file` | قتل ffmpeg -> ما في `out*.mp4`، وفي رسالة خطأ | — |
| E6 | `test_four_sizes_one_decode` | تجسّس على الأوامر: مرّة وحدة `-i <src>` | `--dry-run` |
| **E7** | `test_every_output_frame_comes_from_the_right_source_frame` | كل إطار مخرَج بيحمل رقم إطار مصدره؛ التسلسل == المتوقَّع من `frame_plan`. **صفر تكرار، صفر ناقص** | باركود |
| **E8** | `test_zoom_matches_cycle_on_every_frame` | معامل الزوم المقاس == `zoom_cycle[i % len]` بكل إطار، وبيتبدّل عند حدّ المقطع بالضبط | شبكة |

**E2 لازم يكسر على المعمارية الحالية.** لو مرق على الاتنين فهو مش
اختبار. القيمة المرجعية: الحالية بتعطي +١٦٤ms عند ٣٠ قطعة.

> **E7 مش ترفًا، وهاد الدرس الأهم من المرحلة ٠.**
> الجذع اللي كان بالمواصفة بيمرق من E1 (العدد ٣٣٦/٣٣٦ ✅) وهو بيسقّط
> إطارًا وبيكرّر تاني. الإسقاط والتكرار **بيلغوا بعض بالعدّ**، فأي
> اختبار بيعدّ بس أعمى عنهن بالتعريف. E7 هو الاختبار الوحيد اللي
> بيمسك لغم ٤، ولازم ينكتب **قبل** الجذع الجديد.
>
> نفس المنطق اللي خلّى CR-4 يعيش لهلأ: ما كان في اختبار بيقارن المخرَج
> بالمخطط. هون الدرجة الجاية: **مش كفاية تقارن العدد، لازم تقارن
> الهوية.**

### ٩.٢ ما بيلزمه من بنية تحتية

* **مولّد مصدر اختبار** (`tests/mksrc.py`): نقرات ٢ms بأزمنة معروفة +
  **شبكة خطوط** (لقياس الزوم) + **باركود رقم الإطار** (لهوية الإطار).
  الثلاثة بمصدر واحد. موجود بـ`experiments/redesign/`.
* **قارئ الباركود وقارئ الزوم** مع اختبار أرضية لكل واحد:
  الزوم قِسنا أرضيته ٠.٠٠٠٧، والباركود ٤٠/٤٠ على المصدر نفسه.
* **كاشف نقرات متحقَّق منه** (`tests/clicks.py`): بيرجّع زمن **قمة** كل
  نقرة. **لازم يكون عنده اختبار على المصدر نفسه** يثبت أرضية القياس
  (قِسناها ≤٢ms) — بدونها ما بنعرف نميّز الانزياح عن ضجيج الأداة.
  > هالبند مش زيادة. بهالجلسة أداة القياس طلعت غلط **خمس مرات**
  > (عتبة فوق قمة الإشارة، ألوان بتتكرّر كل ٢٠٠، مقارنة بكسل ما بتحمّل
  > فجوة الرسم، وغيرها). أداة قياس بلا اختبار = نتيجة بلا معنى.
* **ألوان فريدة** بمولّد كابشن الاختبار مع `assert len(set(COLORS)) == n`.

### ٩.٥ CR-4 وCR-5 معايير قبول، مش تصليحات مؤجّلة

قرار المرحلة ٣: ما بينتصلّحوا مستقلّين. الاتنين بينتهوا معماريًا
(§6.3 و`ISSUES.md`)، وتصليحهن هلأ = منطق مؤقت بينشال بعد مرحلتين
وخسارة الدليل إن إعادة التصميم هي اللي أصلحتهن.

**اللي انقاس على الخط الأساسي (٨ مقاطع، ١٤٤ إطار مخطَّط):**

| البند | الخط الأساسي | مطلوب بعد المرحلة ٥ |
|---|---|---|
| E1 عدد الإطارات | ١٤٥ (فرق **+1**) | == المخطط بالضبط |
| E7 تكرار | **٩ إطارات مكررة**، أول إطار بكل مقطع | صفر |
| E7 محتوى المقطع | ١٧ إطارًا مميّزًا من ١٨ | ١٨ من ١٨ |
| E2 انزياح الصوت | ٢١.٣ms ← **١٥٢.٠ms** (~١٨.٧ms/قطعة) | ≤ ٥ms بلا تراكم |
| E3 الكابشن | **١٨ من ٢٤** مزحلق إطارًا | صفر |
| E8 الزوم | ✅ مطابق | ✅ يضل مطابقًا |

**ممنوع لتحقيق هالجدول:** `xfail`، توسيع عتبة، تغيير السيناريو، أو
تعديل الفحص. لو لزم واحد منهن، وقّف واسأل.

الحارس `test_the_baseline_really_is_broken_in_the_ways_we_claim` بيثبّت
**بصمة** كل خلل مش وجوده بس: فرق `+1` بالضبط، والتكرار عند أول إطار
بالضبط. فلو تغيّر شي بطريقة بتحرّك البصمة بدل ما تلغيها، الحارس بينكسر
وبيلزم قرار — مش تجاهل.

### ٩.٣ اختبارات سريعة (بلا ترميز، بتضل بالمجموعة العادية)

| # | الاختبار |
|---|---|
| U1 | تعبير `select` بيغطي `Σ n_i` إطار بالضبط، بلا تراكب ولا فجوة |
| U2 | حدود `atrim` بتعطي عدد عيّنات صحيح (`SR % fps == 0`) |
| U3 | `caption_frames` بتغطّي `[0, Σn_i)` بلا فجوة ولا تراكب |
| U4 | التسلسل بيولّد وصلة لكل إطار، وكل وحدة بتأشّر على الـPNG الصح |
| U5 | `--dry-run` بيطبع أمر واحد فيه M مخرَج، وفيه `fps` **بعد** `setpts` |
| U6 | fps كسري (29.97) بيرفع خطأ واضح |
| U7 | `-filter_complex_script` مستعمل، مش `-filter_complex` بالسطر |

### ٩.٤ حماية عدم الانحدار

* **الصور المرجعية بتضل متطابقة بايت-ببايت.** الرسم ما بيتغيّر.
  لو تغيّرت، الإعادة لمست شي ما كان لازم تلمسه.
* `tests/test_config_wiring.py` بينضاف إله صفوف المفاتيح الجديدة
  (`--max-parallel-outputs` وأي مفتاح إعدادات جديد) — قاعدة CLAUDE.md.
* **اختبار مطافرة موجَّه** على §2.3: خمس طفرات، كل وحدة لازم تفشّل
  اختبارًا محدّدًا. لو مرقت أي وحدة، الاختبار المعني ما إله أسنان.

  | الطفرة | لازم يفشل |
  |---|---|
  | `fps=FPS` التانية -> no-op | E1 (٢٥fps) |
  | `settb=1/FPS,setpts=N` -> `setpts=N/FPS/TB` | **E7 وحده** (E1 بيمرق!) |
  | `atrim` -> `aselect` | E2 |
  | شيل `asplit` | E6 (فشل تشغيل) |
  | ثبّت الزوم على `zoom_cycle[0]` | E8 |

---

## ١٠. حدود المواصفة (شو **مش** داخل)

| البند | الحالة |
|---|---|
| `motion` / الزوم لكل مقطع | ✅ **صار محلولًا ومقاسًا** — §11 |
| fps كسري (29.97/59.94) | خارج النطاق، بيرفع خطأ |
| الاستئناف بعد الفشل | مرفوض بقرار مقاس (§8.3) |
| `parse_silencedetect` | كود ميت، بينشال بشكل منفصل |
| EC-2 (رموز تعبيرية tofu) | ما إلها علاقة، بتضل مفتوحة |
| EC-3 (`group_words` بتعبر الجمل) | ما إلها علاقة، بتضل مفتوحة |
| موسيقى خلفية / b-roll | ما انبنت أصلًا |

---

## ١١. المخاطر

### ~~مخاطرة ١ (حمرا): الزوم لكل مقطع~~ — ✅ **محلولة ومقاسة** (المرحلة ٠)

الحل: `scale` بتعبير على **فهرس إطار المخرَج** `n`، ثم `crop` ثابت W×H.
نفس فلتر الإنتاج حرفيًا — بس أبعاد `scale` صارت تعبير بدل ثوابت.

```
[zn] scale=w='Σ sw_i·between(n,a_i,b_i)':
           h='Σ sh_i·between(n,a_i,b_i)':
           force_original_aspect_ratio=increase:eval=frame,
     crop=W:H:x='Σ x_i·between(n,a_i,b_i)':y='Σ y_i·between(n,a_i,b_i)',
     setsar=1
```

* `a_i = Σ_{j<i} n_j` و`b_i = a_i + n_i − 1` — حدود المقطع **بالمخرَج**،
  فهارس صحيحة من خطة الإطارات. آخر مقطع بيمتد لرقم كبير كأمان.
* `sw_i, sh_i, x_i, y_i` **ثوابت محسوبة بـPython** بنفس معادلة
  `render.segment_filter` بالضبط. ما في حساب هندسي جوا ffmpeg.
* التعبير **مجموع مسطّح** مش `if` متداخلة: بالضبط حدّ واحد بيشتغل لكل
  `n`، فالمجموع = القيمة. مسطّح يعني بلا عمق تداخل عند ٣٠٠ مقطع.
* `eval=frame` على `scale` **موجود** بهالبناء. `crop` ما عنده `eval` —
  فالخيار أ٢ (لوحة + crop متغيّر) **مستحيل هون**، وما إلنا حاجة إله:
  أ١ أفضل أصلًا لأنه **إعادة تشكيل وحدة** زي اليوم، مش تنتين.
* `crop` بتقيّم `x`/`y` لكل إطار افتراضيًا — فما بدها `eval`.

**ما تغيّر بالجودة: ولا شي.** أ١ بينفّذ نفس عمليتي `scale`+`crop` اللي
بينفّذهن الإنتاج اليوم، بنفس الأبعاد والمراسي.

#### القياس (كل إطار متحقَّق منه، مش عيّنة)

مصدر بشبكة خطوط (لقياس معامل الزوم) + باركود لرقم الإطار (لكشف
الإسقاط/التكرار). ٨ مقاطع، ٣٣٦ إطار، `zoom_cycle=[1.0,1.1,1.04,1.14]`،
`pan_px=26`:

| الفحص | الخط الأساسي | أ١ |
|---|---|---|
| عدد الإطارات | 336/336 ✅ | 336/336 ✅ |
| إطار المصدر الصح بكل إطار | 336/336 ✅ | 336/336 ✅ |
| مكرر / ناقص | 0 / 0 ✅ | 0 / 0 ✅ |
| الزوم مطابق بكل إطار | 336/336 ✅ | 336/336 ✅ |
| الزوم بيتبدّل عند الحدّ بالضبط | 7/7 ✅ | 7/7 ✅ |

أسوأ خطأ بقياس الزوم = ٠.٠٠٠٥، وأرضية الأداة ٠.٠٠٠٧ — يعني الفرق بين
١.٠٠ و١.٠٤ (٠.٠٤) أوسع من الخطأ بـ٥٧ مرة.

#### ١١.١ تصحيح المرحلة ٥: المرساة لازم تكون أرقامًا

المواصفة (و§11 فوق) كانت بتقول إن `crop` بتضل تستعمل تعابير ffmpeg
`(iw-W)/2` و`(ih-H)*bias` زي `segment_filter`، بحجّة إن `crop` بتقيّم
`x`/`y` لكل إطار.

**غلط.** `crop` فعلًا بتقيّم `x`/`y` لكل إطار، بس `iw`/`ih` جواتهن
**بتتقيّدوا وقت ضبط الوصلة** وما بيتتبّعوا مقاس مدخَل بيتغيّر لكل إطار.

القياس: تيار واحد فيه زوم ١.٠ (مدخَل ٥٤٠×٩٦٠) ثم ١.١٤ (مدخَل
٦١٥×١٠٩٤). الإطار الناتج من `x='(iw-540)/2'` **يختلف** عن الناتج من
الرقم الصحيح ٣٧ — يعني `iw` ضلّت ٥٤٠.

**كيف انكشف:** E7. مقاطع الزوم ١.١٤ طلعت رقعة هويتها مزحلقة ٦٧px
عموديًا (`(ih-960)*0.5` صارت صفر بدل ٦٧)، فالقارئ رجّع `None` لـ٣٦
إطار. عدد الإطارات كان **صح** (١٤٤/١٤٤) والزوم **صح** — الخلل كان
بمكان القصّ وبس.

**ليش انخدعت:** تحقّقت بالمرحلة ٤ من إن `crop` بتشوف `iw` المتغيّر…
بقياس **معامل الزوم** (تباعد الخطوط). بس معامل الزوم بيحدّده `scale`
مش `crop`. قِست الشي الغلط: الفحص أكّد إن `scale` بتتغيّر لكل إطار،
وما قال ولا شي عن مرساة `crop`.

**الحل:** `graph.scaled_dims(src_w, src_h, sw, sh)` بتحسب أبعاد
`increase` بايثون (`round(src·max(sw/src_w, sh/src_h))` — مقاسة إنها
بتطابق ffmpeg على ٤ حالات منها نسب مختلفة)، والمرساة بتتحوّل لتعبير
`piecewise` بأرقام صحيحة. وحارس دائم:
`test_size_chain_never_anchors_with_iw_or_ih`.

**الأثر على المصدر:** المسار صار بده أبعاد المصدر. `cli.py` بتقراها
**مرة وحدة** (`render.probe_size`) زي `probe_duration` بالضبط.

### التوسّع (بلا `split=K` أبدًا)

| مقاطع | مخرجات | إطارات | ذروة الذاكرة | طول الرسم |
|---|---|---|---|---|
| 30 | 1 / 2 / 4 | 1080 ✅ | 103 / 132 / 202 MiB | 3.8 / 6.9 / 13 KB |
| 120 | 1 / 2 / 4 | 4320 ✅ | 97 / 134 / 208 MiB | 15 / 28 / 54 KB |
| 300 | 1 / 2 / 4 | 10800 ✅ | 99 / 136 / 214 MiB | 39 / 71 / **135 KB** |

الذاكرة **مستقلة تمامًا عن عدد المقاطع**، وبتتبع عدد المرمِّزات بس —
نفس خاصية §3.4 قبل ما ندخل الزوم. الرسم بيوصل ١٣٥ KB، فـ
`-filter_complex_script` (§3.5) صارت **شرطًا** مش تفضيلًا.

#### التكامل مع الكابشن والصوت

الخمس ثوابت بنفس التشغيلة:

| | ١٠ مقاطع · ٦٠ كابشن | ٣٠ مقطع · ١٥٠ كابشن |
|---|---|---|
| عدد الإطارات | 120/120 ✅ | 360/360 ✅ |
| إطار المصدر الصح | 120/120 ✅ | 360/360 ✅ |
| الزوم مطابق | 120/120 ✅ | 360/360 ✅ |
| حدود الزوم | 9/9 ✅ | 29/29 ✅ |
| الكابشن على إطاره | 60/60 ✅ | 150/150 ✅ |
| الصوت (أقصى · تراكم) | 1.98ms · +1.98ms ✅ | 1.98ms · +1.98ms ✅ |

**الزوم ما كسر توقيت الكابشن ولا فهرسته** — متوقَّع نظريًا (الزوم ما
بيلمس الطوابع الزمنية) و**مقاس** عمليًا.

#### المقارنة النهائية مع الخط الأساسي (الزوم شغّال بالاتنين)

٦٠ مقطع · ٢٤٠ كابشن · ٤ مقاسات · ٢١٦٠ إطار (٧٢s) · `veryfast crf=20`:

| | الخط الأساسي | أ١ |
|---|---|---|
| زمن الجدار | **٣٦٦.٨s** | **١١٤.٠s** (٣.٢×) |
| ترميزات فيديو | ٢٥٦ | **٤** |
| قرص وسيط | ٤٨٢.١ MB | **٠** |
| ذروة الذاكرة | ٩١٧ MiB | ١٥٤٥ MiB |
| عدد الإطارات | 2161 ❌ (CR-4) | **2160 ✅** |

والذاكرة مقابل عدد المخرجات (نفس الحمل):

| مخرجات | زمن | ذروة الذاكرة |
|---|---|---|
| ١ | ٤١.١s | **٥٣٧ MiB** |
| ٢ | ٦٤.٥s | **٨٠٥ MiB** |
| ٤ | ١١٤.٠s | ١٥٤٥ MiB |

الزوم بيرفع الذروة (١٣١٠ ← ١٥٤٥ MiB عند ٤ مخرجات) لأن `scale` بيكبّر
قبل القصّ. سقف `--max-parallel-outputs 2` (§7.2) بيضلّ صالح: ٨٠٥ MiB.

### مخاطرة ٢ (صفرا): الوصلات الرمزية على أندرويد

بعض تخزين أندرويد المشترك ما بيدعم `symlink`. المخفّف: كشف عند التشغيل
-> نسخ حقيقي مع تحذير حجم -> وإلا الرجوع لسلسلة `overlay` على دفعات.
لازم يتفحص على جهاز حقيقي.

### مخاطرة ٣ (صفرا): الذاكرة بـ٤ مخرجات

١٣١٠ MiB. المخفّف موجود ومقاس (`--max-parallel-outputs 2` -> ٦٨٩ MiB)،
بس الافتراضي لازم يكون محافظ.

### مخاطرة ٤ (خضرا): طول تعبير `select`

٣٣ KB عند ٣٠٠ مقطع. `-filter_complex_script` بيحلّها ومقاسة إنها
بتشتغل.

---

## ١٢. ملحق: التجارب والأرقام

| السكربت | السؤال | النتيجة |
|---|---|---|
| `e1` | هل الرسم المفترَض بيزبط؟ | ❌ ١٨٦/٢٢٣ — بداية كل شي |
| `e2` | trim ولا concat ولا المرمِّز؟ | `-ss/-frames:v` = ٢٢٣ ✅ · `-fps_mode passthrough` = ٢٢٢ |
| `e3` | أي صياغة بتعطي المخطط؟ | ٣ من ٦ ✅ |
| `e4` | وين بينضيع معدّل الإطارات؟ | **`setpts`** |
| `e5` | قياس أول للانزياح | ❌ الكاشف ما لقى نبضات (عتبة فوق القمة) |
| `mkclick`+`det` | مصدر وكاشف متحقَّق منهن | أرضية ٢ms |
| `e6` | انزياح الصوت: حالية vs مقترحة | +١٦٤.٦٥ms ← +١.٩٨ms |
| `e7` | هل الانزياح خطّي؟ | نعم: ٥.٣٧–٥.٨٣ ms/قطعة عند K=5..60 |
| `e8` | ٤ مخرجات برسم واحد | ❌ OOM ١٣.٥ GiB · وكشف قيد `asplit` |
| `e9` | ٣ صياغات كابشن أرخص | ✅ demux ٢٠٥٥ MiB · ❌ loop+concat OOM |
| `e10` | select بدل split | ذاكرة ٢٣٢١ ← ٢١٠ MiB · لكن ٣٠١/٣٦٠ (لغم ١) |
| `e11` | demux vs chain مع fps مصحّحة | demux ثابتة · chain بتنفخ |
| `e12` | هجين: select + atrim | ✅ الثلاثة سوا |
| `e13` | التوسّع: مقاطع × مخرجات | ذاكرة مستقلة عن عدد المقاطع |
| `e14` | الكابشن على إطاره؟ | ❌ ٥/٢٥ مزحلقين |
| `e14b` | ليش؟ | **قاعدة زمن concat demuxer = ١/٢٥** |
| `e15` | تسلسل مفهرس بالإطار | ✅ ١٢٠٠/١٢٠٠ · ذاكرة ٨٢ MiB |
| `e16` | تكامل الثوابت الثلاثة | ✅✅✅ |
| `e17` | دلالات الفشل | mp4 بلا moov · `-progress` بيشتغل |
| `e18` | مقارنة نهائية | ١٤٩s ← ٢٣s · ٢٥٦ ← ٤ ترميز · **وكشف CR-4** |
| `z0` | أداة قياس الزوم | ❌ عتبة كتل مرتين · ✅ مرشّح مطابَق، أرضية ٠.٠٠٠٧ |
| `z1` | أ١ مقابل الخط الأساسي | زوم مطابق بالضبط، ٣٤٨/٣٤٨ |
| `z2` | مصدر بباركود رقم الإطار | القارئ ٤٠/٤٠ على المصدر |
| `z3` | فحص كل إطار | ❌ كشف إسقاط+تكرار بالجذع المعتمَد |
| `z4` | أي فلتر بالجذع السبب؟ | **الـ`fps` الأخيرة مع `setpts=N/FPS/TB`** |
| `z5` | `settb`+`setpts=N` | ✅ صفر غلط · وpassthrough بيعلن 30.09fps |
| `z6` | التوسّع ٣×٣ | ٩/٩ ✅ · ذاكرة مستقلة عن K |
| `z7` | تكامل زوم+كابشن+صوت | الخمس ثوابت ✅✅✅✅✅ |
| `z8` | مقارنة نهائية بالزوم | ٣٦٧s ← ١١٤s · ٢٥٦ ← ٤ ترميز |

### أخطاء أدوات القياس بهالجولة (للتسجيل)

| # | الخلل | كيف انكشف |
|---|---|---|
| ١ | عتبة الكاشف ٠.٢٥ وقمة الإشارة ٠.١٣٧ | ٠ نبضات من ٢٠ |
| ٢ | المقاطع بتبلّش بعد النقرة | "المتوقع −٠.٠٣s" |
| ٣ | ألوان الكابشن بتتكرّر كل ٢٠٠ | ٤٠٠ "مزحلق" فجأة عند ٦٠٠ |
| ٤ | `ru_maxrss` تراكمية فبتتلوّث بين المعماريتين | فصلناهن لعمليتين |
| ٥ | `frames()` بيستدعي ffmpeg فبيلوّث قياس الذاكرة | نقرا الذروة قبله |
| ٦ | صف القياس وقع على **خط شبكة أفقي** فطلع أبيض كامل | ولا خط انكشف |
| ٧ | كشف الكتل بعتبة: الخط بينشقّ لكتلتين مع التنعيم | فروق [26.0, 3.9, 30.1] |
| ٨ | ٣٠٠ مقطع على مصدر ١٨s -> مدايات `select` متداخلة | ٥٣٩/٦٠٠ "فشل" وهمي |
| ٩ | استخراج PNG بـcfr بيضيف إطار | ٣٣٧ بدل ٣٣٦ |
| ١٠ | `-fps_mode passthrough` **قبل** `-i` (خيار مخرَج) | ٠ صور، و"٠/٠ ✅" نجاح فاضي |

**كل واحد من هدول كان رح يعطي رقم غلط بالمواصفة.** وهاد سبب §9.2.

رقم ١٠ يستاهل وقفة: طبع **`0/0 ✅`** — اختبار نجح وهو ما فحص ولا شي.
ضفنا `assert len(files) == TOTAL_F` قبل أي مقارنة. أي فحص ممكن يمرق
على مجموعة فاضية لازمه حارس، بالتجارب وباختبارات القبول سوا.

---

## ١٣. خطة التنفيذ (بعد الموافقة)

بالترتيب، وقفة بعد كل مرحلة:

| # | المرحلة | المخرَج | معيار التوقف |
|---|---|---|---|
| ٠ | ~~تجربة الزوم~~ | ✅ **خلصت** — §11 | نجحت، وكشفت لغم ٤ و٥ |
| ١ | بنية اختبار القياس | `mksrc.py`, `clicks.py`, `barcode.py`, `gridzoom.py` + اختبار أرضية لكل واحد | الأرضيات موثّقة (٥ms · ٠.٠٠٠٧ · ٤٠/٤٠) |
| ٢ | E1/E2/E3/E7/E8 **على الكود الحالي** | ٥ اختبارات، بعضها فاشل | **E2 لازم تفشل** · E1 لازم تفشل (CR-4) — وإلا مش اختبارات |
| ٣ | **تثبيت الخط الأساسي** (مش إصلاح) | CR-4 وCR-5 مسجّلين ببصمتهم؛ E1/E7 بيضلّوا فاشلين | الحرّاس بيثبّتوا `+1` بالضبط والتكرار عند أول إطار بالضبط |
| ٤ | باني الرسم (نقي، بلا تشغيل) | `autoreel/graph.py` + U1..U7 + حارس انفصال مدايات `select` | ٦٢ فحصًا بلا ffmpeg؛ الوحدة **مش موصولة** بالإنتاج |
| ٥ | مسار الفيديو + الزوم | ✅ **خلصت** — `fps,select,settb,setpts=N,fps` ثم `scale(eval=frame)`+`crop` بمرساة **رقمية** | E1 ✅ · E7 ✅ · E8 ✅ · E2/E3 لسا فاشلين كما هو مخطَّط |
| ٦ | مسار الصوت | ✅ **خلصت** — `atrim` بفهرس العيّنة + `concat` + AAC واحد | **E2 ✅** ١٥٢ms ← ١.٩٨ms · وسقالة المرحلة ٥ انشالت |
| ٧ | مسار الكابشن | ✅ **خلصت** — تسلسل مفهرس **موحّد المقاس** + overlay واحد | E3 ✅ · ١٣/١٣ صورة مرجعية متطابقة بايت-ببايت |
| ٨ | دلالات الفشل + التقدّم | ✅ **خلصت** — `.part`+`os.replace` · `-progress pipe:1` · stderr لملف · قتل عند المقاطعة | ١٠ فحوص بـ`tests/test_failure.py` · E1–E8 ما تغيّروا |
| ٩ | تنظيف | شيل الكود الميت المتبقّي | الاختبارات كلها تمرق |

المرحلة ٢ قبل ٥–٧ عمدًا: **الاختبار اللي ما شفناه بيفشل مش اختبار.**

**`--max-parallel-outputs` (E6) انحذفت من الخطة، ما انتأجّلت.** كانت
موجودة لأن كل مخرَج كان **حزمة** عمليات ffmpeg (قصّ + زوم + دفعات حرق)،
فتشغيل مقاسين سوا كان ممكنًا وبده سقف ذاكرة. هلأ كل مقاس تشغيلة وحدة،
والمقاسات بتنعمل **بالتتابع** بحلقة `cli.main` — وسقف التوازي ما إله
معنى لما ما في توازي. التفصيل والتصحيح بـ§8.3.

الترقيم بعدها انزاح: ٨ صارت دلالات الفشل و٩ التنظيف.

---

## ١٤. ملخّص القرارات للمراجعة

| # | القرار | برهانه |
|---|---|---|
| ١ | خطة الإطارات هي مصدر الحقيقة، والزمن مشتقّ | §1 |
| ٢ | fps صحيح مطلوب؛ 29.97 مرفوض بخطأ | §1.1 مقاس |
| ٣ | `select`+**`settb`+`setpts=N`**+`fps` للفيديو | §3.1 · لغم ١ و٤ |
| ٤ | `atrim` (عيّنة) مش `aselect` (إطار ترميز) للصوت | §4.3 · ٣٥ms ← ٢ms |
| ٤ب | `atrim=start_sample/end_sample` مش `start=/end=` بالثواني | §4.6 · ضبط بالبناء، **مش** فرقًا مقاسًا |
| ٥ | ترميز AAC واحد لكل مخرَج نهائي، بلا وسيط | §4.2 · ١٦٤ms ← ٢ms |
| ٦ | تسلسل صور مفهرس بالإطار للكابشن | §5.3 · concat demuxer عالق على ١/٢٥ |
| ٧ | `-filter_complex_script` ملف | §3.5 |
| ٨ | ~~`--max-parallel-outputs` افتراضي ٢~~ **سقطت** — المقاسات بالتتابع، فما في توازي ينتسقّف | §8.3 · تصحيح |
| ٩ | كتابة ذرّية `.part`، لا استئناف | §8.2/§8.3 |
| ١٠ | CR-4 (+١ إطار) ينتسجّل ويتصلّح مستقل | §6.3 |
| ١١ | `captions.py` منطق الرسم ما بينلمس | §5.4 |
| ١٢ | الزوم لكل مقطع بـ`scale(eval=frame)` وتعبير مسطّح على `n` | §11 · **مقاس، كل إطار** |
| ١٣ | `-fps_mode passthrough` مرفوض (بيعلن 30.09fps) | §2.3 لغم ٤ |
| ١٤ | مدايات `select` لازم تكون منفصلة — `assert` صريحة | §2.3 لغم ٥ |
| ١٥ | E7 (هوية كل إطار) شرط، مش E1 لحاله | §9.1 |
| ١٦ | مرساة القصّ **أرقام** مش `(iw-W)/2` | §11.1 · `crop.iw` ما بتتتبّع مقاسًا متغيّرًا |
| ١٧ | تسلسل الكابشن **موحّد المقاس** | §2.3 لغم ٦ · فرق بكسل = ٧٣/١٤٤ |
| ١٨ | **`ffprobe` مش مطلوب** — كل شي من `ffmpeg -i` واحد | §8.5 · PR-1 |
| ١٩ | `Duration: N/A` = فشل صريح بلا تخمين | §8.5 |
| ٢٠ | دقة المدة جزء المئة (**≤٥ms**) — قرار مقصود، ولا ضمان تطابق ميكروثاني | §8.5 · مقاس |
| ٢١ | **ولا demuxer خاص** (`mvhd` وغيره) | §8.5 · مرفوض بقرار |

---

*هالسطر كان صحيحًا وقت كتابة المواصفة (جولة التصميم، بلا كود). المراحل
٥–٩ وPR-1 نفّذت بعدها — الحالة الحيّة بـ`ISSUES.md`.*
