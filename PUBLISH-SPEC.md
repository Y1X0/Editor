# مواصفة التسليم: auto-reel ← دلو ← rsdf ← إنستغرام

> **مواصفة، مش تنفيذ.** ولا سطر كود انكتب لهالمسار لحد الآن. هدف الملف
> إن كل قرار يكون مكتوبًا قبل ما ينكتب، وإن الفشل يكون معرَّفًا قبل ما
> يصير.

**القرار المعتمد:** الدلو عمودًا للنقل، ونداء API مباشر إشارةً.

---

## ٠ · الحقيقة اللي بتحكم التصميم كله

`publishing/providers/instagram_provider.py` **ما بيرفع بايتات**:

```
POST /{ig-user-id}/media   {"video_url": …, "media_type": "REELS"}
```

سيرفرات Meta هي اللي بتنزّل الفيديو من الرابط. وrsdf بيفرض هاد أصلًا —
`services/publishing_service.py` بيرمي `AssetNotPubliclyHosted` لأي
`asset_url` ما بتبلّش بـ`http`، وتعليقه بيقول: «this is the one
enforcement point every publish path goes through».

**النتيجة:** الدلو مش وسيطًا انضاف للمعمارية. هو **صيغة المدخل الأصلية**
للنشر. أي تصميم بينقل بايتات بين المشروعين بيكون عم يبني الطريق الطويل
لنفس المكان.

### وحقيقتان مقيستان (مش مفترضتان)

| الادعاء | القياس |
|---|---|
| ffmpeg على Render | `imageio-ffmpeg==0.6.0` بيجيب `ffmpeg 7.0.2-static` من johnvansickle — **نفس البناء والنسخة** اللي المشروع كله معايَر عليها |
| raqm بصورة rsdf | wheel تبع `Pillow 12.3.0` بيجيب raqm جاهزًا (harfbuzz 14.2.1 · fribidi 1.0.13) — `libraqm0` من apt مش لازم |
| Whisper محلي على Render Free | ❌ ‏512MB و‏0.1 CPU. وrsdf حالّها من قبل بـ`TRANSCRIPTION_PROVIDER=groq` |

**فالترميز ما بيصير على Render لا لأن ffmpeg ناقص — هو موجود — بل لأن
٠.١ CPU وطفي-بعد-خمول ما بيحمّلوا شغلًا محجوزله ٩٠ دقيقة على عدّاد
GitHub.** الترميز بيضل مكان ما هو معايَر.

---

## ١ · عقد الأصل

### المفتاح

```
autoreel/<YYYY-MM-DD>/<run_id>/<basename>.<size>.mp4
```

`run_id` هو `GITHUB_RUN_ID`. **ولا مرة بينداس مفتاح موجود** — كل تشغيلة
مفتاح جديد. ليش: الدلو سجل، ودَوس المفتاح بيخلّي رابطًا منشورًا يشاور
على فيديو تاني بصمت.

الرابط العام = `MEDIA_BACKUP_PUBLIC_BASE_URL` + `/` + المفتاح.

> **حدّ صلب:** `videos.asset_url` عمود `String(500)`. الرابط الناتج
> ~١٥٠ محرفًا، فالهامش واسع — بس الفحص قبل الإرسال إلزامي، لأن قصّ
> السلسلة بقاعدة البيانات بيعطي رابطًا مكسورًا **بلا خطأ**.

### الرأسيات عند الرفع

| الرأسية | القيمة | ليش إلزامية |
|---|---|---|
| `Content-Type` | `video/mp4` | Meta بترفض `application/octet-stream` — وهاد افتراض boto3 لما ما ينتحدّد |
| `Cache-Control` | `public, max-age=31536000, immutable` | المفتاح ما بينداس، فالكائن ثابت للأبد |

### الملف المرافق

جنب كل `.mp4` بينرفع `<نفس المفتاح>.json`:

| الحقل | ليش |
|---|---|
| `sha256` | الوحيد اللي بيربط الرابط بالبايتات اللي انترمّزت فعلًا |
| `bytes` | بينتقارن مع `Content-Length` وقت التحقق |
| `duration_s` | rsdf عنده `videos.duration_s` وبيستنى قيمة |
| `ffmpeg_version` | **قاعدة المستودع:** رقم بلا نسخة ffmpeg رقم ناقص |
| `commit`, `run_id`, `run_url` | من الأصل للتشغيلة اللي عملته |
| `source_basename`, `size`, `sizes_rendered` | |
| `transcript_source` | `whisper:small:ar` أو `srt:<اسم الملف>` |
| `config_sha256` | `config.json` بيحكم التأطير والمؤثرات — الأصل بلا كونفيغه غير قابل للتفسير |

**ما بينرفع:** ولا مفتاح، ولا توكن، ولا مسار محلي، ولا اسم الملف الأصلي
كاملًا لو فيه معلومة شخصية — `basename` بس.

### وشو بيستنى rsdf يلاقي

رابط `https://` بيرجّع `200` و`video/mp4` لطلب **مجهول الهوية** — بلا
توكن وبلا رأسية. سيرفرات Meta ما عندها اعتمادك.

---

## ٢ · المصادقة

| السرّ | وين | نطاقه |
|---|---|---|
| `MEDIA_S3_ACCESS_KEY_ID` | أسرار Editor | كتابة على `content-factory-media` تحت سابقة `autoreel/` **بس** |
| `MEDIA_S3_SECRET_ACCESS_KEY` | أسرار Editor | ↑ |
| `RSDF_AUTH_CLIENT_ID` | أسرار Editor | نفس `AUTH_CLIENT_ID` بRender |
| `RSDF_AUTH_CLIENT_SECRET` | أسرار Editor | نفس `AUTH_CLIENT_SECRET` بRender |
| `RSDF_BASE_URL` | متغيّر Editor (مش سرّ) | أصل الخدمة على Render |

**مفتاح S3 منفصل عن اللي بRender، مش نسخة منه.** ليش: Editor بيلزمه
كتابة تحت سابقة وحدة؛ وrsdf بيلزمه قراءة وكتابة أوسع. مفتاح واحد
مشترك بيخلّي تسريب من أي طرف يوصل للتنين، وتدويره بيوقّف الطرفين سوا.

**JWT:** `POST {RSDF_BASE_URL}/auth/token` بالـclient id/secret →
توكن قصير العمر. بينطلب **مرة وحدة** بالتشغيلة وبينمرَّر بالذاكرة.
ولا مرة بينطبع، ولا بينكتب بملف، ولا بيروح لـ`GITHUB_STEP_SUMMARY`.

> `set -x` بأي خطوة بتلمس التوكن = تسريب بالسجل. ممنوع صراحةً.

---

## ٣ · الفشل — ولا خطوة بتكمل بصمت

القاعدة اللي بيقوم عليها القسم، من `CLAUDE.md`:
**«وأخطر من الخلل: المخرَج اللي بيكذب على قارئه.»**

فالقاعدة التنفيذية: **ولا `✅` بينطبع إلا بعد تأكيد نفّذ فحصًا فعليًا.**
`set -euo pipefail` على كل خطوة، وولا `|| true` على خطوة نتيجتها بتنبنى
عليها.

| # | الخطوة | الفشل | الكشف | النتيجة |
|---|---|---|---|---|
| ١ | الترميز | `cli` رجّع بكود ≠ `0` | كود الخروج | وقوف. ولا رفع |
| ٢ | المخرَج | ملف مفقود أو < ١٠KB | `stat` | وقوف |
| ٣ | المخرَج | ما بينفتح كفيديو | `cuts.probe` بيرجّع مدة موجبة | وقوف |
| ٤ | الرفع | خطأ boto3 | استثناء | وقوف |
| ٥ | **التحقق** | الرابط مش عام | `HEAD` مجهول: `200` | وقوف |
| ٦ | **التحقق** | ملف ناقص | `Content-Length` == `bytes` | وقوف |
| ٧ | **التحقق** | نوع غلط | `Content-Type` == `video/mp4` | وقوف |
| ٨ | **التحقق** | بايتات غير اللي انترمّزت | `GET` + `sha256` مطابق | وقوف |
| ٩ | JWT | ≠`200` | كود الحالة | وقوف |
| ١٠ | التسجيل | ≠`201` | كود الحالة | وقوف |
| ١١ | النشر | ≠`200` | كود الحالة | وقوف + **طبع جسم رد Meta كاملًا** |

### الخطوات ٥–٨ هي القلب

**«الرفع نجح» مش دليل إن الرابط عام.** دلو خاص بيقبل الكتابة ويرجّع
`200` من S3، وبعدين بيرجّع `403` لأي زائر. لو ما انفحص، أول اكتشاف
للخلل بيكون **رسالة خطأ من Meta بعد النشر** — يعني بعد ما الأصل انحط
بمسار النشر وانصرف عليه ترميز.

الفحص لازم يكون **مجهول الهوية**: طلب من نفس العدّاد بأي اعتماد
بالبيئة ممكن ينجح لأسباب غلط.

### والفشل بينوصل، مش بس بينسجّل

كل وقوف بيكتب سببه بـ`GITHUB_STEP_SUMMARY` مع رقم الخطوة من الجدول
فوق. الـjob بيفشل حمرا. **ما في «نشر جزئي»** — يا كل المسار خلص، يا
الفشل ظاهر باسمه.

---

## ٤ · النسخة النائمة

Render Free بتطفي بعد خمول، وأول طلب بيوقظ الحاوية — عمليًا لحد ~٥٠
ثانية، وممكن يفوت مهلة عميل قصيرة.

### القاعدة: الإيقاظ على مدخل قراءة، مش على النشر

```
GET {RSDF_BASE_URL}/health     ← الإيقاظ
```

| الإعداد | القيمة |
|---|---|
| مهلة الطلب | ٩٠ ثانية |
| المحاولات | ٥ |
| التراجع | ٥ · ١٠ · ٢٠ · ٤٠ ثانية |
| النجاح | `200` من `/health` |
| الفشل بعد الخمسة | وقوف — الخدمة نايمة أو واقعة، والتنين مش وقت نشر |

**ليش `/health` مش النشر مباشرةً:** إعادة محاولة نداء نشر بتنشر مرتين.
`/health` قراءة صرفة، فتكراره آمن بالتعريف. ولمّا يرد، الحاوية صاحية
والنداءات اللي بعدها بمهلة ٣٠ ثانية عادية.

### ومع هيك: `idempotency_key` إلزامي

جسم النشر عند rsdf بيقبل `idempotency_key`، و`services/idempotency.py`
بيحترمه. **مش اختياريًا هون:**

```
idempotency_key = sha256(الأصل)
```

بصمة البايتات نفسها. فحتى لو انقطعت الشبكة بعد ما rsdf نشر وقبل ما
يوصلنا الرد، إعادة المحاولة بترجّع نفس صف النشر بدل ما تنشر ريل تاني.
**هاد آخر خط دفاع ضد النشر المكرر، ومش مبني على الثقة بالشبكة.**

---

## ٥ · auto-reel بيضل يشتغل لحاله

**الـworkflow مستهلك، مش مالك.** الحدّ:

- ولا سطر بينضاف لـ`autoreel/`. كل الربط بملف workflow + سكربت تحت `tools/`.
- ولا تبعية بتنضاف لـ`requirements.txt`. `boto3` و`httpx` بتنصّبوا بخطوة
  الـworkflow لحالها وما بتدخل عقد المشروع.
- ولا متغيّر بيئة من متغيّرات النشر بينقرا داخل الحزمة.

### الفحص اللي بيمنع الزحف

بينضاف لـ`tests/test_packaging.py` — موجود أصلًا:

| الفحص | بيمسك |
|---|---|
| `grep` على `autoreel/` بلا نتيجة لـ`rsdf\|boto3\|s3\|instagram\|publish` | أي تسرّب لمنطق النشر جوا الحزمة |
| `import autoreel` بلا `boto3` منصَّب | تبعية انزرعت بالسكوت |
| `python -m autoreel.cli --help` ببيئة نظيفة | ادعاء إن الأداة صارت بتلزمها إعدادات نشر |

**الفحص هو الضمانة، مش النية.** بلاه، القاعدة بتتآكل بأول PR مستعجل.

---

## ٦ · الشروط التلاتة

### ٦.١ تأكيد يدوي قبل النشر

المسار **مرحلتان بjob منفصلين**:

```
build  →  الترميز · الرفع · التحقق (٥–٨)     يمشي لحاله
                        ↓
                   ⏸ بوابة موافقة
                        ↓
publish →  الإيقاظ · JWT · التسجيل · النشر     بيستنى ضغطة
```

**الآلية:** `environment:` بGitHub مع required reviewer على job النشر.
أنضف من علم بالسكربت: بيوقف الـjob فعليًا بمستوى المنصّة، وبيسجّل مين
وافق ومتى.

**ليش المرحلتان منفصلتان:** عشان الوقوف عند الموافقة ما يرمي ٩٠ دقيقة
ترميز. الأصل بيكون مرفوعًا ومتحقَّقًا منه ورابطه ظاهر بالملخّص —
والمراجع بيقدر يفتحه ويتفرّج عليه **قبل** ما يوافق.

`--yes` بيتخطّاها لاحقًا كـinput للـworkflow، **مش قبل ما تخلص شرط
٦.٢**.

> السبب مذكور صراحةً: كل خلل بهالمشروع كان بيطلّع ناتجًا ويقول نجح. مع
> النشر، نفس الصنف من الخلل **بينشر** بدل ما يحفظ ملفًا غلط. البوابة
> موجودة لأن سجل المشروع نفسه بيطلبها.

### ٦.٢ أول خمس نشرات على حساب اختبار

- `account_id` بالـworkflow **مقيّد بقائمة**، والقائمة فيها معرّف حساب
  الاختبار بس. أي قيمة تانية = وقوف قبل أي نداء.
- كل نشرة بتنسجّل بجدول تحت — رقمها، الرابط، `external_post_id`،
  والنتيجة.
- بعد خمس نشرات ناجحة موثّقة، بينضاف الحساب الحقيقي **بPR مستقل**
  بيشيل القيد صراحةً. مش بتعديل جانبي.

| # | التاريخ | الأصل | `external_post_id` | النتيجة |
|---|---|---|---|---|
| ١ | | | | |
| ٢ | | | | |
| ٣ | | | | |
| ٤ | | | | |
| ٥ | | | | |

### ٦.٣ ‏PKG-1 بيضل مؤجّلًا

`ISSUES.md`/PKG-1 (نقل بيانات التشغيل جوا الحزمة) **ما إله داعي
بهالمسار، ومش بينفتح**.

ليش: الـworkflow بيشغّل `python -m autoreel.cli` من جذر المستودع
المستنسخ — وهاد **بالضبط** مسار التشغيل المدعوم اللي `pyproject.toml`
بيوصفه. `config.json` و`fonts/` و`assets/sfx/` كلهن بأماكنهن النسبية
الصحيحة. ولا واحد من الأسباب اللي خلّت PKG-1 لازمة لخيار «تبعية pip»
بينطبق هون.

**مسجَّل عشان ما ينفتح غلطًا لاحقًا كـ«تحسين».**

---

## ٧ · الفجوة الوحيدة بجهة rsdf — `EXT-1`

مسار النشر عند rsdf بيطلب صف `Video` بشرطين:

1. `status == APPROVED` — `publications.py` بيرجّع `409` بغيرها.
2. أصل من مصدر معروف — `models/video.py` بيوثّق إن الصف بيجي **يا** من
   `script_id` **يا** من `clip_id`، «exactly one of the two, enforced in
   the service layer».

**وريل auto-reel لا هاد ولا هاد.** مش سكربتًا كتبه وكيل، ومش مقطعًا
مقصوصًا من فيديو مصدر بقاعدة بياناتهن.

فبيلزم rsdf **مدخل ضيّق واحد** بيسجّل أصلًا خارجيًا جاهزًا: بياخد
الرابط العام والمدة و`sha256` والميتاداتا، وبينشئ صف `Video` بأصل تالت
صريح، وبيرجّع `video_id`.

> **خارج نطاق هالمواصفة.** شغل بريبو rsdf، بPR وهجرة Alembic
> مستقلين. مذكور هون لأنه **الحاجز الوحيد** بين هالتصميم وبين مسار
> شغّال — وأي جدول زمني ما بيحسبه بيكون ناقصًا.

---

## ٨ · خارج النطاق

| مش هون | ليش |
|---|---|
| جدولة أو نشر دوري | نشرة وحدة بتأكيد أولًا |
| منصّات غير إنستغرام | مزوّدو TikTok/YouTube موجودون بrsdf وما انفحصوا بهالمسار |
| مزامنة المقاييس بعد النشر | مسار قائم عند rsdf — ‏issue #10 مفتوح عليه |
| تخزين نسخة احتياطية بEditor | الدلو هو التخزين |
| ‏PKG-1 | §٦.٣ |
