# SFX-SPEC — تصميم الصوت (مؤثرات مرتبطة بأحداث الفيديو)

> **حالة الوثيقة: مواصفة للمراجعة. ولا سطر كود إنتاج تغيّر.**
>
> كل رقم بهالملف **مقاس** على **ffmpeg 7.0.2-static** بهالبيئة، مش
> مستنتَج. سكربتات القياس بـ`experiments/sfx/` وبتنعاد كما هي.
>
> **ما بتنعاد فتح معمارية الرندر.** التصميم كله بينزل **جوا** المسار
> الحالي (`REDESIGN-SPEC.md` مقفولة)، وما بيلمس `video_stem` ولا
> `size_chain` ولا مسار الكابشن ولا `frame_plan`.

---

## ٠. الخلاصة بسطرين

المؤثرات بتنحطّ **بعد `concat` وقبل `asplit=M`**، كل مؤثر
`adelay=<فهرس العيّنة>S:all=1` ثم `amix=…:normalize=0`. الفهرس مشتقّ
من **`frame_plan`** زي الكابشن بالضبط، فالتزامن **بالبناء**.

مقيس: الكلام بينحفظ **عيّنة بعيّنة** (٠ عيّنة متغيّرة)، وعدد
الإطارات وعدد العيّنات ما بيتغيّروا، وأقصى انزياح للمؤثر **٢ عيّنة =
٠.٠٤٢ms = ٠.٠٠١ إطار** بلا تراكم.

**بس في قرار منتج واحد مش محسوم — سياسة الهامش (§F).** بدونه التنفيذ
بيوقف.

---

# A. مواصفة تصميم الصوت

## A.1 وين بتدخل المؤثرات بالرسم

المسار الصوتي الحالي (`graph.audio_chain`):

```
[0:a] aresample=SR, asplit=K
      -> atrim=start_sample/end_sample لكل مقطع
      -> asetpts=PTS-STARTPTS
      -> concat=n=K:v=0:a=1  [acat]
      -> anull / asplit=M    [ao0]…[aoM]
```

**نقطة الإدخال: بين `[acat]` و`asplit=M`.**

```
[acat] ─┐
[s0]  ──┤
[s1]  ──┼─> amix=inputs=N+1:duration=first:normalize=0 ─> [amixed] ─> asplit=M
 …    ──┘
```

ليش هون بالضبط:

* **بعد `concat`** لأن أحداث المؤثرات معرَّفة على **التوقيت النهائي**
  (بعد القص): بداية كابشن، حدّ مقطع، تغيّر زوم. قبل `concat` التوقيت
  هو توقيت المصدر، وبيلزم إعادة تخطيط — نفس الفخّ اللي `remap_words`
  موجودة لأجله.
* **قبل `asplit=M`** لأن المؤثرات **مستقلة عن المقاس** تمامًا، زي
  الصوت. حطّها بعد التوزيع بيعني بناءها M مرة بلا فايدة.
* **ولا تغيير على `[0:v]` ولا على `atrim`** — الطبقة اللي تحت ما
  بتحسّ فيها.

## A.2 مصدر الحقيقة: الفهرس، مش الزمن

نفس قاعدة المشروع الأساسية. الحدث معرَّف **بفهرس إطار المخرَج**،
والتحويل للصوت **ضرب صحيح**:

```
sample_index = frame_index × (SR // fps)
```

`validate_fps` بتضمن `SR % fps == 0` أصلًا (٤٨٠٠٠/٣٠ = ١٦٠٠ عيّنة
للإطار)، فما في كسر ولا تقريب ولا تراكم.

**قياس (S1):** `adelay=NS` مضبوطة **صفر خطأ** على ٧ أهداف عدائية.
والصيغة اللي بيكتبها الإنسان طبيعيًا — ميلي صحيح — بتغلط ±٢٣ عيّنة:

| الصيغة | أقصى خطأ |
|---|---|
| `adelay=100777S` | **٠ عيّنة** |
| `adelay=2099.520833` (ميلي عشري كامل) | ٠ عيّنة |
| `adelay=2100` (ميلي صحيح) | **±٢٣ عيّنة = ±٠.٤٨ms** |

فالقرار: **`S` دايمًا**، ولا تُكتب أزمان بالميلي بمسار المؤثرات أبدًا.

## A.3 السلسلة القانونية لكل مؤثر

```
[k:a] aformat=sample_rates=48000:channel_layouts=stereo,
      volume=<كسب الحدث>,
      adelay=<فهرس العيّنة>S:all=1   [sN]
```

**التلات أجزاء كلها إلزامية، وكل واحد منهن بيغطّي فخًّا مقيسًا (S4):**

| الجزء | لو انحذف | الأثر المقاس |
|---|---|---|
| `aformat=sample_rates=…` | أصل 44.1k | المؤثر بيقع عند **٥٢٢٤٥** بدل ٤٨٠٠٠ = **+٨٨ms** |
| `all=1` | أصل ستيريو | `adelay` بتأخّر **القناة الأولى بس**، والتانية بتضل بمكانها -> المؤثر بيقع عند **٠** (خطأ −٤٨٠٠٠) |
| `aformat=channel_layouts=…` | تفاوض ضمني | الصح بيتصادف حسب شكل السرير — هشّ، مش مضمون |

**الفخّان صامتان تمامًا** — ولا تحذير ولا خطأ من ffmpeg. المؤثر بيطلع
بالمكان الغلط وبس.

السلسلة القانونية انقاست على **١٢ تركيبة** (سرير مونو/ستيريو ×
٢٢٠٥٠/٤٤١٠٠/٤٨٠٠٠ Hz × ١/٢ قناة): **٠ خطأ بكل واحدة**.

## A.4 المزج: `normalize=0` إلزامية

**هاي أخطر إعداد بالمواصفة كلها.**

`amix` الافتراضية (`normalize=1`) بتقسّم كل مدخل على عددهن. النتيجة
مقيسة (S2/S3):

| | `normalize=1` (افتراضي) | `normalize=0` |
|---|---|---|
| مستوى الكلام مع ٥ مؤثرات | **٠.٣٠×** | ١.٠٠× |
| مستوى الكلام مع ٢٠ مؤثرًا | **٠.١١×** (−١٩dB) | ١.٠٠× |
| عيّنات الكلام المتغيّرة (من ٢٨٨٠٠٠) | **٢٣٢٦٩٦** | **٠** |
| طول المخرَج | محفوظ | محفوظ |

وأسوأ من الخفوت: **تنفّس الكسب**. المؤثرات قصيرة، وكل ما مؤثر بيخلص
`amix` بتعيد توزيع الكسب على الباقي عبر `dropout_transition`. مقيس مع
٥ مؤثرات — مستوى الكلام بين المؤثرات:

```
ثانية 0: 0.0208   ثانية 3: 0.0277
ثانية 1: 0.0226   ثانية 4: 0.0312
ثانية 2: 0.0249   ثانية 5: 0.0354
```

الكلام بيعلى ويهبط لأن **مؤثرات انتهت** — ضخّ مسموع ما إله علاقة
بالمحتوى.

`normalize=0` بتلغي الاتنين: **٠ عيّنة كلام متغيّرة** برّا نوافذ
المؤثرات، وطول محفوظ بالضبط.

> **وهاي الخاصية هي اللي بتحمي E2 بنيويًا:** لو المزج ما بيلمس عيّنات
> الكلام، تزامن الصوت مع الصورة **ما بيقدر** ينكسر بالمؤثرات.

## A.5 إعادة استعمال الأصول: `asplit` مش مدخل لكل استعمال

الأصول قليلة (`whoosh`, `pop`, `impact`, `riser`, `click`)
والاستعمالات كتيرة. فتح الملف مرة وحدة و`asplit` بعدد استعمالاته
أرخص بكل المقاييس (S6):

| N | مدخل لكل مؤثر | مدخل واحد + `asplit` | الوفر |
|---|---|---|---|
| ٥٠ | ٣٤.٣M · ٠.٣٧s · ٤٤٠١B | ٢٦.٣M · ٠.٢٦s · ٢١٥٥B | ٢٣٪ ذاكرة |
| ٢٠٠ | ٩٣.٧M · ٢.٠١s · ١٧٨٤٥B | **٦٢.٢M · ١.٣١s · ٨٧٩٩B** | ٣٤٪ |
| ٥٠٠ | ٢١٧.٢M · ٨.١٩s · ٤٥٠٢٩B | **١٣٥.٥M · ٥.١٧s · ٢٢٤٨٣B** | ٣٨٪ |

فالشكل: **`-i` لكل أصل مميّز**، و`asplit=<عدد استعمالاته>` لكل واحد.

## A.6 خريطة الأحداث (deterministic، بلا AI)

| الحدث | مصدره بالكود | الصوت | ملاحظة |
|---|---|---|---|
| حدّ مقطع (قطة) | `frame_plan` التراكمي | `whoosh` | مش عند الإطار ٠ |
| تغيّر الزوم | `zoom_values` لما تتغيّر | `whoosh` خفيف | بينطبق مع حدّ المقطع عادةً |
| ظهور كابشن جديد | `caption_frames` أول إطار بالمجموعة | `pop` / `click` | الأكثر تكرارًا |
| تمييز كلمة | إطار بداية كلمة الكاريوكي | `tick` خفيف | **مطفي افتراضيًا** — كثيف جدًا |
| أول إطار بالريل | ٠ | `impact` | مرة وحدة |
| آخر مقطع | بداية آخر مقطع | `riser` | مرة وحدة |

كلهن مشتقّون من **بنى موجودة أصلًا** — ولا واحد بيحتاج تحليل محتوى
ولا نموذج. الاختيار جدول بالـconfig، والتشغيل حتمي: نفس المدخل ->
نفس المخرَج بالضبط.

## A.7 المؤثرات المتزامنة

`amix` بتجمع، فالتزامن بيشتغل لحاله — بس بيضاعف الذروة. القاعدة:

* **حدّ أدنى للمسافة بين مؤثرين من نفس النوع** (`sfx.min_gap`،
  افتراضي ٠.١٢s). الأقرب بينشال، والأول بيفوز.
* حدّ أقصى للمؤثرات المتزامنة (`sfx.max_concurrent`، افتراضي ٣).
* الحدّان بينطبقوا **على خطة المؤثرات ببايثون قبل بناء الرسم**، مش
  بفلتر — فبينفحصوا بالوحدات بلا ffmpeg، زي `graph.py` بالضبط.

## A.8 الموسيقى الخلفية (خارج نطاق هالمرحلة، بس المكان محجوز)

لما تجي: مدخل تاني، `aloop` للتكرار، `sidechaincompress` بالكلام
كمفتاح للـducking، ثم بنفس `amix`. **ما انقاست، فما بتنكتب هون
كأرقام.** أول مرحلة SFX بس.

## A.9 الصيغة والقنوات ومعدّل العيّنة

| البند | القرار | ليش |
|---|---|---|
| معدّل العيّنة | **٤٨٠٠٠** بكل السلسلة | موجود أصلًا (`DEFAULT_SR`)، و`SR % fps == 0` شرط مثبَّت |
| القنوات داخل الرسم | **ستيريو صراحة** | `all=1` ما بتكفي لحالها لو التفاوض غيّر الشكل |
| الصيغة الداخلية | PCM لحد المخرَج | ترميز AAC **مرة وحدة** — القاعدة اللي ألغت تراكم priming padding |
| قصّ المؤثر | `atrim` غير مطلوب | `duration=first` بتقصّ الزيادة (مقيس §B.4) |

---

# B. المخاطر والحالات الحدّية

| # | الخطر | الحالة المقاسة | الحاجز |
|---|---|---|---|
| **B.1** | `normalize=1` بتخفت الكلام وتنفّسه | ٠.١١× عند ٢٠ مؤثرًا · تنفّس ٠.٠٢٠٨→٠.٠٣٥٤ | حارس نصّي على الرسم + فحص "الكلام محفوظ عيّنة بعيّنة" |
| **B.2** | أصل ستيريو بلا `all=1` | المؤثر بيقع عند **٠** (−٤٨٠٠٠ عيّنة) | فحص وضع بأصل ستيريو |
| **B.3** | أصل 44.1k بلا `aformat` | **+٤٢٤٥ عيّنة = +٨٨ms** | فحص وضع بأصل 44.1k |
| **B.4** | مؤثر بيتعدّى نهاية الفيديو | **ما بيمدّد**: ٩٦٠٠٠ = ٩٦٠٠٠ ✅ | فحص طول |
| **B.5** | **القصّ (clipping)** | كلام ٠.٨٨٤ + مؤثر ×٠.٢٥ -> **٢١٠ عيّنة مقصوصة** | ⚠️ **مفتوح — §F** |
| **B.6** | `alimiter` كحلّ للقصّ | بيأخّر **٢٣٩ عيّنة = ٤.٩٨ms** على التيار كله | مرفوض — بيرجّع E2 من ١.٩٨ms لـ~٥ms |
| **B.7** | تراكم الانزياح مع العدد | **ولا تراكم**: أول مؤثر وآخر واحد نفس الخطأ (٢ عيّنة = أرضية الكاشف) | `adelay` مطلق مش متسلسل — بالبناء |
| **B.8** | انفجار الذاكرة زي سلسلة الoverlay | **خطّي**: ٦٢M عند ٢٠٠ · ١٣٥M عند ٥٠٠ | حدّ + `log` عند تجاوزه |
| **B.9** | أصل ناقص | فشل صريح: `No such file or directory` | موجود أصلًا — `.part` بتنمسح |
| **B.10** | أصل بمدة صفر أو تالف | **ما انقاس** | ← §C |
| **B.11** | مؤثر عند الإطار ٠ | وقع عند العيّنة ٠ ✅ | فحص حدّ |
| **B.12** | فيديو بلا صوت (`with_audio=False`) | `[0:a]` مش موجودة أصلًا | المؤثرات لازم **تنطفي**، مش تنبني على سرير مفقود ← §C |

## الحالة اللي كسرت أداة القياس (مسجَّلة عمدًا)

كاشف النبضات عتبته **نسبية لقمة الإشارة**. أول قياس لتطابق الصيغة
مرّ على سرير فيه **كلام عالي مستمر**، فالكاشف صار يمسك الكلام نفسه
ويرجّع مواقع بلا معنى (−٢٣٩٨٤ عيّنة). القياس الصح لازمه **سرير
صامت**. مسجَّلة لأن نفس الفخّ رجع تلات مرات بهالمشروع بأشكال مختلفة.

---

# C. القياسات المطلوبة قبل الكود

## انعملت (`experiments/sfx/`)

| # | القياس | النتيجة |
|---|---|---|
| S1 | دقة `adelay` عيّنة/ميلي | `S` صفر خطأ · ميلي صحيح ±٢٣ عيّنة |
| S2 | `amix` والمستوى والتنفّس | `normalize=1` ٠.١١× + ضخّ |
| S3 | حفظ الكلام عيّنة بعيّنة | `normalize=0` -> **٠ عيّنة متغيّرة** |
| S4 | الصيغة والقنوات | فخّان صامتان · السلسلة القانونية ١٢/١٢ |
| S5 | التوسّع | ١٤M/١٩M/٣٤M/٩٤M/٢١٧M عند ٠/١٠/٥٠/٢٠٠/٥٠٠ |
| S6 | `asplit` مقابل مدخل لكل مؤثر | وفر ٣٤–٣٨٪ ذاكرة ووقت |
| S7 | دمج فعلي بالمسار | E1 ٢٤٠=٢٤٠ · العيّنات ٣٨٤٠٠٠=٣٨٤٠٠٠ · نقرات المصدر متطابقة · ٨/٨ مؤثرات بانزياح ≤٢ عيّنة |
| S8 | الهامش والقصّ و`alimiter` | القصّ حقيقي · `alimiter` بيأخّر ٢٣٩ عيّنة |

## لسا مطلوبة قبل التنفيذ

| # | القياس | ليش |
|---|---|---|
| **C.1** | توزيع ذروة الكلام على مصادر حقيقية | سياسة الهامش (§F) ما بتنحسم بلا معرفة المستوى الواقعي |
| **C.2** | كلفة `volumedetect` (تمريرة قياس مستوى) | لو اخترنا التطبيع، هاد فكّ ترميز إضافي — لازم يتقاس مش يتخمّن |
| **C.3** | أصل تالف أو بمدة صفر | B.10 |
| **C.4** | ذروة RSS على المسار **الكامل** (فيديو+كابشن+مؤثرات) | S5 قاست الصوت لحاله؛ المسار الكامل ~٨٠MiB للكابشن + المؤثرات |
| **C.5** | عدد المؤثرات الواقعي لريل ٦٠s | بيحدّد إذا ٢٠٠ سقف معقول ولا متفائل |

---

# D. اختبارات القبول المقترحة

بنفس منهج E1–E8: **تقيس ناتج ffmpeg، مش الأمر.**

| # | الاختبار | المعيار | الطفرة اللي لازم تفشّله |
|---|---|---|---|
| **S-E1** | الكلام محفوظ عيّنة بعيّنة | ٠ عيّنة متغيّرة برّا نوافذ المؤثرات | شيل `normalize=0` |
| **S-E2** | عدد الإطارات ما تغيّر | `= Σ frame_plan` | — |
| **S-E3** | عدد العيّنات ما تغيّر | مع مؤثرات = بلا مؤثرات | شيل `duration=first` |
| **S-E4** | كل مؤثر على إطاره | انزياح ≤ **٢ عيّنة** (أرضية الكاشف) | استبدل `NS` بميلي صحيح |
| **S-E5** | ولا تراكم | خطأ آخر مؤثر = خطأ أوّلهن | — |
| **S-E6** | أصل ستيريو بيوصل مضبوطًا | نفس العتبة | شيل `all=1` |
| **S-E7** | أصل 44.1k بيوصل مضبوطًا | نفس العتبة | شيل `aformat` |
| **S-E8** | E2 الحالي ما انكسر | نقرات المصدر **مطابقة** لتشغيلة بلا مؤثرات | — |
| **S-E9** | المؤثرات موجودة فعلًا | الفرق بين المخرَجين > عتبة | **ضروري** — بلاه S-E1..E8 بتنجح على "ما في مؤثرات" |
| **S-E10** | حدّ التزامن والمسافة | خطة المؤثرات بتحترم `min_gap`/`max_concurrent` | وحدات، بلا ffmpeg |
| **S-E11** | فيديو بلا صوت | المؤثرات بتنطفي، والتشغيلة بتنجح | — |

**S-E9 مش رفاهية.** كل باقي الفحوص بتقيس "ما تغيّر شي" — وهاد
بيتحقّق تمامًا لو المؤثرات ما نزلت أصلًا. هاد بالضبط الفخّ اللي وقعنا
فيه بالمرحلة ٨ (فحص قتل ffmpeg اللي كان بيمرق على عملية ميتة).

---

# E. مراحل التنفيذ

| # | المرحلة | المخرَج | معيار التوقف |
|---|---|---|---|
| **٠** | حسم §F | قرار سياسة الهامش + مصدر الأصول | **بلاه ما في تنفيذ** |
| **١** | أدوات القياس | كاشف مؤثرات + أرضيته الموثّقة | الأرضية مقاسة (٢ عيّنة) ومكتوبة |
| **٢** | S-E1..E11 **على الكود الحالي** | فحوص فاشلة | **S-E9 لازم تفشل** — ما في مؤثرات بعد |
| **٣** | خطة المؤثرات (نقية) | `autoreel/sfx.py` — أحداث -> `[(أصل, فهرس إطار, كسب)]` | وحدات بلا ffmpeg · مش موصولة |
| **٤** | باني الرسم | `graph.sfx_chain()` | وحدات · حارس على `normalize=0` و`all=1` و`aformat` |
| **٥** | الوصل | `build_output` + مفاتيح config | S-E1..E9 ✅ · **E1/E2/E3/E7/E8 ما تغيّروا** |
| **٦** | الأصول والخريطة | `assets/sfx/` + جدول الأحداث | الصور المرجعية ما تتأثر (الكابشن ما انلمس) |
| **٧** | تنظيف وتوثيق | CLAUDE.md · ISSUES.md | الطقم كامل أخضر |

وقفة بعد كل مرحلة، زي إعادة التصميم.

---

# F. القرار: هل نبدأ التنفيذ؟

## **لأ — في قراران لازم تحسمهن إنت أولًا.**

الجانب التقني محسوم ومقاس: نقطة الإدخال، والسلسلة القانونية، ودقة
الوضع، وحفظ الكلام، والتوسّع — كلهن أرقام مش آراء. بس في بندان ما
بينحسموا بالقياس:

### القرار ١ — سياسة الهامش (blocker حقيقي)

`normalize=0` بتجمع، والقصّ **مقيس ومؤكَّد**:

| قمة الكلام | كسب المؤثر | عيّنات مقصوصة |
|---|---|---|
| ٠.٧٠٧ | ×٠.٢٥ | **٠** ✅ |
| ٠.٧٠٧ | ×٠.٥٠ | ٢٤٠ ❌ |
| ٠.٨٨٤ | ×٠.٢٥ | ٢١٠ ❌ |
| ٠.٩٧٢ | ×٠.٢٥ | ٣٩٠ ❌ |

تلات مسارات، وكل واحد إله ثمن مختلف:

| الخيار | الثمن | الأثر على E2 |
|---|---|---|
| **أ. طبّع الكلام لهامش ثابت** (ذروة ~٠.٧) | بيغيّر **مستوى** الكلام (ضرب بثابت)، وبده تمريرة `volumedetect` (C.2) | **ولا أثر** — E2 بتقيس التوقيت مش المستوى |
| **ب. مؤثرات خافتة وبس** (×٠.٢٥ وأقل) | بتضيع على مصدر عالي؛ ما في ضمان | ولا أثر |
| **ج. `alimiter` بعد المزج** | بيأخّر **٢٣٩ عيّنة = ٤.٩٨ms** التيار كله | **بيرجّع E2 من ١.٩٨ms لـ~٥ms** |

**توصيتي: (أ).** التطبيع ضرب بثابت — ما بيلمس التوقيت، فخاصية
"الكلام محفوظ" بتضل قائمة بالمعنى اللي بيهمّ E2. و(ج) مرفوضة لأنها
بتاكل من الرقم اللي صرفنا مرحلة كاملة لنوصله.

بس (أ) بتغيّر **صوت** الريل النهائي — وهاد قرارك مش قراري.

### القرار ٢ — من وين الأصول؟

ما في ولا ملف صوت بالمستودع، وما بقدر أخترع أصولًا. بدّي منك:

* **مصدرها والترخيص** (CC0؟ مشتراة؟ مسجَّلة؟) — بينحطّوا بالمستودع
  ولازم يكونوا قابلين للتوزيع.
* **الشكل**: بقترح WAV 48k ستيريو **معدّة سلفًا بالمعدّل الصح** —
  بيشيل `aformat` من المسار الحرج ويخلّيه شبكة أمان بس.
* **كم أصل بالبداية؟** بقترح خمسة: `whoosh` · `pop` · `impact` ·
  `tick` · `riser`.

---

## اللي ما بيلزم قرار

كل هدول محسومة بالقياس، وبتنفَّذ زي ما هي أول ما ينحسم اللي فوق:

* نقطة الإدخال بين `[acat]` و`asplit=M`
* `adelay=<عيّنة>S:all=1` بعد `aformat`
* `amix=…:duration=first:normalize=0`
* `-i` لكل أصل مميّز + `asplit` لاستعمالاته
* الفهرس من `frame_plan`، ولا زمن بالثواني بمسار المؤثرات
* S-E9 (المؤثرات موجودة فعلًا) شرط قبل أي فحص "ما تغيّر شي"

**ولا سطر كود إنتاج بينكتب قبل موافقتك على هالمواصفة.**
