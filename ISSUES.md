# auto-reel — الحالة والمشاكل المعروفة

> محدَّث بعد المرحلة ٩ من إعادة التصميم. **المصدر هو الكود** — لو
> خالف هالملف الكودَ فالكود صح والملف قديم.

**الطقم: ٦٣٨ فحص، صفر `xfail`.**
عدد الاختبارات مش دليل على صحة المسار. صار `render.py` مفحوصًا على
ناتج ffmpeg نفسه — مش على الأوامر بس: `tests/test_acceptance.py`
بيعدّ إطارات المخرَج، وبيتحقّق من هوية كل إطار، ومن انزياح الصوت، ومن
إطار بداية كل كابشن، ومن الزوم لكل مقطع؛ و`tests/test_failure.py`
بيقتل ffmpeg فعلًا وبيتأكد إنه ما بيضل ملف تالف. `--dry-run` ضلّ
للطبقة اللي تحت (شكل سلسلة الفلاتر). شوف `ENGINEERING-REVIEW.md`
للثغرات الباقية بين الاختبارات والسلوك الفعلي.

---

## اتصلّح مؤخرًا

| المشكلة | الحالة |
|---|---|
| الكابشن الطويل بينقصّ من الطرفين | ✅ `_fit()` بتصغّر وتلفّ سطرين — بحدود، شوف تحت |
| الكابشن بيطفي بين الكلمات (كاريوكي) | ✅ الإطار بيمتد لبداية الكلمة الجاية |
| جسر الفجوة كان رقمًا ثابتًا (٠.٤٠) | ✅ مربوط بـ`cuts.min_gap` |
| فحص raqm بيكسر `--no-captions` | ✅ انتقل لأول رسم كابشن |
| `pass*.mp4` بتتكتب بمجلد الشغل | ✅ بمجلد temp وبتنحذف فور استهلاكها |
| `captions.stroke` مفتاح ميت | ✅ **انحذف** — ما كان ينقرا ولا مرة، وقيمته كانت `0` |
| `from_srt` الكتلة الفاضية بتبلع اللي بعدها | ✅ تقسيم على السطر الفاضي أولًا |
| `remap_words` بتصهر كلمتين متطابقتين | ✅ التمييز بالفهرس مش بالنص |
| عتبة الـ٤٥٪ لكل مقطع لحاله | ✅ صارت تراكمية عبر كل المقاطع |
| `min_seg` بتشيل كلمات بصمت | ✅ تحذير بيسرد الكلمات المشالة |
| كاش Whisper بيتجاهل الموديل واللغة | ✅ `raw.small.ar.words.json` |
| Whisper بيشتغل بلا لزوم | ✅ بينتخطى مع `--no-cut --no-captions` |
| **النص المختلط عربي/لاتيني** | ✅ `_bidi_runs()` — **شوف القسم المخصص تحت** |

`geometry.crop_bias` صار **٠.٣٠** (كان ٠.٣٨ وبيقصّ ١٣px من أعلى الرأس
على العيّنة المقاسة). **قيمة أولية مش رقم عام** — معايَرة على تأطير
رأس بين ١٦٪ و٤٤٪ من ارتفاع الإطار. شغّل `--preview-frames` على أي
فيديو جديد قبل ما تصرف ترميز.

---

## Mixed Arabic/Latin Text

### المشكلة الأصلية

الكابشن بينرسم **كلمة كلمة** — وهاد مش ترف، هو أساس التلوين
الكاريوكي: كل كلمة لازم تنرسم لحالها عشان نقدر نلوّنها وقت نطقها.

بس bidi خوارزمية على مستوى **السطر** مش الكلمة. الرسم كلمة كلمة
بترتيب RTL بحت بيتخطاها، والنتيجة:

```
حمّل من App Store هلأ   ->   حمّل من Store App هلأ
تحديث iOS 18 نزل        ->   تحديث 18 iOS نزل
افتح Google Play Store  ->   افتح Store Play Google
```

**الانكسار محدَّد بدقة: كلمتان لاتينيتان متجاورتان أو أكتر.** كلمة
لاتينية وحدة محاطة بعربي كانت بتنجو — لأنها run واحد أصلًا وبتحتل
خانة RTL واحدة، فبيتصادف الصح. لهيك `شغال على Flutter من ٢٠٢٤`
كان سليم من قبل التصليح.

### كيف يشتغل `_bidi_runs`

`captions.py:_bidi_runs(tokens)` بترجّع **ترتيب التوكنات البصري من
اليسار لليمين** لسطر قاعدته RTL. تلات خطوات:

1. **التصنيف** — `_token_dir()` بتقرا أول محرف **قوي** بالتوكن عبر
   `unicodedata.bidirectional`: `R`/`AL` -> `'R'`، `L` -> `'L'`،
   ولا واحد -> `'N'` (محايد).
2. **حلّ المحايدات** — التوكن المحايد بياخد اتجاه اللي قبله لو كان
   `'L'`، وإلا `'R'`. المحايد بأول السطر بياخد `'R'` (اتجاه القاعدة).
3. **التجميع والعكس** — المتجاورات بنفس الاتجاه بتصير run واحد.
   الـruns بتتصفّ من اليمين لليسار، وجوّا الـrun اللاتيني الكلمات
   بتتصفّ من اليسار لليمين.

حلقة الرسم بـ`render_caption` بتمشي على هالترتيب. والاسمان مفصولان
صراحة بالكود لأن خلطهن هو مصدر الباگات هون:

```
logical_index = مكان التوكن بالنص، وهو اللي highlight_idx بيشير إله
draw_position = مكان رسمه على الصورة من اليسار لليمين
```

بالعربي الخالص هدول معكوس بعض، وبالمختلط لا هيك ولا هيك.

### ليش قسّمنا لـruns بدل حلول تانية

| البديل | ليش انرفض |
|---|---|
| رسم السطر بنداء واحد (bidi صحيح ١٠٠٪) | **بيكسر الكاريوكي.** Pillow ما بيكشف مواقع الglyphs بعد الترتيب، فما في طريقة نعرف وين وقعت الكلمة رقم i لنلوّنها |
| `python-bidi` لحساب الترتيب | شوف `Architectural Decisions` تحت |

### العربي + الإنجليزي + الأرقام

القاعدة اللي رح تتنسى، فهاي أمثلتها المقاسة:

| النص | التصنيف | ليش |
|---|---|---|
| `تحديث iOS 18 نزل` | `18` تصير LTR | قبلها `iOS` لاتينية -> نفس الـrun |
| `رقم 2024 و 2025 سوا` | `2024` و`2025` RTL | قبلهن عربي -> كل وحدة خانة RTL لحالها |
| `Flutter + Dart` | `+` تصير LTR | قبلها لاتينية -> run واحد |
| `الإصدار 2.5 جاهز` | `2.5` RTL | قبلها عربي |
| `iPhone 15 Pro Max` | كله LTR | run واحد |

الأرقام العربية (`٢٠٢٤`) صنفها `AN`، واللاتينية (`2024`) صنفها `EN` —
الاتنين محايدان عند `_token_dir` وبيتبعوا نفس القاعدة.

### الحدود الحالية — الحل **مش** UAX#9 كامل

هاد تقريب مقصود، بيغطي الحالة العملية (عربي فيه أسماء منتجات وأرقام
وروابط) وبس. اللي ما بيغطيه:

- **المستويات المتداخلة** — RTL جوا LTR جوا RTL.
- **علامات الاتجاه الصريحة** — LRM/RLM/LRE/RLE وما شابه بتنتجاهل.
- **الترقيم على حدود الـruns بدقة تامة** — قواعد W1–W7 وN0–N2
  بالمواصفة أدقّ من "خُد اتجاه اللي قبلك".
- **run بينقسم على سطرين** — بينرتّب **داخل كل سطر لحاله**. المواصفة
  بتعامل الـrun كوحدة عبر اللف. النتيجة عمليًا مقبولة (كل سطر بيقرا
  صح لحاله) بس مش مطابقة للمواصفة. عليها اختبار صريح
  (`test_latin_run_split_across_lines_is_ordered_per_line`).
- ~~انعكاس الأقواس غير المزدوجة~~ — **اتصلّح**. كل توكن بينرسم
  باتجاه الـrun تبعه، فالمحايد على طرفه ما عاد ياخد اتجاه القاعدة
  وينعكس.

**ما منّدعي إن المشكلة محلولة بالكامل.** المحلول: ترتيب الـruns
للحالة العملية، متحقَّق منه بمقارنة آلية بمرجع raqm على ١٧ حالة تشمل
الروابط والأقواس والترقيم والemoji والتشكيل. الباقي هش: شكل الأقواس،
والحالات اللي بتحتاج UAX#9 فعلي.

---

## Architectural Decisions

### ١. ليش PNG overlay بدل ASS/libass

`libass` بيعتمد على `fribidi` لترتيب النص، ومكدّس مختلف يعني نتيجة
مختلفة — والمشروع انكوى من مفاجآت العربي مرة. الPNG بيضمن الشكل ١٠٠٪
لأننا احنا اللي رسمناه بـPillow+raqm وشفناه بعينّا (صور مرجعية
بـ`tests/golden/`).

**الثمن الحقيقي لهالقرار:** التلوين الكاريوكي بيصير صورة لكل كلمة —
٢١ PNG لـ١٦ ثانية، ×٣ مقاسات = ٦٣ ملف. وحرقهن كان overlay لكل كابشن،
وهاد اللي فرض تقسيم الدفعات (`PERF-1`). **انحلّت:** الصور صارت تسلسلًا
مفهرسًا بالإطار وoverlay **واحد**، فعدد الكابشنات ما عاد يغيّر لا شكل
الرسم ولا الذاكرة (~٨٠ MiB ثابتة بدل ٢٧٧١ MiB عند ٢٠٠ كابشن).

### ٢. ليش القص من word timestamps بدل `silencedetect`

`silencedetect` بيكشف الصمت **الصوتي**، وفيديو الكلام فيه أنفاس
وضجيج خلفية وتردد — فبتطلع حدود مش عند حدود الكلام. توقيت الكلمات
من Whisper بيعطي حدود **لغوية**: بنعرف وين خلصت الكلمة، فالقص بيوقع
بالمكان الصح ومنقدر نعيد تخطيط توقيت الكابشن على نفس الأساس
(`remap_words`).

`parse_silencedetect()` كانت موجودة بـ`cuts.py` كبديل احتياطي بلا ولا
مستدعي ولا علم يشغّلها. **انحذفت بالمرحلة ٩.** كود ما بينشتغل ما
بينفحص، وبيوهم اللي بيقرا إن في مسار تاني مدعوم.

### ٣. ليش انرفضت `python-bidi`

**الرفض مش تقني.** `python-bidi` بتعطي ترتيبًا **أصح** من تقريبنا —
bidi كامل بمواصفة UAX#9 بدل الثلاث خطوات تبعنا.

الرفض لأن **وجودها بملف التبعيات إغراء**. أي حدا بيجي بعدنا وبيشوفها
مثبّتة رح يستنتج إنها مسموحة، وأقرب استعمال إلها هو تمرير النص عليها
قبل الرسم — وهاي بالضبط الوصفة اللي بتعطي انعكاسًا مزدوجًا مع
Pillow+raqm، يعني "العربي بيطلع مقلوب" أو "الفيديو بيطلع أسود".

الخطر مش بشو بتعمله المكتبة اليوم، الخطر بشو بتغري فيه بعد ستة أشهر
لما ما يكون حدا فاكر ليش انحطت. فالمقايضة: bidi تقريبي مكتوب عنا
ومختبَر، مقابل إغلاق باب الانحدار اللي كوى المشروع مرة.

---

## مشاكل مفتوحة

> **إعادة التصميم مقفولة ومقبولة** (`2c0d1cf`, `REDESIGN-SPEC.md`).
> المفتوح غير الحاجز **تلاتة بس: MO-1 · EC-2 · EC-3** — ولا واحد فيهن
> بيبرّر إعادة فتح معمارية الرندر. أي تغيير معماري بده blocker مثبَت
> **باختبار فاشل**.

مرتّبة حسب الأثر. التفاصيل الكاملة (السبب، الاستنساخ، الحل) بـ
`ENGINEERING-REVIEW.md`.

| # | المشكلة | التصنيف |
|---|---|---|
| MO-1 | مسار الـM مخرجات برسم واحد **مبني ومش موصول** | 🟠 مفتوحة |
| EC-2 | الemoji بتطلع مربعات فاضية — ما في font fallback | 🟠 مفتوحة |
| EC-3 | `group_words` بتخلط جُمل مختلفة بكابشن واحد | 🟠 مفتوحة |
| ~~CR-1~~ | ~~توقيت الكابشن بينزاح عن الصورة~~ | ✅ **متصلّح** |
| ~~CR-2~~ | ~~انزياح الصوت عن الصورة بيتراكم~~ | ✅ **متصلّح** |
| ~~CR-3~~ | ~~توكن ما بينلف بينتج كابشن أعرض من الإطار~~ | ✅ **متصلّح** |
| ~~CR-4~~ | ~~`burn_captions` بتزيد إطار واحد~~ | ✅ **متصلّح** |
| ~~CR-5~~ | ~~أول إطار بكل مقطع بينكرّر~~ | ✅ **متصلّح** |
| ~~EC-1~~ | ~~القوس غير المزدوج بينعكس~~ | ✅ **متصلّح** |
| ~~PERF-1~~ | ~~١٨ عملية ffmpeg لريل بتلات مقاسات~~ | ✅ **متصلّح** |
| ~~PR-1~~ | ~~`probe_duration` بتلزم `ffprobe`~~ | ✅ **متصلّح** — ⚠️ شوف الباقي تحت |
| ~~EC-6~~ | ~~مدخل VFR بيعطي مدد مقاطع غير متوقعة~~ | ✅ **متصلّح** |
| ~~PERF-1~~ ب | ~~ترميز المقاطع لكل مقاس (PERF-2)~~ | ✅ الترميز مرة لكل مخرَج · الفكّ لسا مكرَّر ← MO-1 |
| EC-4 | `--dry-run` بيكتب صور الكابشن | 📘 **قرار موثّق** |
| EC-5 | المحايد بعد لاتيني بينضم للـrun اللاتيني | 📘 **قرار موثّق** |
| PERF-3 | صور كابشن لكل مقاس | 📘 **قرار موثّق** |
| ~~—~~ | ~~`parse_silencedetect` كود ميت~~ | ✅ **انحذفت** |

**CR-1** — الخطة صارت بالإطارات (`cuts.frame_plan`) و`-frames:v` بدل
`-to`. مقاس على ٢٤ مقطع: الخطأ **+٠.٨٣٨s → صفر مطلق** (٥٣٨ = ٥٣٨ إطار).
تقريب الأزمان لشبكة الإطارات **ما كان بيكفي** — قِسناه فقلب الخطأ من
‎+112ms لـ‎−38ms بس ما صفّره، لأن قرار عدد الإطارات كان لسا عند ffmpeg.

**CR-3** — `_fit` صار عندها قيد عرض حقيقي: التوكن اللي ما بيسع بينكسر
عند `/` و`-` وأمثالهن (`_split_token`). العرض مضمون **بالبناء**،
و`assert_fits_frame` رجعت شبكة أمان مش آلية تخطيط.

**EC-1** — كل توكن بينرسم باتجاه الـrun تبعه (`_resolve_dirs`) بدل
`"rtl"` الثابت.

**CR-4** — مكتشَف أثناء قياسات `REDESIGN-SPEC.md`. المخرَج بيطلع **إطار
زيادة** عن `frame_plan`. مقاس على كود الإنتاج:

```
python -m autoreel.cli long.mp4 --srt many.srt --sizes reel
    خطة الإطارات: 538 · المخرَج الفعلي: 539     ❌ +1
--no-captions
    خطة الإطارات: 538 · المخرَج الفعلي: 538     ✅
```

فالمصدر `burn_captions` مؤكَّد. تتبّعناه لأول دفعة حرق: `720 → 721`،
وباقي الدفعات بتحافظ على ٧٢١. السبب: مدخلات الـPNG تيارات صورة بإطار
واحد ومدة مجهولة، وما في `-shortest` ولا `-frames:v` بيحدّ المخرَج،
فالـoverlay بيطلّع إطار زيادة عند EOF.

من نفس عائلة CR-1 (المخرَج بيخالف المخطط)، وما انكشف قبل لأن **ولا
اختبار كان بيقارن عدد إطارات المخرَج بـ`frame_plan`** — الاختبار المفقود
أهم من الباگ. إعادة التصميم بتلغيه بالبناء، بس هو موجود اليوم بكل مخرَج
فيه كابشن.

**CR-5** — مكتشَف بالمرحلة ٢ من التنفيذ، بأول تشغيلة لاختبار **هوية
الإطار** (E7). **كل مقطع بيعرض أول إطار له مرتين**، فبيضيع إطار من
محتواه المخطَّط.

قياس على مصدر كل إطار فيه بيحمل رقمه:

```
مقطع 0: [54, 54, 54, 55, 56, …]     ١٩ إطار · ١٧ مميّز
مقطع 1: [114, 114, 115, 116, …]     ١٨ إطار · ١٧ مميّز
مقطع 2: [174, 174, 175, 176, …]     ١٨ إطار · ١٧ مميّز
…كل المقاطع
```

السبب معزول بالقياس: **زمن `-ss` مش على شبكة الإطارات**.

```
seek 1.780 (برّا الشبكة) -> ١٨ إطار · ١٧ مميّز · تكرار ١
seek 54/30 (على الشبكة)  -> ١٨ إطار · ١٨ مميّز · تكرار ٠
```

`build_base` بتمرّر `-ss {a:.3f}` و`a` جاي من
`segments_from_words` (بداية كلمة ناقص `pad`)، يعني عدد عشري عشوائي.
ffmpeg بيحطّ بداية المخرَج عند الزمن المطلوب، وأول إطار مفكوك بيقع
بعده بجزء إطار، فتحويل CFR بيكرّره ليعبّي `t=0`. مش فلتر `fps` —
جرّبناه بدونها وتكرّر كمان.

**الأثر:** تلعثم مرئي عند كل قطعة، و~٥.٦٪ من محتوى كل مقطع بينستبدل
بتكرار. وبتطول: ريل فيه ٢٠٠ قطعة بيخسر ٢٠٠ إطار محتوى.

**ليش ما انكشف قبل:** `-frames:v n` بيضمن **العدد** مهما تكرّر
المحتوى، فما في ولا فحص بيعدّ قادر يشوفه — ولا حتى E1. انكشف أول ما
صار في فحص بيقرا **هوية** كل إطار.

الإصلاح موجود بإعادة التصميم أصلًا: `select` بتقصّ بفهرس الإطار مش
بالزمن، فالمشكلة بتنقرض بالبناء.

### ✅ الاتنين انقرضوا بالمرحلة ٥ (مسار الفيديو الواحد)

مقاس على نفس السيناريو (٨ مقاطع، ١٤٤ إطار مخطَّط):

| | قبل | بعد |
|---|---|---|
| عدد الإطارات (E1) | ١٤٥ (**+1**) | **١٤٤ ✅** |
| إطارات مكررة (E7) | **٩** | **٠ ✅** |
| محتوى المقطع | ١٧ مميّز من ١٨ | **١٨ من ١٨ ✅** |
| بداية المقطع | ٥٤ (`ceil` من seek) | **٥٣ = `round(a·fps)` ✅** |

ولا واحد منهن انتصلّح بكود خاص فيه: `select` بتقصّ بفهرس الإطار
فما ضل في `-ss` برّا الشبكة (CR-5)، وتشغيلة الفيديو الواحدة ما فيها
تمريرات حرق متسلسلة (CR-4).

**دليل العزل:** مسار الصوت ما انتغيّر ولا حرف بهالمرحلة، وقياس E2 طلع
مطابقًا للرقم: `[21.3, 40.0, 58.7, 77.3, 96.0, 114.7, 133.3, 152.0]` ms
قبل وبعد. يعني اللي اتغيّر هو الفيديو وبس.

### القرار الأصلي (للسجل): ما انتصلّحوا كتصليحات مستقلة

**مسجَّل عمدًا، مش سهو.** الاتنين بينتهوا معماريًا مع المسار الواحد:

| الخلل | شو بيلغيه |
|---|---|
| CR-4 | مسار الكابشن الجديد (تسلسل صور مفهرس بالإطار) — ما عاد في تمريرات حرق متسلسلة تضيف الإطار |
| CR-5 | `select` بتقصّ **بفهرس الإطار**، فما عاد في `-ss` بزمن برّا الشبكة |

تصليحهن هلأ منفصلين بيعني منطقًا مؤقتًا بينشال بعد مرحلتين، **وخسارة
القدرة على إثبات إن إعادة التصميم هي اللي أصلحتهن**.

**الشرط المقابل — إلزامي:**

* E1 وE7 بيضلّوا **فاشلين** لحد ما يخلص المسار الواحد.
* بعدها لازم ينجحوا بلا أي `xfail`، وبلا تخفيف عتبة، وبلا تعديل
  السيناريو.
* `test_the_baseline_really_is_broken_in_the_ways_we_claim` بيثبّت
  **بصمة** كل خلل (فرق `+1` بالضبط، والتكرار عند أول إطار بالضبط) —
  فلو حدا "صلّحها" بطريقة بتغيّر البصمة، الحارس بينكسر ولازم قرار.

**CR-2** — انصلّح بالمرحلة ٦: الصوت بينقصّ بـ`atrim` بفهرس العيّنة
وبيضل PCM جوا الرسم لحد المخرَج، فترميز AAC بيصير **مرة وحدة**.

| | قبل | بعد |
|---|---|---|
| انزياح كل نقرة (٨ مقاطع) | `21.3 → 152.0` ms | `[0, 0, 0, 2, 0, 0, 2, 2]` ms |
| أقصى | ١٥٢.٠ms | **١.٩٨ms** = أرضية القياس |
| تراكم | **+١٣٠.٧ms** | **+١.٩٨ms** |
| ترميزات AAC | واحد لكل مقطع | **واحد للملف** |

والسبب البنيوي: `concat -c copy` ما بيقدر يشيل priming padding من
مقاطع مرمَّزة مستقلة، فالتأخير بينضاف مع كل قطعة. الحل مش تعويض
بالحساب — الحل إنك ما ترمّز إلا مرة وحدة.

---

### 🟠 MO-1 — مسار الـM مخرجات برسم واحد مبني ومش موصول

`graph.build_graph(cfg, plan, starts, sizes, …)` بتاخد **قائمة** مقاسات
وبتبني `split=K` للفيديو و`asplit=M` للصوت وmap لكل مقاس، وعليها
اختبارات وحدة بتمرّر ٢ و٣ مقاسات.

**بس الإنتاج ما بيستعملها هيك.** `cli._one_export` بتنادي
`render.build_output` مرة لكل مقاس، وكل نداء بيمرّر `[(اسم, cfg)]` —
مقاسًا واحدًا. مقيس بـ`--dry-run`:

```
python -m autoreel.cli in.mp4 --srt test.srt --sizes all --dry-run
    عدد أوامر ffmpeg: 3
      -> out.reel.part.mp4
      -> out.square.part.mp4
      -> out.wide.part.mp4
```

يعني `split[z0]` بتطلع `null` دايمًا، و`asplit=4` بالرسم فوق هي
تقسيم **المقاطع** مش تقسيم المخرجات.

**الثمن:** فكّ ترميز المصدر وقصّه وبناء الصوت بينعادوا M مرة. الترميز
نفسه ضلّ واحدًا لكل مخرَج (وهاد اللي أعطى ٣٦٦.٨s ← ١١٤.٠s عند ٤
مقاسات)، فالربح الباقي هو شغل الفكّ والقصّ المكرّر.

**ليش ما انوصلت بالمرحلة ٩:** وصلها شغل معماري — كل مقاس بيرسم كابشناته
بعرضه، فالرسم الواحد بده M مدخلات صور وM سلاسل overlay، وبده قياس ذاكرة
جديد (§11 قاست الزوم بـ٤ مخرجات عند ١٥٤٥ MiB). المرحلة ٩ تنظيف مش
معمارية.

**مقابل الغلط اللي انكتب بتقرير المرحلة ٨:** قلت هناك إن الـM مخرجات
صاروا تشغيلة وحدة، وإن `--max-parallel-outputs` سقطت لهالسبب. السبب
الصح إن المقاسات بتنعمل **بالتتابع** بحلقة، فما في توازي ينتسقّف —
و"فشل مقاس ما بيسقّط الباقي" ضلّت صحيحة (`cli.main` بتمسك الاستثناء
لكل مقاس وبتكمّل).

### ✅ EC-6 — مدخل VFR (متصلّح بالبناء، ومتحقَّق منه)

كان: `-ss`/`-to` بيقصّوا **قبل** ما `fps` يوحّد المعدل، فمدخل VFR
(وهاد شكل فيديو الموبايل الشائع) بيعطي مدد مقاطع غير متوقعة.

صار: `video_stem` بترتّبهن بالعكس —

    [0:v]fps=30,select='between(n,…)',settb=1/30,setpts=N,fps=30

`fps` **أول شي**، فالتيار بيصير CFR قبل ما يشوفه `select`، وفهرس
الإطار بيصير معرَّفًا تعريفًا وحيدًا. متحقَّق على مصدر VFR حقيقي
(٧ فروق طوابع مختلفة، ١٢٩ إطار):

```
خطة الإطارات: [42, 48, 48, 51] = 189
إطارات المخرَج:                  189   ✅
فروق طوابع المخرَج: [0.0333]  -> CFR   ✅
```

### 📘 EC-4 — `--dry-run` بيكتب صور الكابشن (قرار)

`--dry-run` **جاف بالنسبة لffmpeg بس**. باقي المنطق بيشتغل كامل —
خطة القص والهندسة ورسم الكابشن وأسماء الملفات — فالمطبوع هو الأمر
الحقيقي مش تقريب إله. **وهاد نصّ قيمته:** بلاه ما بينفحص ولا شي من
المسار. الملفات بمجلد temp وبتنحذف مع نهاية التشغيلة.

### 📘 EC-5 — المحايد بعد لاتيني بينضم للـrun اللاتيني (قرار)

مش خلل — هاي **القاعدة المقصودة** لـ`_bidi_runs`، موثّقة بـ`CLAUDE.md`
مع أمثلتها (`تحديث iOS 18 نزل` -> `18` بتصير LTR). جزء من "التقريب
المقصود" اللي حدوده مسرودة بقسم النص المختلط فوق: مش UAX#9 كامل، وما
انقدر ينتج حالة واقعية بتنكسر.

### 📘 PERF-3 — صور كابشن لكل مقاس (قرار)

الكابشن **بينرسم من جديد بعرض كل مقاس**، مش بينشتق بالقص من مقاس
تاني — وإلا بيرجع باگ النص المقصوص من الباب الخلفي (CR-3). `_fit`
بتاخد العرض المتاح كمدخل أصلًا فإعادة الرسم شبه مجانية، والوفر
الحقيقي كان بتقليل الـoverlays (انعمل: overlay واحد) مش بتقليل الرسم.

### ✅ PR-1 — `probe_duration` كانت بتلزم `ffprobe` (متصلّحة)

**كان:** `cli.main` بتنادي `probe_duration` بأول سطر بكل تشغيلة، وهي
بتنادي `ffprobe`. بيئة التطوير فيها ffmpeg static من johnvansickle
بلا `ffprobe` — بالضبط الحالة اللي حذّر منها تعليق `probe_source`:

```
$ python -m autoreel.cli source.mp4 --srt subs.srt -o out.mp4
  File "autoreel/cuts.py", line 6, in probe_duration
FileNotFoundError: [Errno 2] No such file or directory: 'ffprobe'
```

**وليش ما مسكه ولا فحص من الـ٦٢٩:** كل فحص بيمرق على المسار كان بيبدّل
`probe_duration` بـ`monkeypatch` لأن الفحوص بتولّد مصادرها وبتعرف
مدتها. الطقم أخضر والبرنامج ما بيقلع — **الفحص اللي بيبدّل الجزء
المكسور ما بيشوف الكسر.**

**صار:** `cuts.probe(path)` بترجّع `(عرض, ارتفاع, فيه_صوت, مدة)` من
نداء `ffmpeg -i` واحد. `probe_duration` و`render.probe_source` صاروا
واجهتين رفيعتين فوقها — تحليل واحد، ولا نسختين بتنفرقا. القراءة
بـ`cuts` مش `render` لأن `render` بتستورد `cuts`، فالعكس دورة استيراد.

**الحارس:** `tests/test_probe.py` — ٩ فحوص، **ولا واحد بيبدّل**
`probe_duration`. الأساسي بيشغّل الـCLI تشغيلًا حقيقيًا على `PATH`
معمول بوصلات لكل الأدوات **إلا `ffprobe`**، فإله أسنان حتى على جهاز
مثبَّت عليه. أربع طفرات بتثبته: إرجاع `ffprobe`، تخمين مدة صفر بدل
الفشل، إلغاء حدّ الساعات، إلغاء حدّ الدقائق.

---

#### ⚠️ الباقي بعد الإصلاح: المدة صارت مقرَّبة لجزء المئة

**مقاس، ومش قابل للتفادي بـffmpeg لحاله.** `Duration: HH:MM:SS.ss` هو
أدقّ شي بيطلّعه `ffmpeg -i` بأي `-loglevel` (جرّبنا verbose وdebug
وtrace)، بينما `ffprobe -show_entries format=duration` كان بيعطي
ميكروثانية. البدائل انقاست وانرفضت:

| البديل | الفرق عن `ffprobe` |
|---|---|
| `Duration:` من stderr | **≤ ٥ms** ← المختار |
| `-progress` + `out_time_us` | لحد **١٨٦ms** — بيقيس نهاية أطول تيار مش مدة الحاوية |
| `-map 0 -c copy` + `-progress` | نفسها، ١٨٦ms |

المرجع بالقياس كان صندوق `mvhd` مقروءًا بايت-ببايت — نفس الرقم اللي
بيرجّعه `ffprobe` لملفات mp4، بلا ما نحتاجه.

**وين بتوصل:** `duration` بتدخل بتلات أماكن بس — `--no-cut`، ولا كلمات
أصلًا، وحدّ `min(duration, prev_end + pad)` اللي بيشتغل بس لما آخر كلمة
تخلص على بعد أقل من `pad` (٠.١s) من نهاية الملف. **بالمسار المعتاد (قص
شغّال وفيه كلمات) ما إلها أثر إطلاقًا.**

بالحالات التلاتة بتغيّر `frame_plan` **بإطار واحد** لـ**٨.٢٥٪** من
المدد عند ٣٠fps (مسح ٢٠٠ ألف مدة عشوائية). مقاس على ملفات حقيقية:

```
ملف                 ffprobe      ffmpeg   خطة قديمة  خطة جديدة
s_1501_30.mp4      50.155000       50.15        1505       1504  ⚠️
s_44_24.mp4         1.984000        1.98          60          59  ⚠️
s_301_30.mp4       10.219000       10.22         307         307
s_997_30.mp4       33.234000       33.23         997         997
```

**مش خطأ بأي اتجاه** — الاتنين قراءة صحيحة لمدة الحاوية، وE1 (المخرَج =
الخطة) بيضل صحيحًا بالحالتين. **بس هو اختلاف حقيقي بالمخرَج**، فمسجَّل
هون بدل ما ينمرق بصمت. لو بدك صفر اختلاف، الطريق الوحيد قراءة صندوق
`mvhd` بأنفسنا — وهاد بيعني كتابة demuxer مصغّر لكل حاوية مدعومة، يعني
شغل معماري مش إصلاح.

---

**لسا ما انبنى:** موسيقى خلفية، b-roll أوتوماتيك، كشف اللقطات الضعيفة.
