"""طبقة تفويض رقيقة بين `autoreel` (المحرر) و`ai_pipeline` (التوليد).

**ولا سطر منطق هون.** كل شي بهالحزمة إما إعادة تصدير لدالة موجودة
بـ`autoreel`، أو غراء صغير ما إله مكان طبيعي بأي طرف.

ليش الطبقة أصلًا لو كانت إعادة تصدير: عشان **الحدّ يصير معلَنًا**.
`ai_pipeline` بتستورد من `shared` بس؛ فأي يوم بدنا ننقل التنفيذ فعلًا
من `autoreel` لـ`shared`، بينتنقل من مكان واحد ومستدعو التوليد ما
بيحسّوا. والاتجاه واحد:

    ai_pipeline  ──►  shared  ──►  autoreel
    autoreel     ──X──  shared            (ممنوع — الحدّ بيصير دائرة)

وعليه حارس بالاختبارات: `tests/aipipeline/test_shared.py`.
"""
