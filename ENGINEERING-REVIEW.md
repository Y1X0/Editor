# ENGINEERING-REVIEW — auto-reel

> مراجعة مستقلة للمسار كامل عند commit `38666ac`.
> كل بند مقاس على الكود مش مستنتج منه، إلا لما تقول "درجة الثقة"
> غير هيك. أوامر الاستنساخ مكتوبة عشان تنعاد.

**بيئة القياس:** ffmpeg 7.0.2 · Pillow 12.3.0 · raqm 0.10.5 ·
HarfBuzz 14.2.1 · Python 3.11.

**تحذير على الأرقام:** الطقم ٤٦٣ فحص أخضر. هاد **مش** دليل على صحة
المسار. `render.py` مغطّى عبر `--dry-run` يعني الأوامر اللي بتنبعت
لffmpeg، مش ناتج ffmpeg. تلاتة من أصل أربع مشاكل حرجة تحت **ما
بيقدر يمسكها ولا فحص موجود** لأنها ما بتظهر إلا بمقارنة المخطط
بالمرمَّز فعليًا.

---

## Executive Summary

| # | Finding | التأثير |
|---|---|---|
| **CR-1** | كل مقطع بينرمّز ~٣٥ms أطول من المخطط. الكابشن موقّت على المخطط -> **بيسبق الصورة ٠.٨٤s** على ٢٤ مقطع | الكابشن بينفكّ عن الصوت كل ما طال الريل |
| **CR-2** | انزياح صوت/صورة تراكمي — **٠.٣٤s** فرق بالمدة و**+١.١٩s** تراكم بمواقع النبضات على ٢٤ مقطع | تزامن الشفاه بيضيع بالريلات الطويلة |
| **CR-3** | توكن ما بينلف (رابط طويل) بينتج PNG **١٤٦١px** على إطار **١٠٨٠px** -> ffmpeg بيقصقصه | باگ "الكابشن مقصوص" بيرجع من الباب الخلفي |
| **EC-1** | القوس غير المزدوج **بينعكس**: `(Android` بتنرسم `Android)` | نص مشوّه بأي كابشن فيه أقواس |
| **PERF-1** | ١٨ عملية ffmpeg و٦٣ ملف PNG لريل ١٦ ثانية بتلات مقاسات | بطء ملموس على Termux |

**الخلاصة:** ترتيب النص المختلط صار **صحيحًا فعلًا** ومتحقَّقًا منه
آليًا. اللي لسا هش: **شكل** الأقواس، و**تزامن** الكابشن والصوت مع
الصورة. التزامن هو أخطر شي بالمشروع الآن لأنه غير مرئي بالاختبارات
وبيسوء طرديًا مع طول الفيديو.

---

## 🔴 Critical

### CR-1 · توقيت الكابشن بينزاح عن الصورة

**المشكلة.** الكابشن بينفكّ عن الصورة تدريجيًا. بريل من ٢٤ مقطع،
آخر كابشن بيظهر **٠.٨٤ ثانية قبل** الكلام تبعه.

**ليش بيحصل.** خطة القص بتحسب مقاطع بتوقيت **مستمر** (`segments_from_words`
بترجّع أزمانًا عشرية)، و`remap_words` بتبني توقيت الكابشن على نفس
الأساس. بس ffmpeg بيرمّز كل مقطع لحدود **إطارات**، فالمقطع بيطلع
أطول من المطلوب بجزء من إطار لحد إطار كامل. الفرق بيتراكم مع كل
مقطع، والكابشن بيضل موقّتًا على المخطط.

**وين بالكود.**
- `cuts.py:segments_from_words()` — أزمان مستمرة
- `cuts.py:remap_words()` — توقيت الكابشن من نفس الأزمان
- `render.py:build_base()` — `-ss`/`-to` بيتقربوا لحدود الإطارات
- `render.py:burn_captions()` — `enable='between(t,s,e)'` بأزمان المخطط

**الاستنساخ.**
```bash
# ٢٤ مقطع من فيديو ٣٦ ثانية
python -m autoreel.cli long.mp4 --srt many.srt --no-captions --keep -o out.mp4
# قارن مدة كل seg*.mp4 بالمخطط
```
القياس الفعلي: مجموع المخطط `17.902s` · الفعلي `18.740s` ·
الفرق `+0.838s` = **+٣٥ms لكل مقطع** (إطار واحد عند 30fps = 33.3ms).

**التأثير.** بيسوء **طرديًا مع عدد المقاطع**، مش مع طول الفيديو.
ريل بإيقاع سريع (`min_gap` صغيرة) بيولّد مقاطع أكتر فبينزاح أسرع.
بـ٢٤ مقطع الانزياح محسوس بوضوح.

**الحل المقترح.** لا تثق بالمخطط بعد الترميز — **اقرا المدة الفعلية**
لكل `seg*.mp4` بعد ترميزه، وابنِ `offset` تراكم الكابشن من الأزمان
الفعلية مش من `b-a`. تغيير صغير: `build_base` بترجّع قائمة المدد
الفعلية، و`remap_words` بتاخدها كمدخل بدل ما تحسب من `segs`.

بديل أنظف معماريًا: ثبّت حدود المقاطع على شبكة الإطارات **قبل**
الترميز — قرّب `a` و`b` لأقرب إطار (`round(t*fps)/fps`) وخلّي المخطط
والمرمَّز يتفقوا بالتعريف. هاد بيحل CR-1 ويسهّل CR-2.

**معماري ولا صغير؟** صغير-متوسط. تقريب الحدود لشبكة الإطارات ~٥ أسطر
بـ`segments_from_words` وبيحلها من الجذر.

**الثقة: High** — مقاس مباشرة، والفرق بيطابق مقدار إطار واحد.

---

### CR-2 · انزياح الصوت عن الصورة بيتراكم

**المشكلة.** الصوت بينزاح عن الصورة كل ما زاد عدد المقاطع.

**ليش بيحصل.** `build_base` بترمّز صوت كل مقطع **AAC لحاله**، وبعدين
`concat -c copy`. كل ملف AAC فيه priming/encoder delay بأوله، والـ
concat demuxer بينسخ الإطارات كما هي فالتأخير بيتراكم. وبيتفاعل مع
تقريب الإطارات تبع CR-1.

**وين بالكود.** `render.py:build_base()` — `-c:a aac` لكل مقطع، ثم
`-f concat -safe 0 -c copy`.

**الاستنساخ.**
```bash
# صوت فيه نبضة عند بداية كل جملة، وقياس مواقعها بعد المسار
ffmpeg -i out.mp4 -af "silencedetect=noise=-40dB:d=0.10" -f null -
```
القياس على ٢٤ مقطع:

| المقياس | النتيجة |
|---|---|
| مدة الفيديو مقابل الصوت بـ`base.mp4` | `19.180s` مقابل `18.840s` = **٠.٣٤s** |
| انزياح موقع النبضة — أول نبضة | `+0.809s` |
| انزياح موقع النبضة — آخر نبضة | `+2.003s` |
| **التراكم** | **`+1.194s`** |

الإزاحة الابتدائية (٠.٨١s) ثابتة وما بتضرّ لحالها. **التراكم
(+١.١٩s) هو الباگ** — ما ينفسّر بإزاحة ثابتة.

**التأثير.** تزامن الشفاه بيضيع بآخر الريل. بريل ٦٠ ثانية بإيقاع
سريع الانزياح بيتعدى الثانيتين.

**الحل المقترح.** شوف `Audio Sync Review` تحت — قارنت تلات معماريات.

**معماري ولا صغير؟** **معماري.**

**الثقة: High** — مقاس بطريقتين مستقلتين (مدة التيارات، ومواقع النبضات).

---

### CR-3 · توكن ما بينلف بينتج كابشن أعرض من الإطار

**المشكلة.** رابط طويل (أو أي توكن بلا مسافات) بينتج PNG أعرض من
الفيديو، وffmpeg بيقصقصه — يعني **باگ "الكابشن مقصوص" بيرجع**، بس
بطبقة تانية.

**ليش بيحصل.** `_wrap()` بترجّع `None` لما كلمة وحدها أعرض من السطر،
فـ`_fit()` بتكمّل تصغير. عند أرضية ٤٥٪ بتستسلم وبترجّع
`lines or [words]` — يعني **كل الكلمات على سطر واحد** مهما كان عرضه.
`assert_fits_frame()` بتفحص **الارتفاع بس**، فما بتمسكها.

**وين بالكود.**
- `captions.py:_fit()` السطر الأخير — `return hard, lines or [words]`
- `captions.py:assert_fits_frame()` — `y`/`H` فقط، ولا فحص للعرض
- `render.py:burn_captions()` — `overlay=x=(W-w)/2` بتعطي x سالبة

**الاستنساخ.**
```python
CAP.render_caption("https://sub.domain.example.com/a/very/long/path/"
                   "segment/that/cannot/possibly/wrap/anywhere", cfg, 1080)
# -> عرض الصورة 1461px مقابل حد 1020px
```
مؤكَّد بصريًا على إطار محروق: النص بيبلّش من نص `domain.example.com`
وبينتهي بـ`wra` — مقصوص من الطرفين.

**التأثير.** أي كابشن فيه رابط أو اسم ملف طويل بيطلع مشوّه. الاحتمال
مش نظري — فيديو تقني عربي فيه روابط بشكل طبيعي.

**الحل المقترح.** تلات طبقات:
1. **كسر التوكن الطويل** عند حدود منطقية (`/`, `-`, `_`, `.`) قبل
   الاستسلام. رابط بينكسر لسطرين بشكل مقروء.
2. **وسّع `assert_fits_frame`** لتفحص العرض كمان — نفس المنطق:
   خطأ يوقف المقاس مش تحذير.
3. **آخر ملجأ:** لو ما ظبطت، اقصّ التوكن بـ`…` صراحة — قصّ مقصود
   ومرئي أفضل من قصّ صامت بحدود الإطار.

**معماري ولا صغير؟** صغير — `_wrap` تتعلم تكسر التوكن، و
`assert_fits_frame` تفحص بُعدين بدل واحد.

**الثقة: High** — مقاس ومرئي.

---

## 🟠 Edge Cases / Potential Bugs

### EC-1 · القوس غير المزدوج بينعكس

**المشكلة.** `(Android` بتنرسم `Android)` و`App)` بتنرسم `(App`.
فـ`حمّل التطبيق (Android App) الآن` بتطلع `حمّل التطبيق Android) (App الآن`.

**ليش بيحصل.** حلقة الرسم بترسم **كل توكن معزول** بـ`direction="rtl"`
دايمًا. القوس محرف محايد (`ON`)؛ لما ينرسم لحاله بقاعدة RTL، UAX#9
بيحلّه لاتجاه القاعدة `R` فـ**بينعكس** (mirroring). بالسطر الكامل
القوس محاط بمحارف لاتينية فبيضل `L` وما بينعكس.

**وين بالكود.** `captions.py:render_caption()` —
`dr.text(..., direction="rtl", language="ar")` بلا استثناء.

**الاستنساخ.**
```python
draw.text(xy, "(Android", font=f, direction="rtl", language="ar")  # -> Android)
draw.text(xy, "(Android", font=f, direction="ltr", language="ar")  # -> (Android
```
الأقواس المزدوجة داخل نفس التوكن (`(A)`, `[x]`, `{y}`) سليمة —
بيتعاكسوا الاتنين فبينلغي الأثر.

**التأثير.** نص مشوّه ومربك. مش نادر: الأقواس شائعة بالكلام التقني.

**الحل المقترح — متحقَّق منه.** ارسم كل توكن **باتجاه الـrun تبعه**:
`'L'` -> `direction="ltr"`، `'R'`/`'N'` -> `"rtl"`. جرّبته:
كل الحالات صارت تطابق المرجع، و**المقاسات ما تغيّرت** فـ`_fit` ما
بتتأثر والصور المرجعية بتضل صالحة.

لازم ياخد الاتجاه **المحلول** من `_bidi_runs` (اللي بيضم المحايدات
للـrun اللي قبلها) مش من `_token_dir` لحالها — عشان توكن زي `(`
لحاله بياخد اتجاه سياقه. يعني `_bidi_runs` لازم ترجّع الاتجاهات كمان
مش الترتيب بس.

**معماري ولا صغير؟** صغير — `_bidi_runs` ترجّع `(order, dirs)`،
وحلقة الرسم تمرّر `direction`.

**الثقة: High** — مقاس بصريًا، والحل مجرَّب.

---

### EC-2 · الemoji بتطلع مربعات فاضية

**المشكلة.** `شغال 🔥 هلأ` بتنرسم بمربع فاضي مكان الرمز.
`👨‍👩‍👧` بتطلع **تلات** مربعات (تسلسل ZWJ ما بينركّب).

**ليش بيحصل.** Tajawal ما فيه glyphs للemoji، وPillow **ما بيعمل font
fallback**. المحرف الناقص بيطلع `.notdef` يعني مربع.

**وين بالكود.** `captions.py:_font()` — خط واحد لكل شي.

**الاستنساخ.** `CAP.render_caption("شغال 🔥 هلأ", cfg, 1080)` وشوف
الناتج.

**التأثير.** Whisper ما بيولّد emoji، فالمسار الطبيعي آمن. بيظهر بس
مع `--srt` من ملف فيه رموز. أثر متوسط بس مرئي وقبيح.

**الحل المقترح.** إما (أ) شيل المحارف اللي ما إلها glyph من النص مع
تحذير — `font.getmask(ch)` بتكشفها؛ أو (ب) خط emoji احتياطي ورسم على
طبقتين. (أ) أبسط بكتير وكافية لهالمشروع.

**معماري ولا صغير؟** صغير مع (أ).

**الثقة: High** — مقاس بصريًا.

---

### EC-3 · `group_words` بتخلط جُمل مختلفة بكابشن واحد

**المشكلة.** كابشن واحد بيجمع نهاية جملة وبداية اللي بعدها:
`هلأ شغال على Flutter` — `هلأ` آخر كلمة بجملة و`شغال على Flutter`
بداية التانية.

**ليش بيحصل.** `group_words` بتقسّم على **عدد الكلمات** و**الفجوة
الزمنية** بس. ما بتعرف حدود الجمل — لا من ترقيم ولا من حدود كتل
الـSRT.

**وين بالكود.** `captions.py:group_words()`.

**الاستنساخ.** شغّل بـ`--srt` على ملف جمله أقصر من `max_words`،
وشوف المجموعات.

**التأثير.** الكابشن بيقرا غريب. أثر تجميلي بس ملموس، خصوصًا مع
`max_words=4` وجمل قصيرة.

**الحل المقترح.** اقطع المجموعة عند **نهاية جملة**: كلمة بتنتهي بـ
`.؟!،؛` أو حدود كتلة SRT (بتحتاج `from_srt` تحمل معرّف الكتلة).

**معماري ولا صغير؟** صغير.

**الثقة: Medium** — السلوك مؤكَّد، بس "هل هو مشكلة" قرار ذوقي.

---

### EC-4 · `--dry-run` ما بيمنع كتابة الملفات

**المشكلة.** الاسم بيوحي إنه ما بيكتب شي، وفعليًا بيكتب **كل ملفات
PNG للكابشن** وملف `concat.txt`.

**ليش بيحصل.** `dry_run` بينمرَّر لـ`render.run()` بس — بيوقف نداء
ffmpeg. رسم الكابشن ببايثون بيشتغل عادي.

**وين بالكود.** `render.py:run()` مقابل `captions.py:build_caption_pngs()`.

**التأثير.** منخفض عمليًا (الملفات بمجلد temp وبتنحذف)، بس بيربك.
وبيتعارض مع فحص موجود اسمه `test_dry_run_produces_no_video` — الفحص
بيتأكد من `*.mp4` بس.

**الحل المقترح.** إما وثّق صراحة ("جاف بالنسبة لffmpeg") — وهو
مقصود فعلًا لأنه بيخلي المسار قابلًا للاختبار — أو ضيف `--dry-run=full`.
**توصيتي: توثيق، مش تغيير.** رسم الكابشن هو نص قيمة `--dry-run`.

**الثقة: High.**

---

### EC-5 · المحايد بعد لاتيني بينضم للـrun اللاتيني

**المشكلة.** `،` (الفاصلة العربية) صنفها `CS` يعني محايد. لو إجت
كتوكن مستقل بعد كلمة لاتينية، بتنضم للـrun اللاتيني وبتنرسم بمكان
غير متوقع.

**وين بالكود.** `captions.py:_bidi_runs()` خطوة حلّ المحايدات.

**التأثير.** منخفض — الترقيم عادةً ملصوق بالكلمة مش مفصول بمسافة.
ما قدرت أنتج حالة واقعية بتنكسر.

**الحل المقترح.** لا تعمل شي الآن. سجّلها كحد معروف.

**الثقة: Medium** — التصنيف مؤكَّد، الأثر العملي غير مؤكَّد.

---

### EC-6 · مدخل بمعدل إطارات متغيّر (VFR)

**المشكلة.** `-ss`/`-to` مع VFR ممكن يعطي مدد مقاطع غير متوقعة،
وهاد بيضخّم CR-1.

**وين بالكود.** `render.py:build_base()`. فلتر `fps={fps}` بيفرض CFR
**داخل** المقطع، بس القص بينعمل قبله.

**التأثير.** فيديو الموبايل غالبًا VFR. يعني هاد **المدخل الشائع**
مش الحالة النادرة.

**الحل المقترح.** أضف `-vsync cfr` أو طبّع المدخل مرة وحدة قبل القص.
ينحل مع تقريب حدود الإطارات المقترح بـCR-1.

**الثقة: Low** — ما اختبرته على مدخل VFR حقيقي. **بيحتاج تحقق.**

---

## 🟡 Performance

### PERF-1 · عدد عمليات ffmpeg وملفات الوسيط

**القياس** على ريل ١٦ ثانية، ٤ مقاطع، ٢١ إطار كابشن، ٣ مقاسات:

| المقياس | العدد |
|---|---|
| عمليات ffmpeg | **١٨** (٦ لكل مقاس: ٤ ترميز مقطع + ١ concat + ١ حرق) |
| ملفات PNG | **٦٣** |
| ترميزات فيديو كاملة | ٦ (٣ concat + ٣ حرق) |

بيتوسّع خطيًا: `(مقاطع + 1 + ceil(إطارات/60)) × مقاسات`.

**ليش مهم على Termux.** إطلاق عملية غالي، وكل ترميز بيقرا ويكتب
الفيديو كامل. الوقت المقاس: ١٧.٣s لتصدير ٣ مقاسات من ١٦ ثانية
مصدر — يعني **أبطأ من الزمن الحقيقي**.

**الحل المقترح.** شوف `Single-Pass Encoding Review`.

**الثقة: High** — معدود من الكود ومقاس بالتشغيل.

---

### PERF-2 · إعادة ترميز المقاطع لكل مقاس

`build_base` بتنعاد كاملة لكل مقاس. القص المنطقي مشترك، بس الترميز
لأ. لتلات مقاسات: ٣× كلفة قص المقاطع.

**قرار مقصود** — اتّخذ عشان ما نلمس `render.py` قبل ما تصير مختبَرة.
صار عندنا `--dry-run` واختبارات هندسة، فالمبرر انتهى.

**الحل:** master مقصوص مشترك بدقة المصدر، والهندسة لكل مقاس بعده.
بيوفّر ~٣٣٪.

**الثقة: High.**

---

### PERF-3 · PNG لكل كلمة لكل مقاس

٢١ إطار كابشن × ٣ مقاسات = ٦٣ PNG بحجم كامل. الصورة الواحدة
~٩٣٤×٢٢٦ RGBA. بتتولد وبتنكتب وبتنقرا من ffmpeg.

**الحل:** الكابشن بيتغيّر بس عند حدود الكلمات، والصورة الواحدة
بتفرق عن اللي قبلها بلون كلمة وحدة. ممكن نرسم صورة **المجموعة** مرة
ونعمل طبقة تلوين صغيرة فوقها — بس هاد بيعقّد المسار. الأربح: تقليل
عدد الـoverlays (شوف Single-Pass) مش تقليل الرسم.

**الثقة: Medium** — الوفر الحقيقي بيعتمد على قياس مين الأغلى.

---

## 🟢 Good Decisions

| القرار | ليش صح — لا تعكسه |
|---|---|
| **PNG overlay بدل ASS** | `libass` بيعتمد fribidi ومكدّس مختلف = نتيجة مختلفة. الPNG بيضمن الشكل لأننا رسمناه وشفناه. الثمن (عدد الملفات) مقبول مقابل ضمان الشكل |
| **القص من word timestamps** | حدود لغوية مش صوتية. وبيخلي `remap_words` ممكنة أصلًا — القص والكابشن على نفس الأساس |
| **رفض `python-bidi`** | الاستدلال هو القيمة: الخطر مش بشو بتعمله المكتبة، بل بشو بتغري فيه بعد ستة أشهر. تمييز نادر بين "شو بيشتغل اليوم" و"شو بينكسر لاحقًا" |
| **`_fit` ما بتقصّ النص** | مسح تنازلي ثم لف سطرين. الحد الوحيد CR-3 (توكن ما بينلف) وهو قابل للإصلاح ضمن نفس التصميم |
| **قاعدة "كل مفتاح config عليه فحص بيقرا الملف الحقيقي"** | كشفت مفتاحًا ميتًا (`captions.stroke`) ومخالفة معمارية (`remap_words` تنداء ٣ مرات) بأول تشغيل |
| **`--dry-run`** | الشي الوحيد اللي خلّى `render.py` قابلة للاختبار. كل بند 🟡 وأغلب 🔴 هون انقاسوا بفضله |
| **`logical_index` مقابل `draw_position`** | تسمية صريحة بمكان بيولّد باگات صامتة |
| **المشترَك بينحسب مرة** | `remap_words` مرة وحدة بيضمن توقيتًا متطابقًا بين المقاسات |
| **`--preview-frames`** | يعالج مشكلة حقيقية: `crop_bias` معايَر على عيّنة، والمعاينة ٢٥× أسرع من التصدير |

---

## Mixed Arabic/Latin Deep Dive

**النتيجة العامة: الترتيب صحيح. الشكل فيه ثغرة وحدة (EC-1).**

### طريقة التحقق

المرجع مش رأيي: بنرسم السطر بنداء **واحد** فيطبّق raqm bidi كامل،
وبنقارن ترتيب `_bidi_runs` فيه. المقارنة بعروض التوكنات بتسامح ٣٪.

**حد الأداة، موثّق:** المقارنة بالعروض، فتوكنان بعرض متقارب ممكن
يتبادلوا بدون ما تنكشف. **وهي عمياء تمامًا عن انعكاس الشكل** — القوس
المنعكس نفس العرض بالضبط. لهيك EC-1 ما انمسك بالمقارنة الآلية وانمسك
بالفحص البصري بس. **هاي ثغرة بين الاختبارات والسلوك الفعلي.**

### الحالات المختبَرة — ١٧ حالة، صفر اختلاف بالترتيب

| الفئة | الحالات | الترتيب |
|---|---|---|
| عربي + إنجليزي | `شغال على Flutter Web` | ✅ |
| عربي + أرقام | `الإصدار 2.5 جاهز` · `سعره 100 دولار` | ✅ |
| روابط | بسيط · بمسار · باستعلام `?q=1&x=2` · بشرطة وسفلية | ✅ |
| أقواس | `(Android App)` · `(Flutter Web) شغال` · `() [] {}` | ✅ ترتيب · ❌ شكل |
| ترقيم عربي | `؟ ، ؛` | ✅ |
| ترقيم مختلط | `, ; ? ! : .` | ✅ |
| انتقالات متعددة | `افتح App Store ثم نزّل WhatsApp من الرابط https://… الآن` | ✅ |
| أرقام جوا لاتيني | `iPhone 15 Pro Max` · `version 2.1.0` | ✅ |
| emoji | `شغال 🔥 على Flutter` | ✅ ترتيب · ❌ شكل (EC-2) |
| تشكيل | `مُحَمَّد شغال على Flutter` | ✅ |

**الروابط سليمة تمامًا** بالترتيب: الرابط كله توكن واحد صنفه `L`
(أول محرف قوي `h`)، والمحارف الخاصة (`:` `/` `?` `&` `=` `-` `_` `.`)
كلها محايدة **جوا** التوكن فبيحلّها raqm داخليًا صح. الخطر مع الروابط
مش الاتجاه — هو الطول (CR-3).

### Highlight timing — تحليل التصميم

هاد أهم سؤال بالقسم، والجواب **مش باگ**:

```
Whisper word index  ──►  logical_index  ──►  highlight_idx
                                   │
                              _bidi_runs
                                   ▼
                            draw_position  ──►  الموضع البصري
```

`highlight_idx` بيشير للفهرس **المنطقي**. حلقة الرسم بتحسب
`logical_index = line_start + draw_position` وبتلوّن لما يطابق. يعني
**التلوين بيتبع الكلمة، مش الموضع.**

**التحقق العملي** — `test_draw_order_follows_bidi_runs`: بنلوّن كل
توكن بدوره، بنسجّل مركزه الأفقي، وبنرتّب الفهارس حسب المركز. الناتج
لازم يساوي `_bidi_runs` بالضبط. مطبَّق على ١٤ حالة.

**على `حمّل من App Store هلأ` لما يجي وقت `App`:**
التوكن `App` فهرسه المنطقي ٢. `_bidi_runs` بترجّع `[4, 2, 3, 1, 0]`
يعني موضعه البصري **الثاني من اليسار**. والتلوين بيوقع هناك بالضبط —
مؤكَّد بإطار محروق.

**سلوك بصري يستاهل التوثيق (مش باگ):** جوا الـrun اللاتيني، التلوين
بيمشي **من اليسار لليمين** بينما بالعربي المحيط بيمشي من اليمين
لليسار. يعني بجملة زي `حمّل من App Store هلأ` التلوين بيقفز:
`حمّل`(يمين) ← `من` ← `App`(قفزة لليسار) → `Store`(لليمين) ←
`هلأ`(قفزة لليسار). **هاد صحيح حسب bidi** وهو نفس اللي بيصير بأي
محرر نصوص، بس بيبيّن غريبًا بالحركة السريعة. لو صار شكوى، الحل
تجميلي (تقليل `max_words` مثلًا) مش منطقي.

### الحالات اللي ما انفحصت

- **مستويات متداخلة** (RTL جوا LTR جوا RTL) — خارج تصميم الحل.
- **علامات الاتجاه الصريحة** (LRM/RLM) — بتنتجاهل.
- **run بينقسم على سطرين** — بينرتّب داخل كل سطر لحاله. عليه اختبار
  صريح، بس السلوك مختلف عن UAX#9.
- **العبرية أو أي RTL تاني** — `_token_dir` بتقبل `R` فبتشتغل نظريًا،
  بس ما انفحصت ولا في صور مرجعية إلها.

---

## Audio Sync Review

### هل المشكلة موجودة فعلًا؟ نعم، ومقاسة

المعمارية الحالية **بالضبط** كما وُصفت:
1. قص الفيديو لمقاطع (`-ss`/`-to` لكل مقطع)
2. ترميز كل مقطع، **الصوت AAC لحاله** (`-c:a aac -b:a 128k`)
3. `concat -f concat -safe 0 -c copy`

**مصدر الانزياح — تلات مساهمين، مش واحد:**

| المصدر | المقدار المقاس | بيتراكم؟ |
|---|---|---|
| تقريب حدود الإطارات بالفيديو | +٣٥ms/مقطع | ✅ نعم |
| AAC priming/padding لكل مقطع | ضمن الـ٠.٣٤s الإجمالية | ✅ نعم |
| إزاحة ابتدائية ثابتة | +٠.٨١s | ❌ لأ |

**قابل للقياس؟ نعم**، بطريقتين مستقلتين:
```bash
# ١) مدة التيارين بشكل منفصل
ffmpeg -i base.mp4 -map 0:a -c:a pcm_s16le a.wav
ffmpeg -i base.mp4 -map 0:v -c copy v.mp4
# ٢) مواقع النبضات — الأدق، بيميّز التراكم عن الإزاحة الثابتة
ffmpeg -i out.mp4 -af "silencedetect=noise=-40dB:d=0.10" -f null -
```

### مقارنة المعماريات

**A — الحالي: ترميز لكل مقطع ثم concat copy**
- ✅ بسيط، ذاكرة قليلة، كل مقطع مستقل
- ❌ priming بيتراكم · ❌ ما بيحل تقريب الإطارات · ❌ ٣ ترميزات صوت
- **الحكم: مش صالح للريلات الطويلة.**

**B — PCM موحّد: استخراج الصوت كامل، القص على PCM، ترميز AAC مرة**
- ✅ بيشيل priming تمامًا — ترميز واحد يعني تأخير واحد
- ✅ القص على عيّنات، دقة ~٢٠µs
- ❌ **ما بيحل CR-1** — الفيديو لسا بينقرّب لحدود الإطارات، فالصوت
  بيصير دقيقًا والفيديو لأ، **والفجوة بتضل**
- كلفة القرص: PCM 48kHz ستيريو 16-bit = **١١.٥ MB/دقيقة**. ريل ١٠
  دقائق = ١١٥MB مؤقت. مقبول حتى على Termux
- الذاكرة: بالبثّ عبر ffmpeg، مش بالتحميل الكامل. RAM ثابتة

**C — المقترَح: PCM موحّد + حدود مقاطع على شبكة الإطارات**
- كل اللي بـB، **زائد** تقريب `a`/`b` لأقرب إطار **قبل** القص
- ✅ المخطط والمرمَّز بيتفقوا **بالتعريف**، فـCR-1 بينحل من الجذر
- ✅ توقيت الكابشن بيصير صحيحًا بلا قياس بعدي
- ✅ الصوت بينقصّ عند نفس النقاط الزمنية بالضبط
- ❌ حدود المقاطع بتتحرك ≤ ١٦ms — غير محسوس

### التوصية

**C.** B لحاله بيصلّح نص المشكلة وبيخلي التاني مخفيًا أكتر — الصوت
بيصير دقيقًا والفيديو مقرَّب، فالانزياح بيضل بس بيصير أصعب تشخيصًا.

**هل تستحق الآن؟** **نعم، وقبل PERF.** التزامن مشكلة صحة مش أداء،
بتسوء طرديًا مع الاستعمال، ومش مرئية بالاختبارات. وتقريب الشبكة
(الجزء اللي بيحل CR-1) **٥ أسطر** وبينفّذ لحاله قبل إعادة هيكلة الصوت.

**ترتيب التنفيذ:** ١) تقريب الشبكة (يحل CR-1، صغير) ← ٢) قياس المتبقي
← ٣) PCM موحّد لو المتبقي محسوس.

---

## Single-Pass Encoding Review

### ليش في دفعات ٦٠ أصلًا

`burn_captions(batch=60)`. السبب الموثّق: "سلسلة overlay طويلة بتاكل
ذاكرة وبتكسر ffmpeg على الموبايل".

**التقييم: السبب حقيقي، بس الحل غلط.** كل إطار كابشن بينحط كـ
**مُدخل منفصل** (`-i cap00000.png -i cap00001.png …`) وoverlay منفصل.
ريل دقيقة فيه ٢٠٠ كلمة = ٢٠٠ مدخل و٢٠٠ overlay بسلسلة واحدة. هاد
فعلًا بيكسر ffmpeg. بس الدفعات بتعالج العرَض: **كل دفعة ترميز كامل
للفيديو**، فريل بـ٢٠٠ إطار = ٤ ترميزات متتالية، كل وحدة بتقرا وتكتب
الفيديو كامل، مع خسارة أجيال.

### المعمارية المقترحة — تمريرة واحدة

**المفتاح: الكابشن مش ٢٠٠ صورة، هو طبقة فيديو شفافة وحدة.**

```
# ١) الكابشن كتسلسل صور بمعدل الإطارات
#    اكتب cap_%06d.png لكل إطار فيديو (تكرار الملف نفسه بين الكلمات)
#    أو أفضل: فيديو شفاف بترميز بلا فقد
ffmpeg -framerate 30 -i caps/cap_%06d.png -c:v qtrle caption_track.mov

# ٢) تمريرة وحدة: قص + زوم + هندسة + كابشن + ترميز
ffmpeg -i src.mp4 -i caption_track.mov -filter_complex "
  [0:v]trim=1.2:3.4,setpts=PTS-STARTPTS,<segment_filter #0>[v0];
  [0:v]trim=5.0:7.1,setpts=PTS-STARTPTS,<segment_filter #1>[v1];
  ...
  [v0][v1][...]concat=n=N:v=1:a=0[base];
  [0:a]atrim=1.2:3.4,asetpts=PTS-STARTPTS[a0];
  ...
  [a0][a1][...]concat=n=N:v=0:a=1[aud];
  [base][1:v]overlay=x=(W-w)/2:y=Y-h/2[out]
" -map "[out]" -map "[aud]" -c:v libx264 -c:a aac out.mp4
```

**هاد بيحقق كل المتطلبات:**

| المتطلب | كيف |
|---|---|
| تلوين كلمة كلمة | محفوظ — طبقة الكابشن فيها الإطار الصح بكل لحظة |
| العربي/RTL والنص المختلط | ما بيتغيّر — نفس `render_caption` |
| الزوم لكل مقطع | `trim` + `segment_filter` لكل مقطع بنفس الرسم |
| القص والنسب الثلاث | `segment_filter` كما هي، مرة لكل مقاس |
| **الصوت** | `atrim` + `concat` على نفس الرسم = **بيحل CR-2 مجانًا** |

**المكسب:** من `(مقاطع + 1 + دفعات) × مقاسات` عملية إلى
`1 + مقاسات` — من **١٨** لـ**٤** بالمثال المقاس. وترميز واحد يعني
**صفر خسارة أجيال**.

### شو بيمنعها اليوم

**ولا شي معماريًا.** التلات عوائق كلها قابلة للحل:

1. **حجم طبقة الكابشن.** `qtrle` بلا فقد مع شفافية على ١٠٨٠×١٩٢٠
   بمعدل ٣٠ إطار — الحجم كبير. **الحل:** الكابشن بياخد شريطًا صغيرًا
   من الإطار، فارسم الطبقة بمقاس الشريط بس (مثلًا ١٠٨٠×٣٠٠) وحطها
   بـ`overlay` عند `y`. بيقلل الحجم ٦×.
2. **`trim` بيحتاج فك ترميز المدخل كامل مرة.** حاليًا كل مقطع
   بيسيك لحاله. للفيديوهات الطويلة مع مقاطع قليلة الحالي أسرع؛ مع
   مقاطع كتير (وهي الحالة الشائعة) التمريرة الواحدة أسرع بكتير.
3. **طول سلسلة الفلاتر** بيصير `O(مقاطع)` بدل `O(إطارات كابشن)` —
   ٢٤ بدل ٢٠٠. ضمن المدى الآمن.

**التوصية:** نفّذها **بعد** تقريب شبكة الإطارات، مش قبل. التمريرة
الواحدة بتخلي `trim` يشتغل على أزمان دقيقة، والنتيجة بتكون صحيحة
بالتعريف بدل ما نصلّح انزياحًا موجودًا.

---

## Android/Termux Profiling Plan

**لا تحسّن قبل ما تقيس.** الأرقام تحت من ×86 وليست من Termux —
نسب الكلفة على ARM بتختلف كتير، خصوصًا Whisper.

### نقاط القياس

ضيف عدّادًا حول كل مرحلة (نقطة واحدة لكل مرحلة، وطباعتها مع `--keep`):

| المرحلة | وين | شو تقيس |
|---|---|---|
| `probe_duration` | `cuts.py` | ثواني |
| تفريغ Whisper | `transcribe.py` | ثواني + ذروة RAM |
| استخراج الصوت | (غير موجود حاليًا) | — |
| خطة القص | `segments_from_words` | ثواني (متوقع: مهملة) |
| رسم الكابشن | `build_caption_pngs` | ثواني + عدد الملفات + مجموع البايتات |
| ترميز المقاطع | `build_base` | ثواني لكل مقطع + عدد العمليات |
| الحرق | `burn_captions` | ثواني لكل دفعة + عدد الدفعات |
| قرص | كل مكان | ذروة استهلاك مجلد temp |

**قياس جاهز بلا كود:**
```bash
/usr/bin/time -v python -m autoreel.cli in.mp4 --sizes all -o out.mp4
du -sh $TMPDIR/autoreel_*          # مع --keep
ls $TMPDIR/autoreel_*/caps_*/ | wc -l
```

### CPU أم ذاكرة أم قرص؟

**فرضيتي، بتحتاج تأكيد على جهاز حقيقي:**
- **Whisper = CPU وذاكرة.** موديل `small` بـint8 ~٥٠٠MB. على جهاز
  بـ٤GB هاد الخانق. **متكاش**، فبيتدفع مرة وحدة لكل فيديو.
- **الترميز = CPU.** ٦ ترميزات كاملة لتلات مقاسات، `veryfast`
  بيخفف بس ما بيلغي.
- **رسم الكابشن = CPU + قرص.** ٦٣ PNG لريل ١٦ ثانية.
- **القرص = الحجم مش السرعة.** الوسيط: مقاطع + base + تمريرات + PNG.

### أعلى ٣ تحسينات بالـROI

**١. التمريرة الواحدة (Single-Pass).** من ١٨ عملية لـ٤، ومن ٦
ترميزات لـ٣. أكبر مكسب مفرد، وبيحل CR-2 معه. **ROI: عالي جدًا.**

**٢. master مقصوص مشترك.** القص مرة بدل ٣ مرات. يوفّر ~٣٣٪ لو ما
انعملت التمريرة الواحدة. **لو انعملت (١) هاد بيصير جزءًا منها** —
فنفّذ (١) مباشرة.

**٣. تخطي المقاسات اللي ما بتنعرض.** ثقافي مش تقني: `--sizes all`
افتراضيًا مغرٍ، وأغلب التشغيلات بتحتاج `reel` بس. الافتراضي الحالي
(مقاس واحد) صح — **لا تغيّره**، ووثّق الكلفة.

**اللي ما بنصح فيه:** `multiprocessing`. الترميز محكوم بـCPU وffmpeg
بيوازي داخليًا أصلًا؛ عمليات بايثون متوازية رح تتزاحم على نفس النوى
وتضاعف ذروة الذاكرة على جهاز أصلًا مضغوط.

---

## Additional Findings

بنود ما انسألت عنها. **مش كلها مشاكل** — التصنيف حسب النتيجة الفعلية.

| البند | النتيجة | التصنيف |
|---|---|---|
| **تشكيل عربي** (`مُحَمَّد`) | بينرسم صح، `NSM` ما بيأثر على `_token_dir` | 🟢 |
| **تطبيع يونيكود** NFC/NFD | نفس الرسم بالضبط — raqm بيطبّع داخليًا | 🟢 |
| **تسلسل ZWJ** | `👨‍👩‍👧` بتطلع ٣ مربعات — ما بينركّب | 🟠 مع EC-2 |
| **فيديو بلا صوت** | اشتغل، كود الخروج ٠. `-c:a aac` و`-map 0:a?` بيتعاملوا | 🟢 |
| **قص بيبلّش/بينتهي جوا كلمة** | العتبة التراكمية (٤٥٪) بتتعامل معها، والكلمة المنقسمة بتندمج | 🟢 |
| **كلمات بتنشال بالقص** | صار في تحذير بيسردها، ونفس قاعدة `remap_words` بالضبط | 🟢 |
| **تنظيف الملفات المؤقتة** | `shutil.rmtree` بـ`finally`، وتمريرات الحرق بتنحذف فور استهلاكها | 🟢 |
| **الفشل الجزئي** | فشل مقاس ما بيوقف الباقي، وكود الخروج ≠ ٠ | 🟢 |
| **فيديو عمودي مصدرًا** | مدعوم، وهو الحالة الأساسية | 🟢 |
| **مدخل منخفض الدقة** | `scale=…:increase` بيكبّر — بيطلع ناعم بلا خطأ. **ما في تحذير** لو المصدر أصغر من الهدف | 🟡 |
| **معدل إطارات غير معتاد** | `fps={fps}` بيفرض CFR داخل المقطع | 🟡 EC-6 |
| **فيديو طويل جدًا** | ما انفحص فوق ٣٦ ثانية. CR-1 وCR-2 بيسوؤوا خطيًا | 🔴 ضمن CR-1/CR-2 |
| **إطارات مكررة/ناقصة** | ما انفحص. `fps` بيدرج/بيسقط إطارات عند الحدود | Low confidence |
| **font fallback** | ما في. أي محرف برّا Tajawal = مربع | 🟠 EC-2 |
| **ذاكرة PNG** | الصور بتنكتب فورًا وما بتنخزّن بالذاكرة | 🟢 |
| **`parse_silencedetect`** | كود ميت — ولا مستدعي | 🟡 |
| **تحذير الحجم < ٥٥** | بيشتغل، وبيشترط تصغيرًا فعليًا فما بيطلع كذبًا | 🟢 |
| **ثغرة اختبارية** | المقارنة الآلية للنص المختلط **عمياء عن شكل الglyph** — EC-1 ما انمسك إلا بصريًا | 🔴 منهجي |

**آخر بند هو الأهم بالجدول.** الاختبارات الحالية بتفحص **الترتيب**
مش **الشكل**. الصور المرجعية بتغطي الشكل، بس تغطيتها للحالات
المختلطة كانت ٥ حالات وما فيها ولا قوس. **اقتراح:** ضيف صورة مرجعية
لـ`حمّل التطبيق (Android App) الآن` — بتمسك EC-1 وأي انحدار شكل مشابه.

---

## Recommended Order of Work

مرتّبة حسب: صحة ← باگات مرئية ← تزامن ← أداء ← صيانة.

### ١. صحة — قبل أي شي

| # | العمل | الحجم |
|---|---|---|
| CR-1 | تقريب حدود المقاطع لشبكة الإطارات بـ`segments_from_words` | ~٥ أسطر |
| CR-3 | كسر التوكن الطويل + `assert_fits_frame` تفحص العرض | صغير |
| — | صورة مرجعية للأقواس (تمسك EC-1 قبل ما تصلّحه) | صغير |

### ٢. باگات مرئية

| # | العمل | الحجم |
|---|---|---|
| EC-1 | ارسم كل توكن باتجاه الـrun تبعه — **الحل مجرَّب** | صغير |
| EC-2 | شيل المحارف اللي بلا glyph مع تحذير | صغير |
| EC-3 | اقطع مجموعة الكابشن عند نهاية الجملة | صغير |

### ٣. التزامن

| # | العمل | الحجم |
|---|---|---|
| CR-2 | قِس المتبقي بعد CR-1، وبعدها PCM موحّد لو محسوس | معماري |
| EC-6 | تحقق من مدخل VFR حقيقي | تحقق أولًا |

### ٤. الأداء

| # | العمل | الحجم |
|---|---|---|
| PERF-1 | التمريرة الواحدة — بتشمل PERF-2 وبتساعد CR-2 | معماري |
| — | قياس على Termux **قبل** التنفيذ | تحقق |

### ٥. الصيانة

| # | العمل |
|---|---|
| — | احذف `parse_silencedetect` أو أعطها علمًا يشغّلها |
| — | وثّق إن `--dry-run` جاف بالنسبة لffmpeg بس |
| — | تحذير لما المصدر أصغر من المقاس الهدف |
| — | صور مرجعية لRTL غير العربي لو انفتح المجال |

---

### ملاحظة أخيرة على المنهج

تلاتة من أربع مشاكل حرجة انكشفت بمقارنة **المخطط بالمرمَّز فعليًا**،
وهاي مقارنة ما بيعملها ولا فحص من الـ٤٦٣. والرابعة (EC-1) انكشفت
بالنظر للصورة، مش بالأرقام.

الدرس القابل للتنفيذ: أضف فحصًا واحدًا بيرمّز مقطعًا حقيقيًا ويقارن
مدته الفعلية بالمخططة. بطيء، فحطّه خلف علامة `@pytest.mark.slow`.
بيمسك CR-1 وCR-2 وأي انحدار تزامن جاي.
